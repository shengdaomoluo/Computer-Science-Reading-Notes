## 11.3  推理

探讨开发具有基础推理能力的智能体技术。

### 11.3.1  产生式系统

**产生式系统**（production system）：是人工智能领域的一种重要的知识表示和推理机制。它通过规则库、工作内存和推理机协同工作。

给机器编写程序，使机器能够完成基本的推理活动。产生式系统就是解决一类具有共同特征的推理问题的抽象实体。产生式系统由 3 个主要部分组成：

* ***状态集合***（collection of states）
  * **状态**（state）：是指应用环境中存在的情形。
    * **开始状态**（start state）：即现状。
    * **目标状态**（goal state）：即期望实现的情形。
* ***产生式系统集合***（collection of productions）即**规则**（rules）或**步骤**（moves）
  * **产生式**（production）：是指能在应用环境中完成的让系统从一种状态转换到另一种状态的操作。<br>每个产生式都会关联一些先决条件，也就是说，在产生式被应用之前，环境中必须出现一些可能让产生式发挥作用的条件。

* ***控制系统***（control system）

  * 控制系统：就是根据**初始状态**， 在**产生式系统集合**中选择某个**产生式**，以实现**目标状态**的（选择）活动。

  在这个过程中，控制系统必须必须决定接下来应该根据当前状态应用产生式中的哪一个来达到目标状态。

**问题空间**（problem space）：是产生式系统中所有状态集合、产生式集合和控制系统的集合。<br>问题空间通常会用采用**状态图**（state graph）的形式表达。

* **状态图**（state graph）：是指一种**有向图**（directed graph）的结构，即一组由箭头连接起来的节点组成。其中节点表示系统中的状态，箭头表示系统从一个状态转换到另一个状态的产生式。

状态图是概念化手头问题的一种方法，但不能用来表示全部内容。

在产生式系统中，控制系统的工作就是搜索状态图，找出一条从开始节点到目标节点的路径。完成这种搜索的简单方法是遍历每一个从初始状态出发的箭头，在每种情况下，记录目标状态，然后继续遍历从这些新状态出发的箭头，再次记录下结果，以此类推。<br>这个过程会一直持续到其中一个新状态就是目标状态，这时，解决方案就找到了，控制系统只需沿着这条从开始状态到目标状态的路径应用产生式即可。

这种策略的结果实际上是构建了一课称为**搜索树**的树。

### 11.3.2  搜索树

**搜索树**（search tree）：由控制系统经过分析得到的部分状态图构成。

* 搜索树的根节点是开始状态；
* 每个节点的孩子是那些通过应用一个产生式从双擎节点可到达的状态；
* 节点之间的每条线段代表一个产生式的应用；
* 从根到叶子的每条路径代表状态图中相应状态之间的一条路径。

### 11.3.3  启发式

在试图解决更复杂的问题时，产生的搜索树会变得非常庞大。因此，开发一个完整的搜索树是不切合实际的。不用一层一层地构建树的**广度优先**（breadth-first）的方式，而是采用**深度优先**（depth-first）的方法构建搜索树。<br>也就是说，树是以建立纵向路径而不是横向层次的方式构建的。更确切地说，这种方法通常被称为**最佳优先**（best-first）构建，因为在搜索中被选中的纵向路径看起来是最优的。

为此，我们需要一种方式来确定几个状态中哪一个看上去是最有希望的。我们的方法是采用**启发式**（heuristic /hjʊəˈrɪs.tɪk/）。<br>**启发式**（heuristic）：是一个与每个状态相关的数值，用来衡量这个状态与最近目标之间的“距离”。启发式是规划代价的一个量度——给定两个状态之间的一个选择，具有较小启发值的状态是那个可以明显以最小代价到达目标的状态。因此，该状态代表了应该追求的方向。启发式应具备两个特征：

* 第一，如果到达相关状态，启发式必须对该解决方案中的剩余工作量有一个合理的估计。
  * 这意味着，它在多个选项中做出选择时能提供有意义的信息——启发式提供的估计越好，根据此信息所做的决定就越好。
* 第二，启发式应该易于计算。
  * 这意味着，启发式的使用应有益于搜索过程而非成为一种负担。
  * 如果计算启发式非常复杂，那还不如把时间花在进行广度优先搜索上。

### 11.4  其他研究领域

知识处理、学习以及处理非常复杂的问题，对人脑来说似乎很简单，但对机器来说就明显负担沉重了。

### 11.4.1  知识的表达和处理

现代计算机系统擅长存储和检索任何可以离散化、数字化以及存储为 1 和 0 的数据、海量信息。软件工具在人工智能、机器学习和统计技术的帮助下，可以发现以前在大型数据集中看不见的模式和连接。

**基本知识点**

* 大型数据集为提取信息和知识提供了机会和挑战。
* 大型数据集提供了确定趋势、建立数据连接和解决问题的机会。
* 计算工具有助于发现大型数据集内部信息的连接。

真实世界的知识和信息是庞杂多元的，且知识和信息的应用也需要根据特定的环境得到恰当的含义和使用范围，这些都为人工智能在知识的表达和应用处理上带来了巨大的困难和挑战。

### 11.4.2  学习

除了表示和处理知识，我们还希望**赋予智能体获取新知识的能力**。<br>我们可以通过编写和安装新程序、显式地向存储数据中添加新知识来“教”基于计算机的智能体“学习”，但是我们更希望智能体能够自己学习。我们希望智能体能够适应环境的变化并完成我们无法完成的任务。

**机器学习**（machine learning）：是人工智能的一个分支，是指通过开发算法和模型，使计算机能在从数据中进行学习和推断，而无需明确的程序指令。<br>简而言之，机器学习通过发现数据中的模式和规律，从而帮助计算机自动化处理任务和做出决策。<br>根据所需的人为干预级别，机器学习可以分为三级：

* 第一级是通过**模仿**（imitation）学习，即人通过直接执行一系列的计算机操作或者通过一系列动作移动机器人，直接演示任务的步骤，计算机只需要记录这些步骤。
* 第二级是**监督训练**（supervised training），即人对一连串的示例做出正确的反应，然后智能体对这些示例进行归纳，开发出一种适用于新案例的算法。
  * 这一连串的示例称为**训练集**（training set）。
* 第三极是**强化**（reinforcement）。智能体自己能够通过对一个任务的反复试验，积累成功的经验和失败的教训，得到一个一般规则，进而指导自己的进行判断。

因为还没有发现覆盖所有可能的、通用的关于学习行为的学习规则，所以学习仍然是一个有挑战性的研究领域。

### 11.4.3  遗传算法

A*<sup>（asterisk/ˈæs.tər.ɪsk/，加星号于）</sup>算法（最优解决方案）可以找到许多搜索问题的最优解，但有一些问题因为太过复杂而无法通过这些搜索技术求解。对于这些问题，有时可以通过一个涉及多代试探解的演进过程获得解。这一策略是**遗传算法**（genetic algorithm）的基础。从本质上来说，遗传算法通过将随机行为与繁殖理论的模拟和自然选择的进化过程相结合来发现解。

> 随机行为：是指系统或过程的输出或状态无法被完全预测或确定的情况。

> 繁殖：是产生新解的过程，其灵感来自于生物学中自然繁殖。

遗传算法首先生成试探解的一个随机池，其中的每个解都只是一种猜测。

* 每个试探解称为一个**染色体**（chromosome /ˈkrəʊ.mə.səʊm/）;
  * 构成染色体的每个独立个体称为**基因**（gene /dʒiːn/）。

因为每条初始染色体都是一个随机猜测，所以它不太可能表示手头问题的一个解。因此遗传算法会继续生成一个新的染色体池，其中每条染色体是前一个池中两条染色体（双亲）的后代（孩子）。<br>这些双亲是从池中随机选择的，而这个池子也会概率性地优先选择那些似乎更有可能生成解的染色体，于是这就模拟了适者生存的进化原则。每个后代都是双亲基因随机组合的产物。此外最终的后代偶尔可能以某种随机的方式变异。

我们希望，通过反复这一过程，能够演变出越来越好的试探解，支持找出 虽然不是最佳，但是非常好的试探解。<br>遗憾的是，遗传算法并不保证最终一定能够找到解，但研究表明，遗传算法可以有效解决种类繁多的复杂问题。

**进化规划**（evolutionary programming）：是指被用于程序开发任务的遗传算法方法。

* 目标是通过模拟进化过程开发程序，而不是直接编写程序。
* 研究人员已经使用函数式程序设计语言将进化规划技巧应用于程序开发过程。
  * 函数式程序设计语言：是一种基于数学中“函数”概念的编程范式，这种语言强调使用函数进行计算，而不是通过改变状态和数据。
* 运用这个方法，要从建立一个包含各式各样的函数程序集合开始。
  * 这个开始集合中的函数构成了“基因池”，之后的各代程序将通过“基因池”来构建。
  * 接下来，进化过程展开并延续许多代，期望每次通过上一代的最佳组合生成新的一代，从而让目标问题的解逐步演进。

## 11.5  人工神经网络

虽然人工智能领域已经取得了巨大的进步，但该领域里的许多问题仍然让使用传统算法的计算机负担沉重。因为指令序列的感知和推理能力还不能与人类的大脑匹敌，所以研究人员将目标转向了利用人工神经网络来解决该问题。

### 11.5.1  人工神经网络的基本特征

生物神经网络是一个单个的细胞，它由三部分组成：

* 树突，即输入触角；
* 轴突，即输出触角；
* 突触，即树突与轴突之间的小间隙。

一个细胞的轴突传递的信号反映了细胞是处于抑制状态还是处于兴奋状态。<br>树突通过突触从轴突中获得信号。<br>树突根据收到的信号的组合来决定细胞的抑制或兴奋状态。

一个人的大脑大约包含 10<sup>11</sup>神经元，每个神经元约有 10<sup>4</sup>个突触。神经元的树突非常多，以至于他们看起来像一个纤维网络。

人工神经网络是模仿活体神经系统中神经网络的计算机处理模型。人工神经网络中的一个神经元是模仿对生物神经元的一个软件单元。

从其他神经元（轴突）获得的值作为输入，经过判断这个有效的输入是否超过了一个给定的值——即阈值（也就是突触），该神经元（树突）产生输出（即`0`或`1`）。

人工神经网络通常按照拓扑结构分层排列。

* 输入神经元位于第一层；
* 输出神经元位于最后一层；
* 其他神经元可以包含在输入层与输出层之间。
* 一个层中的每个神经元与随后层中的每个神经元互相连接。

人工神经网络的一个重要特征是，它们不再是通过传统意义上的编程来实现，而是通过训练来实现。<br>也就是说，由人工神经网络通过监督训练学习获得合适的权值，监督训练涉及一个反复的过程，在这个过程中来自训练集的输入被应用到网络，然后用小的增量调整权以便网络的性能接近期望行为。