## *9.3  面向对象数据库

面向对象数据库（Objected-Oriented Database，OODB）：是一种将面向对象编程（Objected-Oriented Programming，OOP）与数据库技术结合的数据库类型。在这个模型中，数据以对象的形式存储，而不是传统关系数据库中的表格形式。这使得面向对象数据库能够直接存储面向对象编程语言中的对象，从而实现更自然的数据表示和操作。

* 面向对象数据库中两个对象之间的链接通常是由数据库管理系统来维护的，所以编写应用软件的程序员不需要关心如何实现这些链接的细节。
* 面向对象数据库管理系统为其托管的对象提供了永久的存储空间。

**面向对象数据库的优势：**

1. 面向对象方法使应用软件、数据库管理系统和数据库本身等整个软件系统用同样的范型来设计。
2. 面向对象范型对数据库设计而言还有一个好处是，不仅可以存储数据，还可以存储智能对象。即对象能够包含一些方法，用来描述它应如何响应有个它的内容和关系的消息。

## *9.4  维护数据库完整性

数据库管理系统的主要作用是通过防范信息不准确、数据丢失等问题来维护数据库的完整性。

### 9.4.1  提交/回滚协议

在计算设备出现故障的情况下，数据库管理系统的目标为确保设备的故障问题不会把数据库冻结在不一致的状态。为了做到这一点，经常通过在磁盘等非易失性存储系统中维护一个包含每个事物活动记录的**日志**（log）来实现。在允许事务更改数据库之前，先将要执行的更改情况记录到日志中。这样，这个日志就包含了每个事务操作的永久记录。

> **日志**（log）：是指记录系统活动的文件或数据结构。
>
> 日志用于追踪和存储系统中发生的事件或操作的详细信息。日志的用途广泛，可以用于调试、监控、审计、性能分析和故障恢复等。

把一个事务的所有步骤记录在日志中的那个点，称为**提交点**（commit point）正是在这个点上，数据库管理系统拥有在必要时靠自己重建事务所需要的信息。在这个点上，数据库管理系统在一定意义上为事务提供了保证，即它负责确保事务活动在数据库中得到反映。<br>在设备出现故障的情况下，数据库管理系统可以利用其日志中的信息，重建自上一次备份以来已经完成的（提交的）事务。

如果问题出现在事务达到其提交点之前，那么数据库管理系统可能会发现自己不能完成已经执行了一部分的事务。这种情况可以利用日志**回滚**（roll back）（也称为撤销）实际上已经被事务执行的活动。

回滚也是数据库管理系统正常操作的一部分，而不是仅仅局限于设备故障恢复的处理。例如，由于有试图访问特权信息的情况发生，事务可能会在完成全部步骤前被终止，或者可能是遇到**死锁**情况，即竞争资源的几个事务发觉自己一直在等待数据，而等待的数据正好被对方使用。在这些情况下，数据库管理系统能够利用日志回滚事务，从而避免未完成的事务使数据库出错。

> **死锁**（Deadlock）是指在计算机系统中，两个或多个进程或线程彼此等待对方释放资源，从而导致永久阻塞的情况。
>
> 简单来说，死锁是一个“僵局”，各方都在等待中，无法继续进行。

### 9.4.2  锁定

在计算机科学中，锁定（Locking）是控制对资源访问的机制，通常用于确保在多线程或并发环境下对共享资源（如数据、文件或内存）的安全访问。<br>锁定机制可以防止数据竞争和不一致性问题，并确保只有一个线程或进程可以在特定时间修改资源，使得并发操作更加安全和可靠。

**锁定协议**（locking protocol），该协议规定，数据库中当前正在被某个事务使用的项都要按协议加以标记。这些标记称为锁，已标记的项目称为被锁定。通常锁有两种，它们分别对应于访问事务所需数据的两种形式——

* **共享锁**（shared lock），对应共享访问。如果一个事务不打算改变数据项，那么它请求的就是共享访问，这就意味着允许其他事务查看该数据项。
* **排他型锁**（exclusive lock），对应互斥访问。如果一个事务打算改变数据项，那么它请求的就必须是互斥访问，这就意味着只有该事务才能访问该数据项。

在锁定协议中，每次事务请求访问数据项时，该事务还必须告诉数据库管理系统它需要访问的类型——是共享访问，还是互斥访问。

* 如果事务请求的是对一个数据项的共享访问，那么不论这个数据项是否用共享锁锁定，这个访问都将得到批准，并且将该数据项用共享锁进行标记。
  * 但是，如果请求的数据项已经用排他型锁标记了，那么别的访问都将会被拒绝。
* 如果事务请求的是对数据项的互斥访问，那么只有在这个数据项没有被锁定的情况下，请求才会被批准。
  * 通过这种方式，一个打算改变数据项的事务就可以通过获得的互斥访问来防止别的事务对数据项的干预。

* 如果几个事务都不打算改变数据项，那么它们就能共享访问这个数据项。

一旦事务完成对数据项的访问，就会通知数据库管理系统，从而解除相关的锁定。

当出现事务的访问请求被拒绝的情况时，可以用许多不同的算法进行处理。

1. 一种算法是指强制事务等待，直至所请求的项可用为止，但是这种方法容易造成死锁。
2. 为了避免这种死锁的发生，有些数据库管理系统会优先处理较旧的事务。
   1. 如果一个较旧的事务请求访问被较新的事务锁定的数据项，那么那个较新的事务就会被迫释放其所有的数据项，并依据日志回滚它的活动。于是，较旧的事务就获得了对所需数据的访问权限，而较新的事务只得重新开始。
   2. 如果这个较新的事务被反复抢占，那么随着过程的进展它也会变旧，并最终成为一个具有高优先级的较旧的事务。<br>这个旧的事务将新的事务挂起，而新的事务等待变成旧事务的协议称为**受伤等待协议**（wound-wait protocol）。