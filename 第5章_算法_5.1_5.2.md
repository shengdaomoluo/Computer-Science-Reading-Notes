# 第5章  算  法

计算机科学的核心主题是对算法对研究。

## 持久理解

**创造性开发可能是创建计算制品的必不可少的过程。**

**算法是可由计算机执行的过程（性）的精确指令（顺）序列（表），是使用程序设计语言实现的。**

**算法能解决许多计算问题，但不能解决所有计算问题。**

## 5.1  算法概念

在第 0 章中，我们把**算法**非正式地定义为一组步骤，这些步骤定义了完成任务的方法。

### 5.1.1  非正式的回顾

实际上，人脑中的每个活动，包括幻想、创造和决策，实际上都是算法执行的结果。

### 5.1.2  算法的正式定义

科学必须建立在定义明确的**术语**之上。

> **术语**（terminology /ˌtɜː.mɪˈnɒl.ə.dʒi/）：是指在特定领域或专业中使用的专门词汇或短语。它通常具有明确、特定的含义，用于表达该领域的概念或知识。术语的特点是专业性强，通常需要相关背景知识才能准确理解。
>
> 例如，医学中的“病理”、法律中的“合同”，都是术语。

**算法的正式定义：**

* **算法是定义一个可终止的过程的一组无歧义的、可执行的步骤的有序集合。**
  * 算法的一组步骤是有序的。这意味着，算法的各个步骤必须有一个就执行顺序而言非常明确的结构。
    * 算法的步骤是**有次序的**（sequenced/ˈsiːkwənst/，sequence的过去分词和过去式），它们是按照给定的顺序执行的。
    * 有些算法，称为**并行算法**，包含的步骤序列不止一个，其结构是一种多执行流结构，这些执行流在整个任务的不同部分被不同的处理器执行时不断分支和再接合。
  * 算法必须由可执行的步骤组成。这意味着，算法中的每个步骤是可行的。<br>计算机科学家使用*有效的*（effective）这个术语来表示可执行的概念。
  * 算法定义的是一个可终止的过程。这意味着，一个算法的执行必须能够最终结束。

### 5.1.3  算法的抽象本质

算法是抽象的，与算法的表示是有区别的。一个算法可以用多种方式来表示。但无论用哪种方式表示，底层的算法都是一样的，只是表示方式不同罢了。

在算法及其表示的主题上，我们应该明确另外两个相关概念——程序和进程——的区别：

* 程序（program）是算法的表示。
* 进程（process）是执行程序的活动。

算法将程序和进程联系在了一起：

程序是算法的表示，进程是执行算法的活动。

## 5.2  算法的表示

引入原语和伪代码的基本概念，并且建立一种为我们所用的算法表示系统。

### 5.2.1  原语

一个算法的表示需要使用某种形式的语言。对人类来说，这种形式的语言可能是一种传统的自然语言（英语、汉语、法语、日语……），也可能是一种图形语言，还可能是其他的形式。

当用来描述算法表示的语言并没有被准确定义或者并没有给出足够详细的信息的时候，就会产生交流问题。计算机科学解决这些问题的途径是建立一组定义明确的构建块，利用构建块来构造和执行算法的表示。这种构建块称作**原语**（primitive/ˈprɪm.ɪ.tɪv/，原始的；早期的）。

> **原语**（primitive）：在计算机科学领域中，是指一种基本的、不可再分的操作、数据类型或构造。它是系统或编程语言提供的最底层的功能，用于构建更复杂的操作和系统。原语通常是设计和实现更高级功能的基础。

赋予原语准确的定义消除了很多由歧义造成的问题，并且要求用这些原语来描述算法建立了统一的细致程度。

每个原语都有其自己的**语法**和**语义**。

* **语法**：是指原语的符号表示。
* **语义**：是指该原语的含义。

> 以“原语”*air*（空气）一词为例：
>
> > 其语法由“a”、“i”、“r”三个符号组成。
> >
> > 其语义是：一种充满整个世界的气体物质。

为了获得用于表示计算机执行的算法的原语集合，我们需要借助于为机器执行而设计的单个指令。如果算法在这个细致程度上表达，我们肯定会有一个适合机器执行的程序。然而，在这个细致程度上表达算法是非常单调的，所以我们一般使用一些“更高级的”原语，其中每个原语都是由机器语言提供的较低级的原语组成的一个抽象工具。因此，我们可以得到一种正式的**程序设计语言**（programming language）。在这种语言中，算法可以在比机器语言更高的概念层次上表达。

**程序设计语言**（programming language）：是指原语的集合以及说明如何组合这些原语来表达复杂的想法的规则集合。

### 5.2.2  伪代码

**伪代码**（pseudocode /ˈsjuːdəʊˌkəʊd/，pseudo-：伪、假）是一种用于在算法开发过程中非正式地表达想法的符号系统。<br>伪代码的优点是比程序设计语言更容易让人理解，且比自然语言更准确。

-----

**基本知识点**

* 用自然语言和伪代码描述算法的目的是为了让人可以理解算法。

------

一种获得伪代码的方法是，简单地放宽正式程序设计语言的规则，借用程序设计语言的语法-语义结构，与不太正式的结构混在一起。无论伪代码借用哪种程序设计语言的语法形式，其本质目的都是为算法服务：伪代码必须有一种一致且简洁的用来表示反复出现的语义结构的表示法。

* 一种反复出现的语义结构就是**保存计算的值**。<br>例如，我们计算了支票账户和储蓄账户上的余额总和，并打算命名和保存这个结果以便以后引用。这种情况下，我们用`name = expression`的形式来表达。其中，`name`是我们引用结果的名字；`expression`描述的是其结果要被保存的计算。<br>这个伪代码结构直接照搬了 Python 的**赋值语句**（assignment statement）。

* 另一种反复出现的语义结构是，**根据某个条件的真与假从两个可能的活动中选择一个**。<br>这样的例子有：<br>*如果国内生产总值增长了，就买进普通股，否则卖出普通股。*<br>*若国内生产总值增长了，则买进普通股，否则卖出它。*<br>*买进或卖出普通股取决于国内生产总值是增长还是降低。*

---

**基本知识点**

* 选择使用布尔条件来确定使用算法的两个部分中的哪一个。

----

这些语句中的每一句都可以被写成符合如下结构的形式：

```python
if (条件):
  活动
else:
  活动
```

这里，我们使用关键字`if`（如果）和`else`（否则）来表示这个主结构中的不同子结构，使用`:`（冒号）和缩进限定这些子结构的边界。条件和`else`后面总是紧跟着一个`:`（冒号）。相应的`活动`会被缩进。如果`活动`由多个步骤组成，则对这些步骤应该采用同样的缩进。

通过对我们的伪代码采用这种语法结构，我们便得到了可以表达这种常用语义结构的统一方法。因此尽管语句<br>*根据年份是不是闰年，分别将总数除以 366 或 365。*<br>可能会产生一种更富有创造性的文字风格，但是我们坚持选择下面这种简单的形式：

```python
if (年份是闰年):
  每日总计 = 总数 / 366
else:
  每日总计 = 总数 / 365
```

对于不涉及`else`活动的情况，我们也可以采用下面这种较短的语法：

```python
if (条件):
  活动
```

使用这种表示形式，语句<br>*如果销售下降的情况下，价格降低 5%。*<br>将可以简化为

```python
if (销售下降):
  价格降低 5%
```

还有一种常见的语义结构是，只要某个条件仍然为真就重复执行一条语句或一组语句，即**迭代**（iteration）。

-----

**基础知识点**

* **迭代**（iteration）：是算法一部分的重复，一直重复到满足条件或者达到指定次数为止。

> iteration（/ˌɪt.ərˈeɪ.ʃən/）：
>
> * 反复，即为了作出改善而反复做某事。
> * 迭代，叠代：数学里若干次使用一个数学法则得出的量。

---

非正式的例子有：<br>*只要有票可卖，就继续卖票。*<br>和<br>*当有票可卖时，就继续卖票。*

对于这两种情况，我们统一采用下列伪代码：

```python
while (条件):
  活动
```

简而言之，这个语句表示检查条件，如果条件为真，则执行活动，并返回再次检查条件。但是，如果说条件为假，则转移到`while`结构后的下一个指令。于是，前面的两个语句可以简化为：

```python
while (仍有票可卖):
  卖票
```

迭代通过**定序**和选择来完成我们构建算法所需的伪代码构建块的集合。

> 定序：是指按照定义的顺序执行代码中的操作或处理数据，确保每个步骤在正确的时间点发生，前后步骤之间的逻辑关系保持一致。<br>简单来说，定序就是程序“从上到下”按顺序执行指令。

然而，还有几种额外的快捷方式可以提升我们用于编写简洁算法的伪代码的可用性。

---

**基础知识点**

* 定序、选择和迭代是算法的构建块。
* 每个算法都可以直使用定序、选择和迭代来构造。

-----

在许多程序设计语言中，**缩进**通常可以提高程序的可读性。

> 缩进（Indentation/ˌɪn.denˈteɪ.ʃən/）：是指在代码中，通过在行的开头插入空格或制表符（Tab），将代码块向右移动一小段距离，从而组织代码结构、表示层次关系以及提高代码可读性的一种编程习惯或规则。
>
> 缩进的主要作用是让代码结构清晰，方便程序员和编译器（或解释器）理解代码的逻辑关系。

在 Python 以及由 Python 衍生的伪代码中，缩进也是必不可少的符号。例如，在下面的语句中：

```python
if (未下雨):
  if (温度 == 热):
    去游泳
  else:
    打高尔夫球
else:
  看电视
```

缩进告诉我们，除非条件为“未下雨”，否则根本不会问“温度是否等于热”这个问题。有关温度的问题是**嵌套**（nested）在`if`语句内部的，实际上是外部`if`语句为真时才会执行的活动。<br>同样，缩进告诉我们，“`else: 打高尔夫球`”属于内部`if`语句，不属于外部`if`语句。<br>因此，我们将在伪代码中使用缩进。

我们想用伪代码来描述那些可在其他应用中用作**抽象工具**的活动。对于这样的程序单元，计算机科学有许多术语，如子程序、子例程、过程、模块和函数，其中每个在含义上都有自己的变化。

> * 抽象工具（Abstract Tools）：是一种简化和管理复杂系统的方法，通过隐藏底层细节，将复杂问题转化为更易理解和处理的形式。<br>这种工具可以是概念性的，也可以是具体的编程语言特性或软件构件。
> * 函数（function）：是一个命名的代码块，用于执行特定任务。函数可以接收输入（称为“参数”），并可以返回输出（称为“返回值”）。

我们将按照 Python 的约定，在我们的伪代码中使用术语**函数**（function），并用 Python 的关键字`def`来声明用于标识伪代码单元的标题。更精确地说，我们将以下列形式的语句开始一个伪代码单元：

`def name ()`<br>其中，`name`是该单元特有的名字。在这个引导性语句的后面是一系列定义该单元动作的语句。

```python
def greeting ():
  Count = 3
  while (Count > 0):
    print the message "Hello"
    Count = Count - 1
```

以上是名为 Greetings 的函数的伪代码表示，该函数打印3 次“Hello”。

当一个函数完成的任务在伪代码的其他地方需要时，只需通过名字来请求它。<br>例如，如果有取名为`ProcessLoan`和`RejectApplication`的两个函数，那么我们可以通过编写下面的语句来在`if-else`结构内请求它们的服务：

```python
if (...):
  ProcessLoan ()
else:
  RejectApplication ()
```

当测试条件为真时，执行函数`ProcessLoan`函数；当测试条件为假时，执行函数`RejectApplication`。<br>注意：我们也可以把这个看作算法的组合，因为**每个函数本身就是一个算法**。也就是说，我们组合“`ProcessLoan ()`”算法和“`RejectApplication ()`”算法，再加上一些额外的算法构建块（“`if-else`”），就能创建一个审查贷款申请的新算法，然后将其命名为一个函数并复用为更大算法的一部分。

---

**基础知识点**

* 算法可以组合形成新算法。

---

如果函数在不同的情况下使用，那么应该将它们设计得尽可能通用。一个给名字列表排序的函数应该设计成能够对任何列表（而不是特定列表）排序，因此应该按照这样的方式来编写该函数：不在函数中指定要排序的列表。事实上，事实上，该列表应在这个函数的伪代码里以**类属名**来指称。

> 类属名：或称“类别名”、“类名”等，是一个标识符，表示一个类的名称。类属名的主要作用是为类赋予一个唯一的名字，使程序员可以通过这个名字引用类。

在伪代码里，我们将采用这样的约定：将这些称为**参数**（Parameter）类属名列在括号里，放在标识函数名的那一行上。

> 参数（parameter /pəˈræm.ɪ.tər/，限定因素；界限；范围；规范）：即函数的输入值。

例如，一个名为`Sort`（sort/sɔːt/：n./v.，种类，类型；把…分类，整理，挑选）的函数，被设计成用来对任何名字列表进行排序。该函数以下列语句开始：

```python
def Sort (List):
```

在后面的伪代码里，在需要引用要排序的列表的地方，就可以使用类属名`List`。同样，当需要`Sort`服务时，我们就会确定哪个列表要替代函数`Sort`中的参数`List`。因此，根据需要，我们可以写成<br>`Sort (组织的成员列表)`<br>和<br>`Sort (婚礼宾客列表)`

#### 命名程序中的项

*在自然语言中，项（item）通常使用多单词的名称，如“cost of producing widget”或“estimated arrival time”。经验表明，在算法的表示中使用这种多单词的名称会使算法的描述复杂化。**最好让每个项使用一个连续的文本块来标识。**多年来，有许多技巧用于将多个单词紧缩成一个词法单元，以便为程序中的项提供描述性名称。一种技巧是使用下划线（“_”）来连接单词，产生像 estimated_arrival_time 这样的名称。另一种技巧是用大写字母帮助读者理解紧缩的多单词名称。这种技术称为 Pascal大小写（Pascal casing /ˈkeɪ.sɪŋ/，外包装，箱，盒，罩，套），该样式除首字母小写外，其他都和 Pascal 大小写相同，如 estimatedArrivalTime。本书中倾向于使用 Pascal 大小写，但这种选择很大程度上是个人偏好。*

> Pascal（/pæsˈkæl/，帕斯卡） 大小写：是一种命名约定（命名规则），在程序设计语言中用于命名变量、函数、类等标识符。它的规则是：
>
> * 每个单词的首字母都大写。
> * 不包含下划线（"_"）或其他分隔符。
> * 其他字母均为小写，单词直接拼接在一起。
>
> 这种命名方式最早源自 Pascal 编程语言，因此得名“Pascal 大小写”。

