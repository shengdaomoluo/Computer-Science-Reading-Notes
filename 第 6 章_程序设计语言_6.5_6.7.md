## 6.5  面向对象程序设计

一个问题，可以通过使用面向对象的方法来解决，而这个方法的重点就是标识出涉及的对象，并将对象作为一个独立的单元来描述。

### 6.5.1  类和对象

**类**（class）是一组对象的模板，它描述了 一组对象的共同特征。

类是一个模板，对象是从这个模板构造出来的。用一个类能创建许多对象。我们经常称对象为构造它的类的**实例**（instance）。

### 6.5.2  构造器

**构造器**（constructor）是在合适的类中定义特殊的对象的方法，是在构建类的对象时自动执行的。构造器时通过在类的定义中使用与类同名的方法来标识的。

### 6.5.3  附加特性

为了简化对具有相似但不完全相同特征的对象的描述，面向对象语言允许一个类通过称为**继承**（inheritance /ɪnˈher·ɪ·t̬əns/）的技术包含其他类的属性。继承的使用导致了各种具有相似但不完全相同特征的对象的存在，也因此导致了重载现象。

**多态**（polymorphism/ˌpɒl.iˈmɔː.fɪ.zəm/）是指多个对象以不同的方式响应相同的方法调用。

**封装**（encapsulation /ɪnˌkæp.sjəˈleɪ.ʃən/）是指限制对一个对象内部属性的访问。被封装的特性被认为是私有的，可以从对象外部访问的特性被认为是公有的。说一个对象的某些特性被封装的，意思是说只有这个对象自己能够访问它们。

## <sup>*</sup>6.6  程序设计并发活动

在程序设计中，并发活动涉及到同时执行多个计算任务或者程序流程，以增强程序的效率和响应能力。并发活动允许多个任务在同一时间段进行处理，而不是按照顺序一个接一个地执行。

## <sup>*</sup>6.7  说明性程序设计

说明性程序设计（Declarative/dɪˈklær.ə.tɪv/ Programming）：是一种编程范式，它强调通过描述“是什么”而不是“怎么做”来定义程序的逻辑。换句话说，在说明性编程中，开发者专注于描述期望的结果，而不是具体的控制流程或步骤来实现这些结果。<br>这种方法与命令式编程（Imperative Programming）形成对比，后者要求开发者明确指定计算机应该如何执行任务。

形式逻辑提供了一个通用的解决问题的算法，围绕这个算法可以构建一个说明性程序设计系统。

> 形式逻辑：是一门研究逻辑推理结构和规则的学科，主要关注如何从给定命题推导出结论。<br>形式逻辑使用数学符号和规则来分析逻辑命题的真实性和有效性。
>
> 形式逻辑的基本概念
>
> > 命题：是一个能够被判断为真或假的陈述。
>
> > 逻辑运算：
> >
> > > 与（AND）：两个命题都为真时，结果为真。
> > >
> > > 或（OR）：至少一个命题为真时，结果为真。
> > >
> > > 非（NOT）：将命题的真假值反转。

