##  10.3  建模

3D 计算机图形项目开始的方式与戏剧舞台制作方式十分相似——必须设计布景并收集或者搭建所需的道具。<br>在计算机图形学的术语中，布景称为**场景**（sence），道具称为**物体**（object）。

**3D 图形场景是虚拟的**，因为组成它的物体是由数字编码模型“构建”而成，并不是实际的物理结构。

### 10.3.1  单个物体的建模

物体的计算机模型和相互作用的模拟必须从抽象开始，这些抽象比现实世界中的抽象更简单。

**基本知识点**：

* 模型和模拟是较复杂的物体或现象的简化表示。

一个通用规则：**模型越精确，制作出的图像的质量越高，但渲染所需要的时间也越长**。<br>因此，现在进行的大多数对于计算机图形学的研究都是在寻求用于构建高度细致但高效的物体模型的技术的开发。这些研究中有些涉及开发可以根据场景中的物体的最终作用来提供不同的细节层次的模型，这样可以在变化的场景中使用同一个物体模型。

描述一个物体所需的信息包括物体的形状和其他特性。

#### 1.  形状

3D 图形学中的物体的**形状**（shape）是指被描述成称为**平面片**（planar patch/ˈpleɪnər pætʃ/）的小平面的集合。其中每一个平面片都是一个多边形。

* **平面片**（planar patch）：是指一个平面上的小片段或区域，通常用于表示三维物体表面的局部部分。这些平面片可以组合在一起形成复杂的形状和模型。

这些多边形（平面片）构成一个**多边形网络**（polygonal /pɒˈlɪgənl/ mesh /meʃ/），它近似于被描述物体的形状。<br>多边形网络中平面片经常选择为三角形，因为每个三角形都能用它的三个顶点表示，这是三维空间中确定一个平面所需点的最少数目。在任何情况下，多边形网络都可以表示成其平面片顶点的集合。

越常规的形状可以用越复杂的分析方法来描述。

* 一种方法是使用**贝塞尔曲线**（Bezier curve）来描述，描述曲线的贝塞尔方法可以被扩展为描述三维曲面，称为**贝塞尔曲面**（Bezier surface）。
  * **贝塞尔曲线**（Bezier curve）允许在三维空间中只用几个称为对称点的点来定义曲线段；移动这些点，曲线被扭曲成不同的形状。
* 在无法用优雅的数学技术表示形状的情况下，一种流行的方法是**用蛮力的方式**获得多边形网格。这个过程是
  * 先构建物体的物理模型；
  * 然后通过用能记录自身三维空间位置的笔来触摸表面，记录下表面点的位置——这个过程称为**数字化**（digitizing/ˈdɪdʒɪtaɪzɪŋ/）。
  * 然后将得到的点的集合用作顶点来描述形状的多边形网格。
* 有些形状非常复杂，难以用几何建模或手工数字化获得真实模型。在这些情况下，多边形网格可以通过编写自动构建所需形状的程序来获得。这样的程序被称为**程序化模型**（procedural/prəˈsiːdʒərəl/ model）。<br>程序化模型提供了一种生成多个相似但又独特的复杂物体的有效方式。
  * **程序化模型**（procedural model）是指应用算法生成所需结构的程序单元。
  * 另一种构建程序化模型的方法是利用**粒子系统**（particle system/prəˈsiːdʒərəl/）将物体的基础结构模拟为一个大的粒子集合。
    * **粒子系统**（particle system）是指应用某些预定义的规则来移动系统中的粒子，产生所需的形状。

#### 2.  表面特征

仅由多边形网格构成的模型只捕获物体的形状。大多数渲染系统能在渲染过程中丰富这些模型，从而根据用户的需求模拟各种表面特征。

通过称为**纹理映射**（texture mapping/ˈtekstʃə(r) ˈmæpɪŋ/）的技术方法，将颜色模式与物体的表面特征相关联。

* **纹理映射**（texture mapping）：是指将一个预定义的图像与一个物体的表面相关联。

纹理映射应用到相对平坦的表面时效果最好。<br>如果必须大幅扭曲纹理图像去覆盖弯曲的表面，或者如果纹理图像完全裹着一个物体，并导致了接缝，在接缝处纹理模式可能不与它本身融合，那么效果看上去会不够逼真。

纹理映射已经被证明是一种模拟纹理的有效方法，被广泛用于实时敏感的场合。

#### 3.  寻求逼真的效果

构建可以生成逼真图像的物体模型是一个正在研究的主题，也是一个活跃的研究领域。它综合了建模和渲染过程的技术。

#### 分  形

**分形**（fractal/ˈfræk.təl/）：是指通过将较低维度的物体的副本“打包”而形成的几何物体。

分形通常是用递归过程来形成的，而递归中的每次激活就是重复“打包”另外用来建立分形模式（更小）的副本。分形的结果是其每个部分都是自相似的，当放大时，都表现为其自身的一个副本。

### 10.3.2  整个场景的建模

**场景图**（scene graph）：是指通过对物体进行充分描述和数字编码，物体被赋予了场景内的位置、大小和朝向等信息的集合被链接起来而形成的数据结构。

场景图类似于传统摄影中的工作室布置，它包括摄像机、灯光、道具和背景布景，它们全都在适当的位置上。不同的是，<br>传统摄影布置包含的是真实的物体，而场景图包含的是物体的数字编码表示。

简而言之，**场景图描述的是一个虚拟世界**。

**基本知识点**：

* 模型可以根据要呈现的物体或现象使用不同的抽象或不同的抽象层次。

场景图的构建在 3D 图形处理中有举足轻重的位置。因为它包含制作最终图像所需的所有信息。它的完成标志着艺术建模过程的结束和渲染图像的计算密集型过程的开始。