## 5.5  递归结构

**递归**：指的是一个函数直接或间接地调用自身来解决问题。<br>它的核心思想是：将一个问题分解成更小的子问题，通过自我调用来解决子问题，直到达到最简单的情况时，停止递归并返回结果。

**递归结构**：指的是一个问题（或过程）通过自身调用来解决的一种结构化方式。<br>递归结构通常被用于解决那些可以分解为更小规模的类似问题的场景。

### 5.5.1  二分搜索算法

**先验知识**（A priori /priˈɔːri/ knwoledge）：在科学和信息论中，通常是指在观察和实验之前已经掌握的信息或假设。<br>它是通过理性和推理获得的知识，无需依赖经验或观察。

**二分搜索算法**（Binary /ˈbaɪ.nər.i/ Search Algorithm）：用于在有序数组或列表中查找特定元素的位置。

该策略的基本思想是：将所讨论的列表连续分成更小的段（列表的一半，的一半，的一半，……），直到最终找到目标或者搜索范围缩小到空段为止。

----

以在有序列表中搜索条目 John 为例：

```python
原始列表
Alice, Bob, Carol, David, Elaine, Fred, George, Harry, Irene, John, Kelly, Larry, Mary, Nancy, Oliver
```

为了搜索该原始列表中，是否存在 John 的条目，我可以用以下的伪代码完成任务。

```python
List = ["Alice", "Bob", "Carol", "David", "Elaine", "Fred", "George", "Harry", "Irene", "John", "Kelly", "Larry", "Mary", "Nancy", "Oliver"]
def Search(List, TargetValue):
    if (List为空):
        报告搜索失败
    else:
        TestEntry = List的中间条目
        if (TargetValue == TestEntry):
            报告搜索成功
        if (TargetValue < TestEntry):
            Sublist = TestEntry前面的List部分
            Search(Sublist, TargetValue)
        if (TargetValue > TestEntry):
            Sublist = TestEntry后面的List部分
            Search(Sublist, TargetValue)
```

注意，这个函数包含了一个对自身的引用。如果顺着这个函数到达指令`Search(...)`我们将把用在原始列表上的那个函数用到较小的列表上。

* 如果那次搜索成功了，我们将返回并声明我们的原始搜索成功；
* 如果这次后续搜索失败，我们将声明我们的原始搜索失败。

-----

我们可以看到，上述伪代码算法所采用的过程是，重复地将所考虑的列表分成两个较小的块，以这种方式将后续搜索严格地限制在其中一个块中。这种一分为二的方法就是该算法称为**二分搜索**（binary search）的原因。

### 5.5.2  递归控制

二分搜索算法与顺序搜索算法相似，因为**它们都要执行一个重复的过程**。但是，这种重复的实现是截然不同的。

* 顺序搜索是以一种循环的方式重复执行一个过程。
* 二分搜索是把每一阶段的重复当作前一阶段的子任务。

执行递归函数产生的<big>*错觉*</big> 是这个函数存在多个副本，每个副本称作这个函数的一次激活（activation）。这些激活通过一种伸缩方式动态创建，并随着算法的前进而最终消失。在任何给定时间存在的激活中，只有一次激活是活跃地进行的，其他激活都处于不确定的状态，每一次激活都要等待另一次激活终止后才能继续。

事实上，正确的递归控制也和循环控制的要求相同，涉及同样的 3 个要素：

* 初始化
* 修改
* 终止条件

在上面的伪代码中：

初始化：是从简单地给出原始列表和目标值**隐式**开始的。

修改：从该初始配置开始，函数就会将分配给它的任务修改为在更小的列表中进行搜索。

终止条件：找到目标值或者任务缩小到搜索空列表。

**隐式**（Implicit /ɪmˈplɪs.ɪt/ adj. 不明言的，含蓄的）：指的是某些信息或操作没有明确地表达出来，而是通过上下文、规则、推导或默认机制间接体现或实现。与显式（Explicit/ɪkˈsplɪs.ɪt/ adj. 清楚明白的，不含糊的，明确的）相对应，显式是直接明确表达的信息或操作，而隐式这是暗含、间接或默认的。