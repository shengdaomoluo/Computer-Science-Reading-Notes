## 10.6  动画

**计算机成像**（computer generated imagery，CGI）是利用计算机技术来制作和显示呈现运动的画像。

相对便宜但功能强大的计算机动画软件工具使创作者能够构建比使用传统手绘动画技术更精确、更复杂的视觉艺术。

### 10.6.1  动画基础

一些基本的动画概念：

#### 1.  帧

动画是通过快速连续地显示一系列称为**帧**（frame）的图像来实现的。

**帧**（frame）：是指动画序列中的单个静止图像。<br>帧可以用传统摄影制作，也可以用计算机图形学人工生成；而且这两种技术还可以结合起来应用。

这些帧（单个静止画像）以固定时间间隔捕获变化的场景的外观，因此，帧的顺序展示就产生了一直观看连续场景的错觉。

* 电影业的标准显示速率是每秒 24 帧。
* 广播视频的标准是每秒 60 帧。

**变形**（morphing /ˈmɔːfɪŋ/）：就是一个物体看起来要变成另一个物体的过程。

仔细观察**变形**可以深入了解动画过程。构建变形效果主要由几个步骤组成：

* 首先，要确定一对包含变形序列的关键帧：
  * 一个是变形出现之前的最后一个图像；
  * 一个是变形出现之后的第一个图像。
* 然后，将变形前的帧的特征，如称为**控制点**（control point）的点和线，与变形后的帧的相似特征相关联；<br>通过应用数学技术同时使用控制点作为向导将一个图像逐渐变形为另一个图像来构造变形。
  * **控制点**（control point）是用于定义曲线或曲面形状的特殊点。<br>通过控制这些控制点，可以方便地修改曲线或曲面的形状，达到所需的效果。
* 通过记录变形过程中所产生的图像得到一个短的人工生成的图像序列，把这个短的人工生成的图像序列填充到当初定义的那两个关键帧之间，就创建了变形幻象。

#### 2.  故事板

**故事板**（storyboard）：是一个二维图像序列，以演示关键点场景草图的形式讲述一个完整的故事。

一个典型的动画项目从创建故事板开始。创作者（称为主动画师）会把故事板细化为称为**关键帧**（key frame）的详细帧，建立动画固定时间间隔的角色和场景的外观；<br>助理动画师将绘制另外的帧来填充两个关键帧之间的间隔，以使动画看起来连续且平滑。这个填充间隔的过程称为**中间渐变**（in-betweening）。

#### 3.  3D动画

大多数视频游戏动画和功能完备的动画制作现在是使用3D 图形创建的。但它们仍旧从创建由二维图像组成的故事板开始。

在 2D 图形项目中，故事板发展成了最终产品。<br>在 3D 图形项目中，故事板只被用作构建三维虚拟世界的蓝图。但其中的物体按照脚本或视频游戏的进展进行移动时，整个虚拟世界将反复地被“拍摄”。

**物体在计算机生成的场景内移动**，其真实的含义并不是”物体“在场景内移动。“物体”实际上是存储在场景图中的数据集合。这个集合中的数据是一些指示物体位置和朝向的值。因此，只要改变这些值就能完成物体的“移动”。一旦完成这些改变，新值就会被用在渲染过程中。结果物体看起来像是在最终的二维图像中已经移动。

### 10.6.2  运动学和动力学

为了实现 3D 图形场景中物体运动过程的自动化，计算机科学研究者开始直接面向找寻能识别和模拟自然现象中的运动的方法。机械学中的两个领域被证明在这一点上特别有用：

**动力学**（dynamics）研究的是通过应用物理定律确定力作用于物体的效果来描述物体的运动。

**运动学**（kinematics）研究的是根据物体各部分间的相对运动方式来描述物体的运动。

### 10.6.3  动画制作过程

如今，动画制作者都使用软件操纵场景图中的三维虚拟物体。用这种方式，动画师能够创建一系列的虚拟场景，这些场景随后被“拍摄”成动画。在某些情况下，这种技术只用于为关键帧制作场景，然后在软件应用动力学和运动学把场景图中的物体从一个关键帧场景位置移到下一个关键帧场景位置时，再使用软件通过自动渲染场景来产生中间渐变帧。

