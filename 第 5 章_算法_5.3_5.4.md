## 5.3  算法的发现

程序开发由两个活动组成：发现潜在的算法和以程序方式表示算法。<br>然而，“**发现潜在的算法**”在软件开发过程中往往是更具有挑战性的步骤。毕竟，发现一个解决问题的算法需要找到一个解决问题的方法。因此，要理解算法是如何发现的就要理解问题求解的过程。

### 5.3.1  问题求解的艺术

数学家波利亚（G. Polya）在 1945 年提出的以下非严格定义的问题求解阶段，其蕴涵的基本原理直至今日仍然是很多人教授问题求解技巧的基础。

* *第 1 阶段*  理解问题。
* *第 2 阶段*  设计一个解决问题的计划。
* *第 3 阶段*  完成计划。
* *第 4 阶段*  评估解决方案的准确度以及它作为解决其他问题的工具的潜力。

要强调的是，这些阶段并不是在尝试解决问题时必须按顺序遵循的步骤，而是在求解过程中有时需要完成的阶段。这里的关键词是<big>*遵循*</big>。仅遵循这些步骤是不能解决问题的，要解决问题必须有主动==开创精神==。<br>如果在解决问题的时候总是抱有“现在我完成了第 1 阶段，该开始第 2 阶段了”之类的想法，那么就不太可能成功。然而，如果仔细考虑问题并最终解决了问题，那么当回过头来看自己做对了什么的时候，很可能会发现，确实经历了波利亚所描述的各个阶段。

----

#### 关于波利亚和问题解决策略

**乔治·波利亚**（George Póly，1887 年 12 月 13 日--1985 年 9 月 7 日，想念 97 岁）是 20 世纪著名的匈牙利裔数学家。以其对数学教育、问题解决策略和概率论的贡献而闻名。 他的研究不仅在数学领域具有深远影响，还对教育学、科学问题解决方法论产生了重要启发。

在《如何解题（How to Solve It）》中，他提出了**问题解决策略的“波利亚四步法”**，即上面的“问题求解阶段”。

----

另一个有意思的现象是：当一个问题求解者正在为问题焦头烂额又一筹莫展时，可能会在完成其他任务的过程中，突然发现原来那个问题的解决方案。这种现象反映了这样一个过程：<br>大脑的潜意识部分好像一直在思考问题，如果成功，便会把解决方案推给显意识。<br>今天，我们把有意识地解决问题与突然的灵感之间的这个时期称作孵化期（incubation period/ˌɪŋ.kjəˈbeɪ.ʃən ˌpɪə.ri.əd/，潜伏期），对孵化期的理解仍旧是当前研究的目标。

### 5.3.2  迈出第一步

对问题进行思考，并尝试着找到解决问题的方法，是为解决问题“迈出的第一步”。当然，知道怎么迈出第一步并不等于迈出了第一步。

得到立足点，并认识到如何把对于问题的初始介入扩展为完整的问题解决方案，需要问题投入者的创造性投入。然而，波利亚和其他人已经提出了许多如何“迈出第一步”的方法：

* **反向解决问题**。
  * 例如：如果问题是从一个给定输入产生一个特定输出的方法，我们可以从输出开始，尝试反向推导出给定输入。

* **寻找一个更容易解决或者以前已经得到解决的相关问题，然后尝试把这个问题的解决方案用到当前待解决的问题中去**。

  * 通常，程序开发的过程并不是解决一个问题的一个特定实例，而是要寻找一种适用于求解一个问题的所有实例的通用算法。

* **逐步精化**（stepwise/ˈstep.waɪz/ refinement/rɪˈfaɪn.mənt/）

  * 将原问题分解为多个子问题，通过逐步解决各个子问题来得出原问题的整体解决方案，其中每一步都要比解决完整的原问题更容易。

  * 根据逐步精化的建议，还可以把这些步骤分解成更小的步骤，这些更小的步骤还可以分解成更小的步骤，这些更小的步骤还可以继续分解，就这样一直分解，直到整个问题被简化为一组容易解决的子问题为止。

    ```mermaid
    graph TD
    当前问题 --> 当前问题的子问题A 
    当前问题 --> 当前问题的子问题B
    当前问题 --> 当前问题的子问题C
    当前问题的子问题A --> 子问题A的子问题a
    当前问题的子问题A --> 子问题A的子问题aa
    当前问题的子问题A --> 子问题A的子问题aaa
    当前问题的子问题A --> 子问题A的子问题aaaa
    当前问题的子问题A --> 子问题A的子问题a...a
    当前问题的子问题B --> 子问题A的子问题b
    当前问题的子问题B --> 子问题A的子问题bb
    当前问题的子问题B --> 子问题A的子问题bbb
    当前问题的子问题B --> 子问题A的子问题bbbb
    当前问题的子问题B --> 子问题A的子问题b...b
    当前问题的子问题C --> 子问题A的子问题c
    当前问题的子问题C --> 子问题A的子问题cc
    当前问题的子问题C --> 子问题A的子问题ccc
    当前问题的子问题C --> 子问题A的子问题cccc
    当前问题的子问题C --> 子问题A的子问题c...c
    ```

    就此而论，逐步精化是一种**自顶向下方法**（top-down methodology /ˌmeθ.əˈdɒl.ə.dʒi/方法论；一套方法；教学法）,这种方法从一般发展到特殊。相反，**自底向上方法**（bottom-up methodology）是从特殊发展到一般。尽管理论上相反，但是实际上这两种方法经常在应用中互为补充。对逐步精化的自顶向下方法提出的问题的分解，通常要以那些可能以自底向上方式工作的问题求解者的直觉为指导。

    ==**切记，切记，切记：**==<br>==**将事先形成的观念和预选的工具带入问题求解任务有时会掩盖问题的简单性。**==

    > 算法并不是现成的知识和规律，而是需要发现的未知，需要试错的技术，需要创造的艺术。

    算法的发现仍旧是一种富有挑战的艺术，必须经过一段时间才能完成，而不是像一门由明确定义的方法组成的学科那样学到。因此，**要成为一名问题解决者，只知固守一定的方法，就会压制那些原本应该培养的创造性技能**。

    ## 5.4  迭代结构

    **迭代结构**（iterative structure）：在这种结构中，一组指令以循环方式重复执行。

    ```mermaid
    graph LR
    重复结构 --> 迭代结构
    迭代结构 --> 顺序搜索算法
    迭代结构 --> 插入排序算法
    重复结构 --> 递归结构 
    递归结构 --> 二分搜索算法
    ```

    

### 5.4.1  顺序搜索算法

```python
def Search(List, TargetValue):
    if (List为空):
        声明搜索失败
    else:
        选择List中的第一个条目作为TestEntry
        while (TargetValue大于TestEntry and 还有条目):
            选择List中的下一个条目作为TestEntry
        if (TargetValue = TestEntry):
            声明搜索成功
        else:
            声明搜索失败
    
```

**顺序搜索**（sequential/sɪˈkwen.ʃəl/ search）算法是按照条目（目标值）在列表中出现的顺序进行查找的。由于这个原因，这个算法有时也被称为**线性搜索**（linear/ˈlɪn.i.ər/ search）。因为这种算法简单，所以经常用于短列表或者出于其他考虑需要使用它的场合。然而，对于长列表，顺序搜索就没有其他技术高效了。

### 5.4.2  循环控制

一条指令或一系列指令的重复使用是一个重要的算法概念。实现这种重复的一个方法是称为**循环**（loop /luːp/）的迭代结构。这种结构中，一组称为循环**体**（body）的指令在某个控制过程的指引下重复执行。

实际上，`while`语句

```python
while (条件):
  循环体
```

就是循环结构特征的一个例证，因为它的执行时跟踪循环模式<br>`检查条件`<br>`执行循环体`<br>`检查条件`<br>`执行循环体`<br>`...`<br>`检查条件`<br>直到条件为假。

作为一般规则，循环结构的使用使程序得到了比仅仅将循环体重写多次更高的灵活度。

循环控制由初始化、测试和修改 3 个活动组成，其中每个活动都决定了循环控制是否能够成功。

* **初始化**：建立一个初始状态，这一状态会朝着终止条件修改
* **测  试**：比较当前状态和终止条件，如果相等则终止重复
* **修  改**：改变状态使之移向终止条件

测试活动有义务通过观察指示终止应该发生的条件来终止循环过程。这个条件就是**终止条件**（termination/ˌtɜː.mɪˈneɪ.ʃən/ condition）。为了这个测试活动，我们在我们伪代码的每个`while`语句中都提供了一个条件。然而，在`while`语句中，规定的条件是循环体应该被执行的条件——终止条件就是`while`结构中出现的条件的对立条件。

循环控制中的另外两个活动确保了终止条件最终可以出现。初始化步骤建立了一个初始条件，修改步骤将这个条件移向终止条件。应当强调的是，**初始化步骤和修改步骤必须导致合适的终止条件**。这个特性对正确的循环控制至关重要，因此在设计循环结构的时候必须多次确认它（终止条件）是否存在。如果没有进行这样的评估，即使是在最简单的情况下，也会导致错误。一个典型的例子就是

```python
Number = 1
while (number != 6):
  Number = Number + 2
  
```

注意 Python 运算符“`!=`”的使用，它读作“不等于”。这里，终止条件是`Number == 6`。但是`Number`的值被初始化为`1`，然后在修改步骤递增`2`。因此，在循环过程中，`Number`的值将是`1、3、5、7、9、11、……`，但是永远不会是`6`，因此循环将无法终止。

循环控制不见的执行顺序可能会产生微妙的结果。事实上，有两种常用的循环结构，它们仅仅在循环控制部件的执行顺序上有所区别。第一种的例子如下面的伪代码语句所示：

```python
while(条件):
  活动
```

`while`结构的终止测试出现在循环体执行之前。

---

#### 相关知识--流程图

**流程图**（flowchart）是一种图形化的工具，用于表示某个过程、系统或算法的步骤和决策点。它通过使用特定的符号（如：矩形、菱形、箭头等）来直观地描述步骤之间的顺序和逻辑关系。流程图常用于分析、设计、优化流程或提供清晰的过程说明。

**流程图的常用符号**：

* 矩形：表示一个步骤或活动（如任务或操作）。
* 菱形：表示决策点，需要选择“是”或“否”或其他分支。
* 椭圆：表示起点或终点。
* 箭头：表示步骤之间的流程方向或顺序。

**流程图的应用场景**：

* 软件开发中的算法设计。
* 业务流程优化。
* 教学中的概念简化。
* 项目管理中的流程分解。

**使用 Mermaid 图表绘制工具绘制流程图**：

**Mermaid**（/ˈmɜː.meɪd/ 美人鱼）是一种基于 Markdown 的图标绘制工具，可以用简单的语法快速创建流程图、时序图、甘特图等。它是一个轻量级的工具，十分适合嵌入到 Markdown 文档中。以下是详细介绍如何使用 Mermaid 绘制流程图的方法。

**基础语法**：

Mermaid的流程图使用`graph`关键字作为开始标志。以下是基本语法结构：

```python
graph TD
    A[起点] --> B[步骤1]
    B --> C{决策点?}
    C --> |是| D[步骤2]
    C --> |否| E[步骤3]
```

* `graph`：定义流程图的方向。
  * `TD`（Top to Down）表示从上到下。
  * `LR`（Left to Right）表示从左到右。
  * `BT`（Bottom to Top）表示从下到上。
  * `RL`（Right to Left）表示从右到左。
* 节点定义：
  * `A[文本]`：方形节点。
  * `B((文本))`：圆形节点。
  * `C{文本}`：菱形节点，用于决策。
  * `D>文本]`：非对称节点（用于子流程或特别标记）。
* 箭头定义：
  * `-->`：普通箭头。
  * `---`：无箭头的连接线。
  * `-->|文本|`：箭头上带有文本。
  * `-.->`：虚线箭头。

**Mermaid 流程图示例**：

* 示例 1：简单的流程图

  ```python
  graph TD
      Start[开始] --> Step1[步骤1]
      Step1 --> Decision{是否通过?}
      Decision -->|是| Step2[步骤2]
      Decision -->|否| End[结束]
  ```

  ```Mermaid
  graph TD
      Start[开始] --> Step1[步骤1]
      Step1 --> Decision{是否通过?}
      Decision -->|是| Step2[步骤2]
      Decision -->|否| End[结束]
  ```

  ---

  虽然许多算法要求在控制循环时慎重考虑初始化、测试和修改活动，但其他一些算法则遵从一些非常常见的模式。特别是在处理数据列表时，最常见的模式是从列表中的第一个元素开始，考虑列表中的每个元素，直到到达列表末尾。暂时回到顺序搜索示例上，我们看到这个模式类似于：

  ```python
  选择 List 中的第一个条目
  while (还有条目):
    ...
    选择 List 中的下一个条目
  ```

  因为这个结构在算法中出现的如此频繁，以致我们将使用下面的语法形式描述一个迭代遍历列表中每个元素的循环：

  ```python
  for Item in List:
    ...
  ```

  注意，这个伪代码原语实际上是比`while`结构更高一级的抽象，因为我们可以用单独的初始化、修改和测试结构来实现同样的效果，但是这个版本去除了不必要的细节，更简洁地传达了这个循环的意思。

  每次通过这个`for`循环结构时，值`Item`都会变成`List`中的下一个元素。这个循环的终止条件在到达`List`末尾的时候是隐含的。作为一个例子，要想得到列表中数字的综合，可以使用：

  ```python
  # List = {1, 2, 3, 4, 21, 55}
  Sum = 0
  for Number in List:
    Sum = Sum + Number
  # print("List 中数字的总和是：", Sum)
  ```

  在 Python 以外的语言中，这种类型的循环通常称为`for-each`循环，是前测试循环的一种特例。<br>**`for`结构最适合的情况是，算法对列表中的每个元素都执行同样的步骤，不必单独跟踪循环计数变量。**

### 5.4.3  插入排序算法

作为另一个使用迭代结构的例子，下面考虑将一个名字列表按照字母顺序进行排序的问题（即将名字按照 从A到Z的顺序排列）。但是在继续介绍之前，我们应该先了解一下排序的限制。简单来讲，我们的目标是在“列表自身的内部”对列表进行排序。换句话说，我们想通过把条目移来移去来对列表进行排序，而不是把列表移到其他位置。我们的情况类似于下面这种列表的排序问题：列表的每个条目记录在一张索引卡片上，所有卡片分散在桌面上，把桌面挤得满满的；我们已经清理出足够的空间来放这些卡片，但是不允许挪开其他东西来挤出更多的空间。<br>这种限制在计算机应用里是很典型的，其原因当然不是机器里的工作空间一定像桌面那样拥挤，而仅仅是因为我们想更有效地利用存储空间。

下面我们来迈出第一步，考虑如何在桌面上对名字进行排序。考虑一个名字列表：

Fred

Alex

Diana

Byron

Carol

要对上面的名字列表进行排序，其实是通过重复地移动条目并将其插入适当的位置来实现的。因为这种插入过程是重复的，所以这种底层算法称为**插入排序**（insertion sort）。伪代码如下：

```python
def Sort(List):
  N = 2
  while (N 的值没有超过 List 的长度):
    选择 List 中的第 N 个条目作为主元
    把主元移到一个临时位置使该 List 留出一个空缺
    while (在空缺位置的上面有名字 and 这个名字比主元大):
      将空缺上面的名字下移到空缺上使该名字上面留出一个空缺
    将主元移到 List 中的空缺上
    N = N + 1
```

注意，上面伪代码所示的结构是一个循环内部还有循环的结构，外层循环用第一个`while`语句表示，而内层循环用第二个`while`语句表述。<br>每次执行外层循环体都导致内层循环体被初始化而且重复执行直到达到终止条件，因此外层循环体的一次执行将导致内层循环体多次执行。

对于外层循环控制：

外层循环控制的初始化部分由建立 `N`的初始值的语句组成：<br>`N = 2`

修改部分通过下列语句每次给循环体末尾的`N`加`1`完成：<br>`N = N + 1`

当`N`的值超过列表的长度时，终止条件出现。

对于内层循环控制：

* 内层循环控制通过移走列表中的主元从而形成一个空缺位置来初始化。
* 循环的修改步骤通过将条目向下移入空缺位置上移来实现。
* 终止条件有两个，要么空缺位置上方紧邻的名字大于主元，要么空缺位置到达列表顶部。

> **主元**（Pivot/ˈpɪv.ət/，枢纽，关键人物；枢轴，支点）：通常是指一个选定的关键元素，用来在某些操作中作为基准或参考点。