## 12.3  通用程序设计语言

**通用程序设计语言**（universal programming language）：是指用来表达计算所有图灵可计算函数的指令性程序设计语言。<br>通用程序设计语言包含在高级程序设计语言之中，是高级程序设计语言的基础和内核；高级程序设计语言给通用程序设计语言带来了易理解、便于使用的“个性”。

### 12.3.1  Bare Bones 语言

Bare Bones：裸露的骨头。

**Bare Bones 语言**：是从通用程序设计语言中分离出来的需求的最小集合，所以称之为 Bare Bones。也就是说，该语言是通用程序设计语言的核心，不能再“分割”下去了。

### 12.3.2  用Bare Bones语言编程

事实证明，在实用的场合使用 Bare Bones 语言不太合适。但，该语言的确是可以用它来编写程序的，这也是无可争辩的事实。

### 12.3.3  Bare Bones的通用性

每种高级语言实质上都包含有 Bare Bones 语言的特性，并将其作为核心。实际上，正是这个核心保证了每种这样的语言的通用性，而各种语言中的所有其他特性都是为了方便使用才引用的。

之所以学习 Bare Bones语言，目的是加深对高级程序设计语言的理解，知道它们能够解决计算问题的基本原理。

## 12.4  一个不可计算的函数

一个函数，不是图灵可计算的。因此，根据丘奇—图灵论题，这个函数在一般意义上也是不可计算的。这样一来，这个函数的计算就超出了计算机的计算能力。

```mermaid
graph LR
A(((一个<br>函数))) -->|不是图灵<br>可计算的| B(((在一般意义上<br>也是不可计算的))) --> C(((这个函数的计算<br>就超出了<br>计算机的计算能力)))
```

### 12.4.1  停机问题

**停机问题**（halting problem）是指要提前预测到当一个程序在某个条件下开始后，是否能够终止（或者说停止）的问题。<br>**停机问题只是不可计算函数的一个实例**。

### 12.4.2  停机问题的不可解性

要证明“求解停机问题”超出了机器的能力。方法是：证明解决这个问题需要一个用来“计算”不可计算函数的算法。也就是说，任务就是要证明：**停机函数是不可计算的**。更进一步，就是**证明：“停机函数是可计算的”不为真**。

因为，停机问题的解决依赖于停机函数的计算；可是，停机函数是不可计算的。因此，停机问题是不可解决的。既然停机问题是不可解决的，就超出任何计算机算法系统的能力范围。<br>可以得到结论：这个问题是**不可解决问题**(unsolvable problem)。

<big>**事实是：<br>一些问题无法用算法解决。**</big>

