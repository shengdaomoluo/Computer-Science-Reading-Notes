## 6.3  过程单元

本节主要讨论函数的概念，函数是一个命令式语言获得程序的模块化表示的主要技术。

### 6.3.1  函数

**函数**（function /ˈfʌŋk.ʃən/，工作，运行；功能、用途，职责；典礼、仪式，社交聚会）就是完成一个任务的一组指令，它能够用作其他程序单元的抽象工具。

* *调用*（call 或者 invoke /ɪnˈvəʊk/，唤起、引起、使记起；援引、借助）函数，是指将控制权转移给函数的过程。
* *调用*单元，是指将请求函数执行的程序单元。

当请求了函数提供的服务时，程序的控制权就转移给了函数，在函数执行完之后，程序控制权又返回到原来的程序单元。

----

**基本知识点**

* 开发抽象的过程涉及去除细节和归纳功能。
* 为了归纳概念，抽象从具体示例中提取共同特征。

---

### 6..3.2  参数

**参数**（parameter/pəˈræm.ɪ.tər/，限定因素；界限；范围；规范），是指函数内部的通用项：

* **形式参数**或**形参**（formal parameter）在函数内部使用的项。
* **实际参数**或**实参**（actual parameter）当函数被调用时，赋给形参的精确含义。
  在某种意义上，形参就像是函数体上的槽口，当函数被请求的时候，实参就被塞入这个槽口中。

----

**基本知识点**

* 参数化可以归纳一个特定的解决方案。

------

```C
void ProjectPopulation (float GrowthRate)
{    int Year;        // 声明一个名为 Year 的局部变量
     Population[0] = 100.0;
     for (Year =0; Year <= 10; Year++)
     Population[Year+1] = Population[Year] + (Population[Year] * GrowthRate);   
}    
```

请求函数执行的语句由函数名和括号中的实参列表组成。因此，像

```C
使用增长率 0.03 调用函数 ProjectPopulation()
```

这样的伪代码语句可以用 C 语言语句

```C
ProjectPopulation(0.03);
```

表示，它是用增长率 0.03 来调用函数`ProjectPopulation`。这个语法与 Python 中的语法时相同的，只是尾部没有分号。

当函数不只包含一个参数的时候，实参要与函数头中所列的形参一一对应，即第一个实参对应第一个形参，依次类推。然后，实参的值就可以有效地传递给它们对应的那个形参，从而函数得以执行。

为了强调这一点，假设函数`PrintCheck`采用这样的函数头来定义：

```Python
def PrintCheck(Payee, Amount):
```

其中，`Payee`和`Amount`是函数的形参，分别指向收支票的人和支票的数额。那么，用语句

```Python
PrintCheck('John Doe', 150)
```

调用该函数，将会使形参`Payee`关联实参`John Doe`，形参`Amount`关联实参`150`，从而使函数得以执行。但是，使用语句

```Python
PrintCheck(150, "John Doe")
```

调用函数会将值`150`赋给形参`Payee`，将`'John Doe'`赋给形参`Amount`，而这会导致错误的结果。

**按值传递**（pass by value）：是指对于实参所表示的数据产生一个副本传给函数，函数对数据所做的任何修改仅仅是对副本的修改——调用程序单元中的数据并没有被修改，我们称这种参数传递方式为按值传递。<br>按值传递参数可以保护调用单元中的数据，使之不会被设计不当的函数错误地修改。

**按引用传递**（pass by reference）是指在调用程序单元中告诉函数所需的实参的地址，从而使函数可以对实参进行直接存取。<br>按引用传递参数允许函数修改调用环境中的数据。

----

**基本知识点**

* 抽象用允许软件复用的输入参数来概括功能。

___

### 6.3.3  有返回值的函数

实际上，计算机科学中的**函数**源于数学中的函数概念，默认情况下，它是一个输入集和一个输出集之间的关系。也就是说，执行函数的结果就是计算出一个值并将这个值发送回调用程序单元。这个值可以存储在一个变量里供以后引用，也可以立即用于计算。
在程序中定义有返回值的函数和没有返回值的函数的方式基本相同。它们的不同在于，前者的函数头通常以指定要返回的值的数据类型开始，函数定义通常以明确指定要返回的值的`return`语句结束。
下面给出了一个名为`CylinderVolume`的函数的定义：

```C
float CylinderVolume (float Radius, float Height)
{float Volume;        //声明一个名为 Volume 的局部变量
 Volume = 3.14 * Radius * Radius * Height;        //计算气缸的容积
 return Volume;        //终止函数并返回变量 Volume 的值
    
}
```

当名为`CylinderVolume`的函数被调用的时候，函数的形参`Radius`和`Height`接收确定的值，函数使用这些尺寸计算气缸容积并返回这个计算结果。因此，类似下面的语句可用在程序的其他地方来确定一个半径为 （实参）3.45、高为 12.7 的气缸内所盛物体的成本：

```C
cost = CostPerVolUnit * CylinderVolume(3.45, 12.7);
```

`CylinderVolume`的 Python 版本为：

```python
def CylinderVolume(Radius, Height):
    Volume = math.pi * Radius * Radius * Height
    return Volume
```

程序设计语言允许软件开发人员创建自己的函数作为手段，用于实现软件复用、提高程序清晰度和可读性以及提升正确代码。复杂的整体算法比明智分解为较小函数的等价程序更难测试。编写良好的函数可以独立于较大的算法进行测试，增加发现隐藏软件 bug 的机会。一旦设计者更加确定组成函数块是正确的，他就会更加确信整个系统是正确的。

----

**基本知识点**

* 使用现有的正确算法作为构建块来构造新算法有助于确保新算法是正确的。

----

