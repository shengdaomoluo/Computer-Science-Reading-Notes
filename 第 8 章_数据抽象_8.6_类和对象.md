## 8.6  类和对象

面向对象范型使得系统可由称为对象的单元组成，这些对象通过彼此交互来完成任务。每个对象都是一个响应来自其他对象的消息的实体。对象由称为类的模板描述。

在许多方面，这些类实际上就是抽象数据类型（其实例称为对象）的描述。下面是 Java 语言和 C# 语言是如何定义`StackOfIntegers`类的：

```Java
class StackOfIntegers implements StackType
{
  private int[] StackEntries = new int[20];
  private int StackPointer = 0;
  
  public void push(int NewEntry)
  {  if (StackPointer < 20)
        StackEntries[StackPoint++] = NewEntry
  }
  
  public int pop()
  {  if (StackPointer > 0) return StackEntries[--StackPointer];
        else return 0;
  }
  public boolean isEmpty()
  {      return (StackPointer == 0);  }
  
  public boolean isFull()
  {      return (StackPointer >= MAX);  }
}
```

注意：这个类为每个在抽象数据类型`StackType`中声明的函数都提供了一个函数体。此外，这个类包含一个称为`StackEntries`的整型数组和一个用来确定数组中栈顶位置的整数`StackPointer`。

用这个类作为模板，在 Java 或 C#程序中用语句`StackType StackOne = new StackOfIntegers();`或者C++程序中语句`StackOfIntegers StackOne();`来创建一个名为`StackOne`的对象。在后面的程序中，可以用语句`StackOne.push(106);`将值106压入`StackOne`中，或者用语句`OldValue = StackOne.pop();`检索`StackOne`的栈顶顶放到变量`OldValue`中。

这些特征与抽象数据类型的那些特征本质上是一样的。不过，类与抽象数据类型之间还是有区别的。**类是抽象数据类型的扩展**。