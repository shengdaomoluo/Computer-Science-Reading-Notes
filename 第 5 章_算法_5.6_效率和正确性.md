## 5.6 效率和正确性

效率和正确性是计算机科学研究领域的重要主题。

从一般意义上，什么是效率和正确性呢？

* **效率**（efficiency /ɪˈfɪʃ.ən.si/）：是一个描述资源（如时间、精力、金钱等）利用程度的概念，通常用来衡量在完成某项任务或实现某个目标的过程中，资源的使用是否令人满意或是否达到最佳状态。简而言之，效率就是如何在尽可能少的资源投入下获得尽可能多的产出。

* **正确性**

### 5.6.1  算法效率

通常，在高效算法和低效算法之间的选择会产生问题的实用解决方案和不实用解决方案之间的区别。

<img src="https://p.ipic.vip/qo83p8.jpeg" alt="插入排序算法最差情况分析图" style="zoom:50%;" />

<img src="https://p.ipic.vip/11u61b.jpeg" alt="二分搜索算法最差情况分析曲线图" style="zoom:50%;" />



两图的区别在于图的大体形状，该大体形状揭示了一个算法在应对越来越大的输入规模的情况下到底应该有多好。此外，图的大体形状是有表达式的类型而非具体的表达式所决定的：

* 所有的线性表达式都会产生一条直线；
* 所有的二次表达式都会产生一条抛物线；
* 所有的对数表达式都会产生一条对数曲线。

习惯上，我们用可以产生一个形状的最简单的表达式来标识该形状。具体而言，我们用表达式 n<sup>2</sup>来标识抛物线现状，用 log<sub>2</sub>n 来标识对数曲线形状。

由于通过直角一个算法执行起任务所需的时间于起输入数据大小所得的图的形状反映了算法的效率特征，因此可以根据这些图的形状对算法进行分类——通常是基于算法的最差情况分析。

用来标识这些类型的符号有时称作**大Θ符号**（big-theta/ˈθiː.tə/ notation/nəʊˈteɪʃn/）:

* 所有图为对数曲线的算法，如插入排序算法，都划分到Θ(n<sup>2</sup>)（读作“n的平方的大Θ”）表示的算法类型；
* 所有图为对数曲线的算法，如二分搜索算法，都划分到Θ(log<sub>2</sub>n)（读作“log<sub>2</sub>n 的大Θ”）表示的算法类型；

知道特定算法归为哪一类，就可以预测它的性能，并且可以拿它于能做解决相同问题的其他算法进行比较。

----

**基础知识点**

* 找到问题的高效算法可以帮助找到问题的更大实例。
* 确定算法的效率是通过对算法的形式化或数学推理来完成的。
* 同一个问题的不同的正确算法可能会有不同的效率。

-----

### 5.6.2  软件验证（关于算法的正确性）

**基础知识点**

* 算法的正确性是对算法的形式化或数学推理来确定的，而不是通过测试算法的实现来确定的。

软件验证是算法正确性的一项重要的保证，对有效验证技术的搜寻也是计算机科学中一个活跃的研究领域。

在这个领域内，研究的一条主线尝试把形式逻辑技术用于证明一个程序的正确性。也就是说，目标是用**形式逻辑**来证明程序表达的算法确实做了它打算的事。基本论点是通过将验证过程简化为一个跨市过程，以防止那些可能与直觉有关的不准确结论。

> 形式逻辑：是逻辑学的一个分支，主要研究思维的形式结构和规律，而不是思维的具体内容。

程序正确性的形式证明是基于设计程序所使用的规范说明进行的。为了证明一个程序可正确地对名字列表进行排序，我们可以从假设程序的输入是一个名字列表开始，或者说，如果一个程序是设计用来计算一个或更多个正数的平均值的，就假设输入确实是由一个或多个正数组成的。

简而言之，正确性的证明是从以下假设开始的：<br>程序开始执行时满足某些条件——即**前置条件**（precondition /ˌpriː.kənˈdɪʃ.ən/，前提；先决条件）。

正确性证明的下一步是考虑这些前置条件的结果是如何在程序中传播的。以`if-else`结构为例：

```python
if (条件):
  指令 A
else:
  指令 B
```

如果已知某个语句在结构执行之前成立（即前置条件满足），那在紧接着执行`指令 A`之前，我们知道那条语句和那个测试条件皆为真；<br>相反，如果要执行的是`指令 B`,我们就知道那条语句和测试条件的否定一定成立。

依照这些规则，可以通过识别称作**断言**（assertion/əˈsɜː.ʃən/，明确肯定）的语句来进行正确性的证明，断言可以建立在程序的各个点上。结果是一个断言集合，每个断言都是程序前置条件的一个结果以及可以导致在程序中的某点建立断言的一组指令。如果在程序的末尾建立的断言可以得到想要的输出规格说明[称为**后置条件**（postcondition/pəʊst kənˈdɪʃn/）]，我们就能够断定程序是正确的。

<img src="https://p.ipic.vip/h0uj22.jpeg" alt="与典型的 while 结构相关的断言" style="zoom:50%;" />

以上图为例。假设，作为在 A 点给定的前置条件的结果，循环过程中每次进行终止测试的时候（B 点），我们都能确认一个特定断言为真。然后，如果循环永远终止，执行就会转移到 C 点，在这里我们可以得出结论：**循环不变式**和终止条件均成立。

* 循环不变式仍旧成立是因为终止测试不改变程序中的任何值；
* 终止条件成立是因为如果它不成立循环就不会终止。

如果这些组合语句暗示着期望的后置条件，我们的正确性证明仅仅通过初始化和修改最终导致终止条件的循环组件就可以完成。