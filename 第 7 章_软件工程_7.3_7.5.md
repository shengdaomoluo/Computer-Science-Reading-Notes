## 7.3  软件工程方法学

**瀑布模型**（waterfall model）：强调以一个严格的顺序进行需求分析、设计、实现和测试的软件开发过程，之所以称这种开发过程为“瀑布”模型，是因为它跟瀑布一样，只能向一个方向“流动”。

**增量模型**（incremental model）：软件系统以一个“增量”的模式构建，即软件产品先以功能有限的简化版本出现，一旦这个版本的系统通过了测试并且可能还经过未来用户的评估，就可以以递增的方式不断地向系统中添加更多的功能并测试，直至整个系统全部完成。<br>增量模型使用的是*扩展*产品的每个前期版本到更大的版本。

**迭代模型**（iterative model）:通过逐步构建和改进软件系统来实现最终产品的开发。<br>迭代模型使用的是*改进*每个版本的概念。

增量模型与迭代模型虽然是不同的两个概念，但它们有许多相同的地方。实际上，增量模型包含一个基本的迭代过程，而迭代模型可以增量增加功能。增量模型和迭代模型有时会利用软件开发朝着**原型开发**（prototyping/ˈprəʊtətaɪpɪŋ/） 发展的趋势。

**原型开发**是构建并评估预期系统的非完整版本（称为原型）的过程。

* 在增量模型中，将这些原型发展为一个最终的完整系统的过程称为**演化式原型开发**（evolutionary/ˌiː.vəˈluː.ʃən.ər.i/ prototyping）。
* 在迭代性更强的情况下，可能会丢弃原型，以使最后设计有全新的实现，这种方法称为**抛弃式原型开发**（throwaway prototyping）。

**开源开发**（open-source development）：是指软件的源代码是公开的，任何人都可以查看、使用、修改和分发。这种开发模式鼓励协作和共享，社区成员可以为软件的开发、改进和维护做出贡献。

**敏捷方法**（agile/ˈædʒ.aɪl/ method）：是在增量的基础上进行早期快速的实现，响应需求变更，降低对严格需求分析和设计的强调。敏捷方法的一个例子是**极限编程**（extreme programming，XP）。

* 极限编程：团队在一个场所内自由地交换想法，在开发项目过程中相互协作，通过每天不断重复“非正式需求分析、设计、实现和测试”这样一个周期的方式，增量开发软件。

## 7.4  模块化

**模块化**（modularity/ˌmɒd.jəˈlær.ə.ti/），是指把软件分割成多个易于处理的通常称为模块的单元，每个单元仅承担整个软件的一部分职责。

### 7.4.1  模块式实现

模块可以以不同的方式实现。

* 在命令型范型的语境中，模块表现为函数。
* 在面向对象范型中，则是利用对象作为其基本的模块要素。

### 7.4.2  耦合

一般意义上，**耦合**（coupling /ˈkʌp.lɪŋ/）：通常指两个或多个系统、组件或实体之间的关联程度或依赖关系。

在计算机科学和软件工程领域，**耦合**（coupling）是指模块或组件之间的依赖程度。模块间耦合有多种形式：

* **控制耦合**（control coupling）：指的是两个模块之间移交执行控制给另外一个模块。
* **数据耦合**（data coupling）：指的是两个模块之间的数据共享。
  * **全局数据**（global data）：是指可以被整个系统中的所有模块使用的数据项。

模块间的耦合应最小化。

### 7.4.3  内聚

**内聚**（cohesion /kəʊˈhiː.ʒən/）：指的是每个模块内部各部分的关联程度，它描述了模块内部各个部分相互结合的紧密程度。高内聚意味着模块内的功能高度相关，模块内部各部分共同为实现一个特定的功能而协同工作。

* **逻辑内聚**（logical cohesion）：模块内的各部分元素本质上实现逻辑上相似的活动。
* **功能内聚**（functional cohesion）：是指模块中所有部分都聚焦于某一活动的性能。

软件要力求做到模块内部的高内聚，实现内部绑定程度的最大化。

### 7.4.4  信息隐藏

**信息隐藏**（information hiding）：指的是限制软件系统的指定部分信息的使用。信息隐藏的关键是阻止模块的动作对其他模块产生不必要的依赖或影响。

重要的是要注意到信息隐藏具有两个化身：一个作为设计目标，例子是最大化内聚和最小化耦合；另一个作为实现目标，例子是使用局部变量、应用封装和使用定义明确的控制结构。

### 7.4.5  组件

**组件**（component）：指的是软件的一个可复用的单元。

**接口**（interface）：是一个抽象的概念，它定义了一组功能或方法，但不包含具体的实现。接口用于规定不同软件组件之间的交互方式，使得不同模块或系统之间能够协同工作。

## 7.5  行业工具

行业工具是软件开发的分析与设计阶段使用的一些建模技术和符号系统。

### 7.5.1  较老的工具

**数据流图**（dataflow diagram）：是表示从数据流分析中获得信息的一种手段。<br>在数据流图中，箭头表示数据路径，椭圆表示数据操控发生的位置，矩形表示数据源和数据存储。

**数据字典**（data dictionary）：是整个软件系统中出现的数据项的一个中央信息库。这些信息包括：<br>为引用每个数据项所采用的标识符、每个数据项里的有效条目的构成情况、数据项存储在什么地方、软件在什么地方引用这些数据项。

构建数据字典的一个目标是，增强软件系统的利益相关者与软件工程师之间的沟通，其中软件工程师负责将利益相关者的需求转化为需求规格说明。构建数据字典的另一个目标是确立整个系统的一致性，借助构建字典常常可发现冗余和矛盾。

### 7.5.2  统一建模语言

**统一建模语言**（Unified Modeling Language/ˈjuːnɪfaɪd ˈmɒdlɪŋ ˈlæŋɡwɪdʒ/，UML）是基于面向对象范型思想发展而来的一种标准化的建模语言，用于对软件系统进行可视化、描述和文档化。它提供了一套图形化的工具，帮助开发者和设计者在软件开发过程中表达系统的结构、行为和交互。

* **用例图**（use case diagram）：描述系统功能及其与用户（或其他系统）的交互，重点在于用户需求。用例图是用：
  * 大的矩形框来描述拟建系统的，在这个矩形框中，系统与用户之间的交互，称为**用例**（use case），是用椭圆来表示的，而系统中的用户称为**参与者**（actor），是用火柴人表示的。
* **类图**（class diagram）：描述系统中的类及其属性、方法和类之间的关系。其中：
  * 矩形框表示类，线表示关联。
* ……

### 7.5.3  设计模式

**设计模式**（design pattern）：是用来解决软件设计过程中反复出现的问题的一种预先开发的模型。

* 适配器模式，用来解决通过预制模块来构建软件时经常出现的问题。
* 装饰者模式，提供了一种用来设计系统的手段，而所设计的系统依据当时的情况完成一些相同活动的不同组合。

反复出现的问题的识别和用于解决这些问题的设计模式的创建和分类在软件工程领域里是一个不断进步的过程。然而，其目标不仅仅是找到设计问题的解决方法，还要找到能在软件生命周期的后期提供灵活性的高质量解决方案。因此，对诸如耦合最小化和内聚最大化这样的好的设计原则的考虑，在设计模式的发展过程中起着重要的作用。