## 8.5  定制的数据类型

大多数程序设计语言都提供了如整型、浮点型、字符型和布尔型等基本的数据类型。此外，为了更好地满足某个具体应用的需要，可以定义一些自己的数据类型，以下是几种定义的方式：

### 8.5.1  用户定义的数据类型

许多现代的程序设计语言允许利用基本数据类型作为构建块，定义一些附加的数据类型，即**用户定义的数据类型**（user-defined data type），其本质是几个基本数据类型组合而成的具有同一名字的聚合体（conglomerate /kənˈɡlɒm.ər.ət/）。

```c
struct
{
  char    Name[25];
  int     Age;
  float   SkillRating;
} Employee;
```

上面使用 C 语言的语句定义了一个称为`Employee`的新聚合，其中包含 3 个字段`Name`（字符型）、`Age`（整型）、和`SkillRating`（浮点型）。与之相反，下面的 C 语句并没有定义新聚合变量，而是定义了一个新聚合类型`EmployeeType`：

```C
struct EmployeeType
{
  char Name[25];
  int  Age;
  float SkillRating;
};
```

这样一来，就可以采用与基本数据类型相同的变量声明方式，用这个新的数据类型来声明变量了。也就是说，与 C 语言允许用语句`int x;`将变量`x`声明为整型的方式一样，可以用语句`struct EmployeeType Employeel;`将变量`Employee1`声明为`EmployeeType`类型。然后，在程序的后面，变量`Employee1`将引用一整块存储单元，该存储块中包括员工的名字、年龄和技能级别。存储块中的各个项可以通过诸如`Employee1.Name`和`Employee1.Age`这样的表达式来引用。<br> 因此，下面这样的语句可能被用来将值 26 赋给`Employee1`块中的`Age`字段：

```C
Employee1.Age = 26;
```

此外，下面这样的语句形式可以用来将 3 个变量`DisManager`，`SalesRep1`和`SalesRep2`声明为`EmployeeType`类型：

```C
struct EmployeeType DistManger, SalesRep1, SalesRep2;
```

就像下列形式的语句通常被用作将变量`Sleeve`、`Waist`和`Neck`声明为基本的`float`类型一样：

```C
float Sleeve, Waist, Neck;
```

区分用户定义的数据类型和这个类型的一个实际项非常重要。用户定义的数据类型的一个实际项称为该类型的一个**实例**（instance）。**用户定义的数据类型本质上是一个用来构建类型实例的模板**。该模板描述了这种类型的所有实例所具有的属性，但是它本身并非这种类型的一个实例。<br>在上面的例子中，用户定义的数据类型`EmployeeType`构建了 3 个该类型的实例——`DistManagerr`、`SalesRep1`和 `SalesRep2`。

### 8.5.2  抽象数据类型

在许多程序设计语言里，用户定义的数据类型都扮演着重要的角色，帮助软件设计者根据特定程序的需要量身定制数据表示。然而，传统的用户定义的数据类型仅允许程序员定义新的存储系统，而没有提供对具有这些结构的数据进行的操作。

抽象数据类型（abstract data type， ADT）是一种用户定义的数据类型，可以同时包含数据（表示）和函数（行为）。支持抽象数据类型创建的程序设计语言通常都有两个 特征：

1. 具有“用于将抽象数据类型定义为单个单元”的语法；
   * 这是一个重要的组织工具，能将抽象数据类型的数据和函数组织在一起，这简化了维护和调试。
2. 具有“将抽象数据类型的内部结构隐藏起来，不让程序的其他部分利用”的隐藏机制。
   * 这提供了可靠性，它能阻止抽象数据类型以外的其他代码访问其数据，无须通过专为此目的提供的函数。

为了对此进行说明，这里假设我们想要在一个程序中创建和使用几个整数值的栈。方法可以是，将每个栈实现成一个由 20 个整数值的数组。将栈底的项放在数组的第一个位置，将栈的其他项相继放入数组的高位项。再用一个额外的整型变量作为栈指针，用来存放数组项的下标，而下一个栈顶将会被压入该数组中。因此，每个栈都由一个包含栈本身的数组和一个起着栈指针作用的整数组成。

为了实现这个构想，首先要用下列形式的 C 语句来建立一个称为`StackType`的用户定义的类型：

```C
struct StackType
{
  int StackEntries[20];
  int StackPointer = 0;
};
```

做了这个声明以后，我们就可以通过下列语句声明名为`StackOne`、`StackTwo`和`StackThree`的栈：

```C
struct StackType StackOne, StackTwo, StackThree;
```

此时，变量`StackOne`、`StackTwo`和`StackThree`中的每一个都可以引用一个唯一的存储单元块，用于实现自己的栈。但是，如果现在想把值 25 压入`StackOne`会怎么样呢？当然，我们希望能屏蔽以栈的实现为基础的数组结构的细节，仅将栈作为一个抽象工具来使用，即可能会用类似于`push(25, StackOne);`这样的函数调用。但是，如果不定义称为`push()`的相应函数，这样的一条语句就不可用。<br>我们想对`StackType`类型的变量进行的其他操作有：从栈中弹出项、检查栈是否为空以及检查栈是否为满。所有这些操作都要求定义相应的函数。简而言之，我们的`StackType`数据类型的定义并不包括我们想让这个类型关联的所有属性。此外，程序里的任何函数都有可能访问我们的`StackType`变量的`StackPointer`字段和`StackEntries`字段，绕开我们设计到函数`push()`和`pop()`中的仔细检查。程序另一部分中的草率的赋值语句会重写存储在栈数据结构中间的数据元素，甚至会破坏所有栈共有的特征——后进先出行为。

我们需要一种机制，既能定义允许对`StackType`进行的操作，又能保护内部变量不被外部引用。Java 语言的interface语法就是就是这样一种机制。例如，在 Java 中，可以写：

```java
interface StackType
{
  public int        pop(); /* Get the item at top of stack */
  public void push(int item); /* Add a new item to stack */
  public boolean    isEmpty(); /* Check if stack is empty */
  public boolean     isFull(); /* Check if stack is full */
}
```

单独来讲，这个抽象数据类型没有指定如何存储栈，或者用什么算法来执行`push()`函数、`pop()`函数、`isEmpty()`函数和`isFull()`函数。这些细节（已被 interface 抽象出来）会被其他 Java 代码在其他地方指定。然而，像我们前面的用户定义的数据类型一样，程序员能把变量或函数参数声明为`StackType`类型的。

我们可以用下列语句把 `StackOne`、`StackTwo`和`StackThree`声明为栈：

```Java
StackType StackOne, StackTwo, StackThree;
```

在程序的后面，我们可以用下面的语句将这些项压入这些栈中：

```java
StackOne.push(25);
```

该语句表示用值 25 作为实际参数，执行与`StackOne`相关联的`push()`函数。

相对于跟基本的用户定义的数据类型而言，抽象数据类型是完整的数据类型。

