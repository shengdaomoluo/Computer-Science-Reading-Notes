## *12.6  公钥密码学

**RSA 算法**（RSA algorithm）：是利用一组称为**加密密钥**（encrypting key）的数值对报文进行加密，并利用另一组称为**解密密钥**（decrypting key）的数值对报文进行解密。

* 知道加密密钥的人可以对报文进行加密，但是不能对报文进行解密。
* 只有持有解密密钥的人才能对报文进行解密。

这样一来，加密密钥就可以被广泛地分发而不会破坏系统的安全性。

**公钥加密**（public-key encryption）系统：是指利用 RSA 算法的密码系统。

* **公钥**（public key）即加密密钥。
* **私钥**（private key）即解密密钥。

### 12.6.1  模表示法

为了描述 RSA 公钥加密系统，可以采用记号`x%m`来表示数值 x 除以m 后所得的余数。如果 p 和 q 是**素数**（prime number），m 是 0 到 pq（表示 p 和 q 的乘积）的一个整数，那么，对于任意的正整数 k，就有：

**<big>1 = m<sup>k(p-1)(q-1)</sup> % pq</big>**

**素数**（prime number）：是指大于 1 的自然数，其正因数只包括 1 和其自身。

###  12.6.2  RSA公钥密码学

 利用 RSA 算法构建和分析一个公钥加密系统：

1. 选出两个不同的素数 p 和 q，其乘积用 n 来表示。
2. 另外选出两个正整数 e 和d，使得对于某个正整数 k，满足`e x d = k (q-1)(p-1) +1`。

因此，这里选取了 5 个值：p、q、n、e 和 d。其中：

* 值 e 和 n 是加密密钥；
* 值 d 和 n 是解密密钥；
* 值 p 和 q 只是用来构建加密系统的。

现在考虑如何对报文进行加密。为此，假设当前报文是按照位模式编码的，当将其解释为二进制表示时，报文表示值 m，那么这条报文的加密形式就是值c = m<sup>e</sup> %n的二进制表示。<br>为了解密一条用二进制记法表示的值为 c 的报文，就要计算 c<sup>d</sup> % n 的值：c<sup>d</sup> %n =m<sup>exd</sup> %n =m<sup>k(p-1)(q-1)</sup> %n =m x m<sup>k(p-1)(q-1)</sup> %n =m%n =m

概括来说，一个 RSA 公钥加密系统的产生过程是这样的：

* 选取两个素数 p 和 q，再从这两个数产生值 n、e 和 d。值 n 和 e 被用于加密报文，即为公钥；
* 值 n 和 d 被用于解密报文，即为私钥。

这个系统的安全性基础在于，假定只知道加密密钥 n 和 e，而不允许计算解密密钥 n 和 d。然而，确实有这样的算法能做到。一种方法是对值 n 进行因式分解找到 p 和 q，然后找一个 k 值，使 k (p-1)(q-1) +1 被 e 整除（那么商就为 d），从而确定 d。<br>但是，这个过程的第一步就会很耗时，特别是在所选的 p 和 q 很大的情况下。事实上，如果 p 和 q 大到需要用几百个二进制位来表示，那么即使用最好的因式分解算法对 n 进行分解，也需要好几年时间才确定 p 和 q。

**时至今日，没有人能够在不知道解密密钥的情况下找到一种基于 RSA 密码学加密的报文进行解密的有效方法。**