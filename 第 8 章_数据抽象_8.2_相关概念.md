## 8.2  相关概念

与数据结构紧密相关的主题：抽象、静态结构和动态结构的区别以及指针的概念。

### 8.2.1  再谈抽象

计算机的主存储器并不是按照数组、列表、栈、队列和树这样的结构来组织数据的，而是由一组可寻址的存储单元顺序组成的。这样一来，所有的其他结构都必须进行模拟。<br>数组、列表、栈队列和树这样的组织都是被创建的抽象工具，之所以构造这些抽象工具，是为了便于对数据**用户**屏蔽实际数据存储的细节，允许用户访问，就像这些信息是以一种较为方便的形式存储的一样。

在这里，**用户**这个术语并不一定是指人，用户的含义取决于我们当时的视角。

* 如果从一个使用 PC 来维护保龄球联赛记录的人的角度考虑，那么用户就是一个人。
* 如果从因特网上的一个服务器的角度考虑，那么这时的用户可能是一个客户机。
* 如果从程序的模块化结构来考虑，那么用户应该是需要访问这些数据的任何模块。

所有这些情况中，有一条共同的主线，那就是用户拥有将数据作为一个抽象工具来访问的特权。

### 8.2.2  静态结构和动态结构

数据所模拟的结构是静态的还是动态的，是根据结构的大小或形状是否会随时间改变而进行判断的。

* 如果一个结构是静态的，那么只需要提供一种能够访问结构中不同数据项的方法，或许还需要提供一种能改变指定位置的值的方法。
* 如果一个结构是动态的，就必须处理添加和删除项的问题，还要找到因数据结构增长所需的存储空间。

就一般规律而言，静态结构比动态结构更容易管理。

#### 垃圾收集

**垃圾收集**（garbage collection）是指随着动态数据结构的扩大和缩小，存储空间会被使用和释放。回收不用的存储空间以备将来使用的过程称为垃圾收集。

### 8.2.3  指针

计算机的主存储器中的各个单元是由数字地址来标识的。这些地址作为数值本身就可以进行编码并存储在存储单元中。<br>**指针**（pointer）就是包含这种编码地址的一个存储区，它是用来记录存储数据项的位置的。

许多现代程序设计语言都把指针作为一种基本数据类型。<br>也就是说，这些语言允许对指针进行声明、分配和操作，就相对整数和字符串那样。