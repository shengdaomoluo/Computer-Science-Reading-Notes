## 9.2 关系模型

关系数据库模型是一种组织和管理数据的方式，其中数据存储在类似于电子表格那样格式的表格中，这些表格被称为“关系”，每个表由行和列组成，列定义了表的数据属性，行代表了具体的数据记录。<br>关系数据库模型是由 E.F.Codd 于 1970 年提出的，它利用数学中的集合论和关系代数来管理数据，成为了数据库设计的的主流模型。

关系中的一行称为一个**元组**（tuple）。关系中的一列称为**属性**（attribute），描述的是对应的元组说表示的实体的特征。

### 9.2.1  关系设计中的问题

设计关系数据库的关键步骤是设计构成这个数据库的关系，既表格的行——元组；列——属性。

把一个关系分解成几个比较小的关系时，信息有时会丢失，有时不会丢失。不会丢失信息的关系分解，称为**无损分解**（lossless decomposition 或 nonloss decompsosition）。

分解关系的目标之一就是找出会在数据库设计中引发问题的一些关系特征，并找到从新组织这些关系以消除这些问题特征的方法。

### 9.2.2  关系运算

在一个关系上想要执行一种运算就是要选取具有某些特征的元组，并把这些选出的元组放在一个新关系中。<br>为了表示这种运算，我们采用下面的语法：

```SQl
NEW ← SELECT from EMPLOYEE where EmplId = '34Y70'
```

此语句的语义是：创建一个名为`NEW`的新关系，它包含从`EMPLOYEE`关系选得的其 `EmplId`属性等于`34Y70`的那些元组。

**`SELECT`**运算是从一个关系中提取行；与此相反，**`PROJECT`**运算是从一个关系中提取列。

```sql
NEW2 ← PROJECT JobTitle from NEW1
```

结果是创建了另一个名为`NEW2`的新关系，它包含从`NEW1`关系中`JobTitle`列得到的那些值所构成的一个列。

再举一个`PROJECT`运算的例子。语句：

```sql
MAIL ← PROJECT Name, Address from EMPLOYEE
```

可以用来获取一个名为`MAIL`的新关系，该关系具有`Name`和`Address`两列关系。

**`JOIN`**运算把原来不同的关系组合成一个新关系。<br>两个关系的连接产生一个新关系，而新关系的属性由原来两个关系的属性组成。新关系的元组（行）由原来两个原始关系的元组拼接而成。哪几个元组会实际连接成新关系的元组取决于构建`JOIN`的条件。

### 9.2.3  SQL

基于关系模型的数据库管理系统会包括完成`SELECT`、`PROJECT`、`JOIN`运算的例程，应用软件可以调用它们。通过这种方式，编写应用软件就好像数据库真的是存储在关系模型的简单表格中。

**结构化查询语言**（structured query language， SQL），它构成了绝大多数关系数据库查询系统的主干。<br>虽然看起来用 SQL 表述的查询好像是以命令的形式来完成 ，但本质上它是一种陈述性的语句。应当把一条 SQL 语句看作是对所需信息的一种描述，而不是一串要执行的操作。这样的意义在于，有了 SQL，应用程序员就不必为开发处理关系的算法而花费精力，他们只要描述所需的信息就可以了。

```sql
SELECT EmplId, Dept
FROM Assignment, Job
WHERE Assignment.JobId = Job.JobId
  AND Assignment.TermDate = '*'
```

这个示例表明，每条 SQL 查询语句可包含 3 条子句：

* 一条`SELECT`子句；
* 一条`FROM`子句；
* 一条`WHERE`子句。

其实这样一条语句就是请求如下运算的结果：`from`子句中列出的所有关系的`JOIN`运算，然后是选出满足`WHERE`子句中条件的那些元组的`SELECT`运算，最后在`SELECT`子句中列出的元组上进行`PROJECT`运算。