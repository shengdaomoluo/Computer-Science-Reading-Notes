## 10.2  3D图形学概述

从创建和显示图像的整个过程开始对 3D 图形学的学习，这个过程由三步构成：建模；渲染和显示。

* 建模，是用数字编码数据和算法设计和构建布景。因此，在计算机图形学背景下产生的场景可能在现实中永远都不存在。
* 渲染，通过计算场景中的物体如何出现在由特定位置的摄像机拍摄的照片中，来制作场景的二维图像。
* 显示，当确定了每个像素的外观后，结果被标识成图像的位图（一种图像的文件格式，它通过使用像素网络来表示图像），集中存储在**帧缓冲区**（frame buffer）的存储区域中。<br>存储在帧缓冲区中的图像要么为了观看而显示，要么为了以后显示传送给更持久的存储器。
  * **帧缓冲区**（frame buffer）：可能是主存储器中的一个区域，或者在由专门为图形应用设计的硬件的情况下，它也可能是一块专用存储电路。