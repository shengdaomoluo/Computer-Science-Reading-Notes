# 第 1 章  数据存储

本章主要学习有关计算机中数据表示和数据存储的内容。研究的数据类型包括文本、数值、图像、音频和视频。
**计算制品**是人类用计算机创造出来的东西，可以是但不限于程序、图像、音频、视频、演示或者网页文件。

## 1.1  位和位存储

在今天的计算机内，所有的信息是以 0 和 1 的模式编码的，这些数字称为**位**（bit，是 binary digit 的简写，意思是*二进制数字*）。

> 二进制数字：
>
> > 二进制数字是基于二进制（Binary）数制的数字系统，二进制是计算机科学中的基本数制。与十进制系统使用十个数字（0到9）不同，二进制系统仅使用两个数字：0和1。这两个数字称为“位”（bit），是“binary digit”的缩写。
> > 在二进制系统中，**每个位置上的数字代表的值是2的幂次方**。
> >
> > >例如：二进制数1011可以解释为：
> > >
> > >* 1 × \(2^3\) = 8
> > >* 0 × \(2^2\) = 0
> > >* 1 × \(2^1\) = 2
> > >* 1 × \(2^0\) = 1
> > >  这样，1011在十进制中等于11。
>
> > 二进制系统之所以在计算机科学中如此重要，是因为计算机使用电子开关（如晶体管）来处理数据，这些开关具有两种状态：开（on）和关（off）。这两种状态自然对应于二进制中的1（开，on）和0（关，off），使其成为计算机硬件的最直接和有效的表示方法。通过组合大量的二进制位，计算机可以表示复杂的数据和指令。

位（即 0 和 1）实际上只是一些符号，其含义取决于正在处理的应用。位模式有时表示数值，有时表示字母表里的字符和标点符号，有时表示颜色和图像，还有时表示声音。

### 1.1.1  布尔运算

处理真/假值的运算被称为**布尔运算**（Boolean operation），3 个基本的布尔运算是 AND（与）、OR（或）和 XOR（异或），布尔运算组合的是真/假值，而不是数值。最基本的布尔运算如下：

> 1. 与（AND）运算：只有当所有操作数都是“真”时，结果才是“真”。在二进制中，只有当所有输入都是 1 时，输出才为 1。
> 2. 或（OR）运算：只要有一个操作数为真，结果就是“真”。在二进制中，只要有一个输入为 1，输出都是 1。
> 3. 异或（XOR）运算：这个运算在两个操作数不同时，结果为真。在二进制中，只有当输入不同（一个为 1 一个为 0）时，输出才为 1。
> 4. 非（NOT）运算：这个是一元运算符，它将操作数的值反转，即“真”变为“假”，“假”变为“真”。在二进制中，NOT 运算将 1 变为 0，0 变为 1。

### 1.1.2  门和触发器

>**门**（gate），有时也称**逻辑门**（logic gate），指的是一种设备，给定一种布尔运算的输入值，门可以得出该布尔运算的输出值。

* 逻辑门是布尔函数建模的硬件抽象。
  门为构造计算机提供了*构建块*。因此布尔逻辑和运算符作为基本运算出现在程序设计语言中。

> “*构建块*”，这一术语通常用于描述组成一个更大系统或结构的基础性组件或元素。这一概念可以应用于多个领域，如计算机科学、工程、建筑、化学、教育等。在每个领域，构建块代表不同的意义，但其核心思想是相同的，即它们是更复杂系统的基本组成部分，类似于积木搭建建筑物。
>
> > 在计算机科学中，构建块可以是硬件组件，如CPU、内存、存储设备、输入/输出设备等，也可以是软件组件，如函数、类、模块等。

**触发器**是计算机存储器的基本单元，它是一个可以产生 0 和 1 输出值的电路，它的值会一直保持不变，直到有另一个电路过来的脉冲（临时变成 1 之后在变为 0）使其变换成其他值。换句话说，通过设置，可以让输出在外界刺激的控制下“记住”0 或者 1。

### 1.1.3  十六进制记数法

考虑计算机的内部活动时，我们必须考虑位模式的处理问题，我们将位模式称为位串，有些位串相当长。长位串常被称为**流**（steam）。用一个符号表示位模式的 4 位，即构成了**十六进制记数法**。

> 十六进制是以16为基数的记数系统。它使用16个符号来表示数值，从0到9代表0到9，然后用字母A到F代表10到15。
>
> > 例如：十六进制数“1A3”：
> >
> > * “1”代表 16 的平方（256）的倍数（1 倍）。
> > * “A”代表 16 的 1 次方（16）的倍数（A 等于 10，即 10 倍）。
> > * “3”代表 16 的 0 次方（1）的倍数（3 倍）。

#### 二进制数、十进制数与十六进制数各自的概念定义、使用方法和转换规则

二进制数、十进制数和十六进制数是不同的数字表示系统，它们在计算机科学和数学中有着广泛的应用。以下是它们的概念定义、使用方法和转换规则：

##### 二进制数（Binary Number）

**概念定义**：

二进制是一种使用两个符号表示数值的数制，通常用符号“0”和“1”。它是计算机系统中最基础的数字系统，因为计算机内部采用电路的开和关来表示两个状态。

**使用方法**：

* 二进制数的每一位（bit）表示 2 的一个次方。例如：二进制数“101”表示的是（1\times 2^2 + 0 \times 2^1 + 1 \times 2^0 = 5）。

* 在编程中，二进制数通常用于位操作、掩码、标志和其他底层逻辑操作。

**转换规则**：

* **二进制到十进制**：从最低位开始，每个数位乘以 2 的次方然后求和。
* **十进制到二进制**：不断将十进制数除以 2，记录余数，直到结果为0，然后将余数逆序排列。

##### 十进制数（Decimal Number）

**概念定义**：

十进制是我们日常中最常用的数制，基数为 10，使用符号“0”到“9”。它是人类历史上最常用的数字系统。

**使用方法**：

十进制数的每一位代表 10 的一个次方。例如，数字“345”表示的是（3 /times 10^2 + 4 \times 10^1 + 5 \times 10^0）。

**转换规则**：

* 十进制到二进制：见上文“十进制到二进制”。

* 十进制到十六进制：不断将十进制数除以 16，记录余数，直到结果为 0，然后将余数逆序排列。

##### 十六进制数（Hexadecimal Number）

**概念定义**：

十六进制数是一种使用 16 个符号的数制，通常用符号“0”到“9”和“A”到“F”（或“a”到“f”）表示，其中“A”代表 10，“B”代表 11，以此类推，“F”代表 15。

**使用方法**：

* 十六进制对于表示计算机内存地址、颜色编码（如 HTML 颜色）以及其他需要紧凑表示的数制非常有用。

* 每一个十六进制数字可以用 4 位二进制数来表示（因为：2^4 =16）。

**转换规则**：

* 十六进制到十进制：将每位乘以 16 的次方，然后求和。例如，“1A3”表示（1 \times 16^2 + 10 \times 16^1 + 3 \times 16^0 =419）。
* 十六进制到二进制：将每个十六进制数字转化为对应的 4 位二进制数。
* 二进制到十六进制：将二进制数分为 4 位一组，从右到左，不足 4 位的高位补零，然后将每组转化为对应的十六进制数字。

通过掌握这些数制的概念、使用和转换规则，可以更好地理解和应用它们，特别是在计算机编程和数字电路设计领域。

## 1.2  主存储器

**主存储器**(main memory)又称为**位存储器**，即每一个电路都能存储一个位。它的所有工作都是由电子元件实现的。主存储器又被称为**随机存取存储器**（random access memory RAM）以体现其用任何顺序访问存储单元的能力。

### 1.2.1  存储器结构

计算机的主存储器是有可管理的单元组成的，被称为**存储单元**（cell），一个典型的存储单元的容量为 8 位。一个 8 位的串称为一个**字节**（byte）。所以，一个典型的存储单元的容量是一个字节。

虽然计算机没有左或右的概念，但是我们通常<big>假设</big>存储单元的位是排成一行的。该行的左端称为**高位端**（high-order end），右端称为**低位端**（low-order end）。高位端的最左边的一位称作高位或**最高有效位**（most significant bit），取这个名字是因为，如果把存储单元里的内容解释为数值，那么这一位就是该数的最高有效位。类似地，低位端的最右边一位称为低位或**最低有效位**（least significant bit）。

为了区分计算机主存储器中的各个存储单元，每个存储单元都被赋予了一个唯一的“名字”，称为**地址**（address）。

### 1.2.2  存储器容量的度量

早期计算机存储器的大小通常以 1024（2^10）个 存储单元（一个存储单元的容量位一字节，即一个 8 位的串）为单位来度量。在 20 世纪 90年代后期，国际标准组织为 2 的幂制定了专用术语：*千位*字节（kibi-byte）、*兆位*字节（mebi-byte）、*吉位*字节（gibi-byte）和*太位*字节（tebi-byte），用来表示 1024 的幂。

## 1.3  海量存储器

由于计算机主存储器的*易失性*和*容量的限制*，大多数计算机都有称为**海量存储（mass storage，或者二级存储）系统**的附加存储设备。海量存储系统包括磁盘、CD 盘、DVD 盘、磁带、闪存驱动器和固态驱动器。海量存储系统的数据存取需要花费更长的时间。

### 1.3.1  磁系统

磁技术已经占据了海量存储领域。其中最为普遍的是**磁盘**（magnetic disk）或者**硬盘驱动器**（hard disk drive，HDD），它里面是可以旋转的薄盘片，表面有用以存储数据的磁介质涂层。

一个磁盘存储系统都包含许多独立的扇区，每个扇区都可以作为一个独立的位串进行存取。

一个磁盘存储系统的容量取决于使用的磁盘数与扇区的划分密度。

有6个标准可以用来评估一个磁盘系统的性能：

  * **寻道时间**（seek time）：读/写磁头从一个磁道移到另一个磁道所需的时间。
  * **旋转延迟**（rotation delay）：盘片旋转一周所需时间的一半，也就是读/写磁头定位到期望磁道后期望数据旋转到磁头所需的平均时间。
  * **存取时间**（access time）：寻道时间和旋转延迟之和。
  * **传输速率**（transfer rate）：数据传入和传出磁盘的速率。（速率：是一个标量量，表示物体运动的快慢，即单位时间内物体通过的距离。速率不考虑运动的方向，仅描述运动的快慢程度。）
  * **带宽**（bandwidth）：单位时间内传输的总位数。如每秒 3 兆位。<br>系统带宽是位速率的度量单位，位速率是一个固定时间内发送的数据量（用位度量）。
  * 等待时间（latency）：从请求数据传输到数据到达的总时间。

### 1.3.2  光系统

另一类海量存储系统应用的是**光技术**。光技术的应用有以下几种媒介：

* **光盘**（compact disk，CD）:由涂着光洁保护层的反射材料制成。传统 CD 的存储容量是 600～700 MB。

* **数字多功能光碟**（digital versatile disk，DVD）：由多个半透明层构成，这些层在被精确聚焦的激光查看时充当不同的表面。

* **蓝光光碟** （blu-ray disk，BD）：采用蓝光技术，较新的多层 BD 格式容量能够达到 100 GB。

### 1.3.3  闪存驱动器

闪存通过一种叫做“浮动栅极晶体管”的技术来存储数据。数据以电子的形式被捕获在浮动栅极上，从而改变了晶体管的电荷状态，这种状态的变化代表了存储的二进制数据（0和1）。

* 闪存技术的应用为**闪存驱动器**（flash drive），容量可达几百 GB，可用于一般的海量存储应用。

* 较大的闪存驱动器称为**固态驱动器**（solid-state drive，SSD），是专为替代磁硬盘而设计的。
* 闪存技术的另一应用是**安全数字存储卡**，即**SD 存储卡**。后来又发展为 **SD 高容量存储卡**，即**SDHC 存储卡**；新一代的是**SD 扩展容量存储卡**，即 **SDXC 存储卡**。

## 1.4  用位模式表示信息

什么是计算机科学中的**信息**？

> 计算机中存储的**信息**指的是在计算机的存储设备中保存的所有数据和指令，用于支持计算机的运行、数据处理和用户交互。存储的信息可以分为多个类型具体包括：
>
> 1. 程序（代码）
>    * 操作系统
>    * 应用系统
>    * 固件：嵌入硬件设备中，用于控制设备的低级操作，通常存储在只读存储器（ROM）中。
> 2. 数据
>    * 用户数据：用户创建和使用的数据，如文档、图片、视频、音频等。
>    * 配置数据：用于系统或应用程序设置的数据，如用户偏好设置、系统配置文件等。
>    * 临时数据：在程序运行过程中生成并使用的数据，如缓存、会话数据等。
> 3. 元数据
>    * 描述其他数据的特性的信息，如文件大小、创建日期、修改日期、文件类型等。
>    * 数据库中的元数据可以包括表结构、列类型、索引信息等。
> 4. 系统日志和记录
>    * 记录系统或应用程序活动的信息，用于审计、调试或分析。
>    * 日志文件可能包括系统事件、安全事件、应用程序事件等。
> 5. 数据库
>    * 结构化存储大量信息的系统，支持数据的高效存取和管理。
>    * 数据库中可能包含表、视图、存储过程、触发器等。
> 6. 多媒体内容
>    * 图形、声音、视频等多媒体格式的信息。
>    * 包含图像文件（如JPEG、PNG）、音频文件（如 MP3、WAV）、视频文件（如 MP4、AVI）等。
>
> 总之，计算机中的信息涵盖了从底层硬件控制到用户层面交互的广泛内容，通过不同的存储技术和方法组织和管理这些信息，以支持计算机的广泛应用和高效运行。

### 1.4.1  文本的表示

文本形式的信息通常通过代码方式表示，其中文本中的*每个不同的符号（如英文字母和标点符号）均被赋予<big>唯一</big>的位模式*。

美国国家标准化协会（American National Standards Institute，ANSI）采用了**美国信息交换标准码**（American Standard Code for Information Interchange， ASCII）。这种代码使用长度为 7 的位模式来表示大小写英文字母、标点符号、数字 0～9 以及某些控制信息（如换行、回车和制表符）。

> 制表符（Tab）是一种在文本中用于控制光标移动的特殊字符。它通常用来在文本中实现对齐和分隔的功能。制表符在不同的环境中有不同的表现形式和用途，但其基本功能是一致的，即为文本格式化提供一种简便的方法。

后来，ASCII 码通过在每个 7 位位模式的最高端添加一个 0，扩展为 8 位位模式。这个技术不仅产生了代码（其中代码的每个位模式都能方便地存入典型的字节型的存储单元中），还提供了 128 个附加位模式（通过给附加的位赋予值 1），可以用于表示除英语字母和关联的标准福海之外的符号。

国际标准化组织（International Organization for Standardization）简称 ISO，开发了大量的 ASCII 扩展，每种扩展都是针对某一主要语种设计的。

Unicode 代码采用*唯一的 21 位的位模式表示每个符号*。但 Unicode 字符集与 **Unicode 转换格式 8 位**（Unicode transformation format 8-bit，UTF-8）编码标准结合在一起时，原来的 ASCII 字符仍然可以用 8 位来表示，而像**汉语**等语言所产生的数以千计的其他字符则可以用**16 位来表示**。除了可以表示世界上所有常用语言所需的字符，UTF-8 的 24 位或 32 位模式还可以表示比较鲜为人知的 Unicode 符号，为未来的扩展留出了充足的空间。

由一长串根据 ASCII 或 Unicode 编码的符号组成的文件常称为**文本文件**（text file）。文本文件只包含文本中各个字符的编码。

### 1.4.2  数值的表示

数字信息通常使用**<big>某种形式</big>的二进制记数法**来存储，而不用编码符号。之所以说“某种形式的二进制记数法”，是因为上面描述的简单二进制系统只是机器里应用的若干数值存储技术的基础。除了二进制记数法外还有**二进制补码**记数法通常用于存储整个数，它提供了一种便利的表示负数和正数的方法。为了表示带有分数部分的数，还要使用一种称为**浮点记数法**的技术。

### 1.4.3  图像的表示

图像的一种表示方法是：将图像解释为一组点，每个点称为一个**像素**（pixel，是 picture element 的简写）；然后，对每个像素的显示进行编码，整个图像就表示成了这些编码像素的集合，这个集合称为**位图**（bit map）。表示图像的常见的方法有两种：

* 一种是 RGB 编码，每个像素表示为 3 种颜色成分——红、绿、蓝，它们分别对应于光线的三原色。替代简单 RGB 编码的方法是，使用一个“亮度”成分和两个颜色成分。在这种方法中，“亮度”成分被称为像素亮度，基本上就是红、绿、蓝三种颜色成分的总和。其他两种成分，称为蓝色色度和红色**色度**，分别由像素中的像素亮度与蓝光或红光数量之间的差来决定。

  > 色度：色度（Chromaticity）是指光或颜色的质量，不包括其亮度的部分。它在颜色科学中用于描述颜色的质量特征，通常通过两个参数来定义：色调（Hue）和饱和度（Saturation）。
  >
  > > 色调：是颜色的基本性质，表示颜色的类型，如红色、绿色、蓝色等。它对应于颜色的光谱位置。
  > >
  > > 饱和度：是描述颜色的纯度或强度。高饱和度意味着颜色鲜艳，而低饱和度则意味着颜色更灰暗或褪色。

* 图像的另一种表示方法避免了像素缩放引发的问题，**将图像描述成了几何结构**（如直线和曲线）的集合，这些几何结构可以用**解析几何**技术来编码。

  > 解析几何：解析几何（Analytic Geometry），也称为坐标几何，是数学的一个分支，它通过使用坐标和代数方法来研究几何问题。解析几何将几何问题转化为代数问题，通过坐标系（通常是笛卡尔坐标系）将几何图形表示为代数方程，从而可以利用代数方法进行研究和解决。

### 1.4.4  声音的表示

为了便于计算机存储和操作，对音频信息进行编码的最常用方法是，按有规律的时间间隔对**声波的振幅**采样，并记录所得到的值序列。

## 1.5  二进制系统

### 1.5.1  二进制记数法

在二进制记数法中，每个数字的位置与一个量值相关联，这个量值是它右边两只的两倍。更精确地说，在二进制表示中，最右边的数字与量值 1（即 2<sup>0</sup>）相关联，其左边的下一个位置与量值 2（即 2<sup>1</sup>）相关联，下一个与量值 4（即 2<sup>2</sup>）相关联，再下一个与量值 8（即 2<sup>3</sup>）相关联，以此类推。

为了求得二进制表示所表示的值，我们可以采取和十进制相同的步骤：先将每个数字的值与与其相关联的位置的量值相乘，再计算各个乘积之和。

### 1.5.2  二进制加法

二进制数加法的规则与十进制加法相似，但只使用两个数字：0和1。以下是二进制加法的基本规则：

1. **0+0=0**

  * 无需进位。

2. **0+1=1**或**1+0=1**

  * 无需进位。

3. **1+1=10**：

  * 结果是 0，同时向左一位进 1。

4. **1+1+1=11**

  * 结果是1，同时向左一位进 1。

### 1.5.3  二进制中的分数

为了扩展二进制记数法，使其能够表示分数值，我们使用了**基数小数点**（radix point），其功能与十进制记数法中的小数点是相同的，即点左边的数字代表值的整数部分（整个部分），点右边的数字代表值的小数部分，其解释和其他二进制位类似，只是它们的位置被赋予了分数的量值。更确切地说，基数小数点右边第一位的量值是 1/2（即 2<sup>-1</sup>），下一位的量值是 1/4（即 2<sup>-2</sup>），再下一位是 1/8（即 2<sup>-3</sup>），以此类推。**注意，这仅仅是前面所述规则的延续：即每位说被赋予的量值是它右边大小的两倍**。利用这些赋予二进制位位置的量值，对包含小数点和不包含基数小数点的二进制表示进行解码的步骤基本是相同的。更精确地说，是把表示中的每个位的值与对应位的位置的量值想成。

另外，应用于十进制系统的加法技术同样适用于二进制系统，即两个有基数小数点的二进制表示相加时，只需要对齐基数小数点，然后按照加法步骤计算即可。

## 1.6  整数的存储

二进制补码记数法和余码记数法都是计算设备用于表示<big>整数</big>值的方法。虽然这些系统都是基于二进制系统的，但是它们增加了一些其他的特性，因而与计算机设计更加<big>兼容</big>。

> <big>整数</big>是没有小数部分的数，包括正数、负数和零。整数可以分为以下几种：
>
> * 正整数：大于零的整数，如 1、2、3.
> * 负整数：小于零的整数，如-1、-2、-3。
> * 零：既不是正整数也不是负整数。

> <big>兼容</big>：在计算机科学中，“兼容”指的是不同系统、设备、软件或组件能够一起工作或协同操作的能力。

### 1.6.1  二进制补码记数法

现今计算机中最流行的用于表示整数的系统是**二进制补码**（two‘s complement）记数法。这个系统采用固定数目的二进制位来表示系统中的每个值。现今设备中普遍采用二进制补码系统，其中每个值用一个 32 位的模式表示。

<big>注意</big>:在二进制补码系统中，位模式最左边的二进制位指明了所表示的值的符号。因此，最左边的位常称为**符号位**（sign bit）。在二进制补码系统中，**符号位 为 1 的模式表示负值，符号位为 0 的模式表示非负值**。

在二进制补码系统中，绝对值相同的正负值的表示模式之间有一种对应关系，从右向左读时，直到第一个二进制 1（包括那个 1），他们都是相同的。然后，以这个 1 为分界线，左面的位模式互为补码。要得到一个模式的**补码**（complement），需要将所有的二进制 0 转换为 1，并将所有的二进制 1 转换为0。

> 二进制补码是一种用于表示和处理计算机中**负整数**的方式。为了通俗地解释这个概念，可以把它想象成一种特殊的计数方法，专门用来表示正数和负数。在计算机中，所有的数据最终都是用 0 和 1 来表示的。当需要表示负数时，单纯的 0 和 1 组合就不够用了，因此补码就被引入来解决这个问题。
>
> 负数的补码是通过对正数求反（0变1，1变0），然后加1得到的。

### 1.6.2  二进制补码记数法中的加法

为了把二进制补码记数法表示的值相加，我们采用二进制加法中使用的算法，只是包括答案在内的所有位模式都是一样长的。这就意味着，在二进制补码系统中做加法时，*如果最后一个进位导致答案左边产生了附加位*，那么<big>这个附加位一定会被截断</big>。

### 1.6.3  溢出问题

**溢出**（overflow），指的是计算得出的值超出了可以表示的值的范围。使用二进制补码记数法时，两个正值相加或两个负值相加都可能会出现这种情况。

无论哪种情况，<big>检查答案的符号位就可以发现溢出的条件</big>。如果<big>两个正值相加的结果是负值</big>的模式，或者<big>两个负值相加的结果为正</big>，那么就发生了溢出问题。

### 1.6.4  余码记数法

**余码记数法**（excess notation）与二进制补码记数法相同，余码计数系统中的每个值也都是用相同长度的位模式表示的。

为了建立余码系统：

    1. 要选择要使用的模式长度。

    2. 根据二进制计数呈现的顺序写下那个长度的所有位模式。

    3. 二进制 1 作为其最高有效位的第一个模式，表示 0。

    4. 其后的模式分别用于表示 1，2，3……；其前的模式分别用于表示 -1，-2，-3……。

    <big>注意</big>：余码系统和二进制补码系统的一个区别就是*符号位相反*。

## 1.7  分数的存储

> **分数**：在数学中，分数是一种表示部分和整体关系的数值形式，用于描述一个整体被分成若干份后的一部分。分数主要由两个部分组成：
>
> 1. 分子：位于分数的上方，表示被取的部分数。
> 2. 分母：位于分数的下方，表示整体被分成的等分数。
>
> > 分数的类型：
> >
> > 1. 真分数：分子小于分母，表示小于一个整体。
> > 2. 假分数：分子大于或等于分母，表示一个或多个整体。
> > 3. 带分数：包含一个整体部分和一个真分数部分。
>
> > 分数的操作：
> >
> > 1. 加法和减法：要进行加法或减法，分数必须有相同的分母（成为同分母）。他国找到公分母，可以将异分母分数转换为同分母，然后进行运算。
> > 2. 乘法：直接将分子相乘，分母相乘。
> > 3. 除法：乘以第二个分数的倒数。
>
> 分数本质上体现了部门与整体之间的关系，是数学中非常基础和重要的概念。

> 实数：是数学中的一个基本概念，表示数轴上的所有点，涵盖了整数、有理数和无理数。实数主要用于表示连续的量，如长度、面积、体积、温度等。它是一个广泛应用于各种数学分析和科学计算的数系。
>
> > 实数的组成：
> >
> > 1. 整数：包括正整数、零和负整数。
> > 2. 有理数：表示两个整数之间的比值（分数），其中分母不为零。例如：1/2，-3/4，5。
> > 3. 无理数：不能表示为简单分数的数，通常是无限不循环小数。例如：π（圆周率）、√2。
>
> > 实数的性质：
> >
> > * 连续性：实数形成了数轴上的一个连续序列，这意味着在任何两个实数之间，总能找到另一个实数。
> >
> > * 完备性：实数集是完备的，即任何递增有界的实数序列都有一个极限值，并且这个极限值也是一个实数。
> >
> > * 代数性质：实数在加法和乘法下构成一个代数闭合的结构，这意味着实数集在这些运算下是封闭的。
> >
> >   > 代数是数学的一个分支，用符号（如字母）来表示数和运算关系。它研究如何通过方程和公式解决问题。代数可以用来表示未知数、建立数学模型以及解决实际问题。简单来说，代数是用符号和规则来处理数之间关系的工具。

## 1.8  数据与程序设计

> **数据**：在计算科学和信息技术领域，数据（Data）是指对现实世界中的事实、现象、概念或事物的符号化表示。这些符号可以是数字、文字、图像、音频、视频或其他形式，通常以一种结构化或非结构化的方式存储和处理。<br>简单地说，数据是计算机用来存储和传递信息的“原材料”。通过对数据的组织、分析和处理，我们可以提取有意义的信息或知识。
>
> * 数据的基本特点
>
>   1. 原始性：数据本身通常是未经处理的。例如，一个人的身高、体重，或者一组数字本身没有直接意义，只有通过加工分析后才有价值。
>   2. 多样性：数据可以以多种形式存在，如文字、数字、图表、声音和视频等。例如，文本（"Hello World"）、数字（123）、图像（JPEG文件）都可以是数据。
>   3. 存储和操作性：数据可以被存储在计算机的内存、硬盘或云存储中，并且可以被操作和处理，例如排序、搜索、统计分析等。
>   4. 独立性：数据本身独立于其使用方式。相同的数据可以被不同的程序或工具以不同的方式处理。
>
> * 数据的分类：
>
>   根据不同的属性，数据可以分为以下几类：
>
>   1. 按来源分类：
>
>      * 结构化数据：
>
>        指具有固定格式和结构的数据，如数据库表中的数据。
>
>      * 半结构化数据：
>
>        数据具有一定的组织结构，但不严格遵循固定的格式。
>
>      * 非结构化数据：
>
>        没有固定格式或结构的数据。
>
>   2. 按数据类型分类：
>
>      * 数字数据：包括整数、浮点数等。
>      * 文本数据：包括字母、单词、句子。
>      * 多媒体数据：包括图像、音频、视频。
>      * 时间/日期数据：表示时间或日期的信息。
>
>   3. 按动态性分类：
>
>      * 静态数据：不随时间变化的数据。
>      * 动态数据：随时间或操作而变化的数据。
>
>   4. 按用途分类：
>
>      * 原始数据：未经加工、分析的数据。
>      * 衍生数据：经过处理后的数据或信息。
>
> * 数据的意义：
>
>   数据本身可能没有直接的意义，但通过加工和分析，我们可以从数据中提取到有价值的信息。以下是数据的重要意义：
>
>   1. 支持决策：
>
>      数据通过分析可以帮助组织或个人做出科学的决策。
>
>   2. 描述现实：
>
>      数据反映了现实世界的某些特征。
>
>   3. 预测未来：
>
>      使用历史数据和统计模型可以预测未来的趋势。
>
>   4. 推动技术发展：
>
>      数据是人工智能和机器学习等技术的基础。大数据的蓬勃发展也推动了许多新技术的产生。
>
> * 数据的存储和管理：
>
>   在计算机中，数据需要以特定的方式进行存储和管理。以下是常见的存储方式：
>
>   1. 文件系统：数据存储在文件中，如文本文件、图片文件等。
>   2. 数据库：数据按照一定的规则存储在数据库中，方便查询和管理。
>   3. 云存储：数据存储在远程的云平台上，可以通过网络访问。
>   4. 内存和缓存：用于临时存储数据，方便程序快速读取和处理。
>
> * 数据在计算机科学中的角色：
>
>   **数据是计算机科学的核心**，因为它是所有计算和信息处理的基础：
>
>   1. 输入与输出：
>
>      数据是计算机程序的输入和输出。例如，用户输入一串数字，程序返回其总和。
>
>   2. 算法和逻辑的依据：
>
>      程序运行时需要处理数据，算法的核心目标是对数据进行有效处理。
>
>   3. 通信的基础：
>
>      数据是网络通信的内容。无论是发送电子邮件还是浏览网页，传递的都是数据。
>
> 数据是计算机科学和现代信息技术的基石。它以多种形式存在，并通过存储、处理、分析和传输在各个领域中发挥重要作用。无论是日常生活中的照片和视频，还是科学研究中的实验数据，数据都在推动世界的数字化发展。

人们喜欢在更高的抽象层次上推理计算问题，他们依赖计算机来处理最底层的细节问题。*程序设计语言（ programming language ）*是人类创造的一个计算机系统，通过它，人们能够使用更高层次的抽象来向计算机精确地表达算法。

> 程序设计语言：是指一种用于与计算机沟通的语言，它为程序员提供了一套符号和规则，用来编写程序、表达算法和实现计算逻辑。这些语言的主要目的是将人类的指令和逻辑转换为计算机可以理解和执行的形式。
>
> 计算机本质上只能理解“二进制代码”（0 和 1），但二进制代码难以阅读和编写，因此程序设计语言充当了人与计算机之间的桥梁，使得开发者能够以更高效、更直观的方式向计算机下达指令。
>
> 程序设计语言的基本概念：
>
> 1. 语法（Syntax ）:
>
>    每种程序设计语言都有特定的语法规则，规定了程序代码的书写方式。例如，Python中使用缩进来表示代码块，而C语言中使用大括号 `{}`。
>
> 2. 语义（Semantics）：
>
>    表示语言的实际意义。
>
> 语法告诉你如何写代码；而语义解释代码执行时的行为。例如，'a = 10' 的语法时赋值语句，而语义时将数值 `10`，存储到变量`a`中。
>
> 3. 编译或解释：
>
>    程序设计语言需要通过编译器或解释器将源代码转换为机器可以理解的二进制指令：
>
>    * 编译型语言：源代码在运行前通过编译器（如 C、C++）转换为可执行文件。
>    * 解释型语言：源代码运行时由解释器逐行解释并执行（如 Python、JavaScript）。
>
> 4. 跨平台性：
>
>    一些高级语言（如 Java）可以在不同的平台（如 Windows、Linux、macOS）上运行，而无需调整代码。
>
> 程序设计语言是一种工具，用来描述解决问题的步骤，并将这些逻辑指令传递给计算机执行。它是人与计算机之间的重要桥梁。不同的语言针对不同的需求和场景设计，适合各类任务，从高性能开发（如C、C++）到快速原型开发（如Python、Ruby），再到Web开发（如JavaScript）。初学者可以根据自己的兴趣和目标选择适合自己的语言，从而踏上程序设计的学习之旅。

事实上，有些人已经把程序设计或编码确定为现代读写能力中继阅读、写作继算术之后的又一个支柱了。

### 1.8.1  Python入门

现在 Python 是十大最常用的语言之一。

> “十大最常用的程序设计语言”通常是指在全球开发者社区中被广泛使用的编程语言。这些语言因其应用范围广、社区支持强大和功能强大而深受开发者欢迎。以下是目前根据**流行程度**和**实际应用**总结的十大最常用编程语言（基于2023年的趋势）：
>
> Python、JavaScript、Java、C、C++、C#、PHP、Swift、Go（Golang）、Kotlin。
>
> 这十大语言在全球开发者社区中得到了广泛的应用，它们几乎涵盖了所有的编程领域。从Web开发到数据科学、从嵌入式系统到移动应用开发，每种语言都有自己独特的适用场景。以下是对它们的简单归纳：
>
> - **Web开发**: JavaScript、Python、PHP、Java。
> - **移动开发**: Swift、Kotlin、Java。
> - **系统开发和高性能应用**: C、C++、Go。
> - **数据科学和人工智能**: Python。
> - **游戏开发**: C++、C#。

Python语言强调可读性，包括命令型程序设计范型、面向对象程序设计范型和函数式程序设计范型三大要素。

> 命令型程序设计范型：是计算机程序设计的一种基本范型，它强调通过一系列指令（命令）来告诉计算机如何逐步完成任务。在这种范型中，程序的核心是通过修改状态和控制流程来实现计算目标。通俗来说，命令型程序设计就像给计算机提供一份“操作步骤清单”，计算机按照这份清单逐步执行，知道完成任务。例如，做饭的步骤：1. 洗菜，2. 切菜，3. 加热锅， 4. 放油，等等——这就是一种命令型的思维方式。
>
> 命令型程序设计是一种基础的、传统的编程范型，强调通过变量的状态变化和指令的逐步执行来解决问题。它适用于解决流程明确**、**操作细节可控的任务，是许多现代编程语言的核心思想。随着项目复杂度的增长，命令型编程可能需要结合其他范型（如面向对象或函数式）来提升抽象能力和可维护性。对于初学者来说，命令型编程是最容易理解和学习的编程模式之一！

> 面向对象程序设计范型（Object-oriented Programming， OOP）：是一种以对象（Object）为核心组织程序的编程方法。它将程序中的数据和操作数据的代码紧密结合在一起，封装为对象，以模拟现实世界中的事物和行为。这种范型通过引入类（Class）和对象（Object）的概念来简化复杂系统的设计，强调代码的可复用性、可扩展性和可维护性。
>
> 通俗来说，面向对象就像“搭积木”一样，把程序设计看作是对一个个“对象”的定义和操作。每个对象就像一个小型独立模块，它既有自己的“属性”（描述对象的状态），又有自己的“行为”（定义对象的功能）。
>
> 对于初学者来说，理解面向对象的核心概念（类、对象、封装、继承、多态）是学习现代编程语言的重要基础。

> 函数式程序设计范型（Functional Programming Paradigm）：是一种以函数为核心构建程序的编程范型。它强调通过函数的组合和不可变数据来解决问题，而不是依赖可变状态和逐步执行的命令。这种范型借鉴了数学中“函数”的概念，程序由一系列相互独立的函数组成，每个函数接收输入并返回输出，而不影响全局状态。
>
> 通俗地说，函数是编程就像数学公式一样，每个函数独立运算，输入相同输出一定相同（无副作用），而且函数之间可以像搭积木那样灵活组合，用来解决复杂问题。
>
> 函数式程序设计范型（FP）是一种以函数为核心、强调不可变性和无副作用的编程风格。它通过高阶函数、递归和函数组合实现复杂功能，适合并发计算和复杂算法的实现。尽管学习曲线较陡，但函数式编程提供了更高的代码抽象能力，并提高了代码的可维护性和可靠性。对于初学者，推荐从支持函数式编程的语言（如 Python 或 JavaScript）开始，逐步理解纯函数、高阶函数和不可变数据等概念，再深入学习 Haskell 或 Scala 等纯函数式语言。

Python 语言一直在不断演变，本书的所有示例都将使用 Python 3 这个版本。Python 是一种*解释型语言（interpreted language）*，这意味着初学者可以将 Python 指令键入交互提示符中，或者将 Python 指令存储在一个（称为“脚本”的）纯文本文件里以后运行。

> 解释型语言（Interpreted Language）是指程序代码在执行时不需要事先编译成机器码，而是通过一个解释器（Interpreter）逐行翻译并立即执行的编程语言。简单来说，解释型语言的程序在运行时会由解释器“边读边翻译”，直接将代码转化为机器能够理解的指令并执行。

> 交互提示符：（Interactive Prompt）是指编程语言或软件提供的一种即时交互式输入输出环境。在交互提示符下，用户可以输入命令或代码，程序会立即执行并返回结果。这种环境通常用于快速测试代码、执行简单命令或进行调试。
>
> 提示符 `>>>` 表示你已经进入了 Python 的交互式环境，你可以在这里输入 Python 代码。

> 脚本：是一种用来完成特定任务的程序，通常用于自动化操作。它们是由编程语言编写的文件，并需要特定的解释器或运行环境才能执行。脚本一般是解释型语言编写的代码，比如 Python、JavaScript、Bash 等。

> 纯文本文件：是指只包含文本字符的文件，没有复杂的格式或嵌入的数据（如图片、字体样式等）。纯文本文件的每一行通常是 ASCII 或 Unicode 字符。

### 1.8.2  你好，Python

首先，`print ()`是一个内置函数，是 Python 脚本用来产生*输出*的一个预定义操作，输出指的是一个能够让用户看到的程序结果。

其次，Python 可以使用单引号`''`来表示文本串。

> 在 Mac 电脑中的“终端” App 中，执行`pyhton3`的指令，就可以将“终端” App 转换成交互解释器，在交互提示符`>>>`的 引导下，运行 Python 程序或脚本。
>
> ```python
> >>> print('Hello, World!')
> Hello, World!
> >>>
> ```

### 1.8.3  变量

Python 允许用户给值命名以备日后使用，这是构造简洁、易懂的脚本时的一个重要抽象。这些命名的存储位置被称为**变量（variable）**，类似于代数课程中的数学变量。下面来看一下略有增强的“Hello World”版本：

```python
message = 'Hello, World!'
print (message)
```

在这个脚本中，第一行是**赋值语句（assignment statement）**。这个赋值语句应该读作：“变量 message 被赋予字符串值‘Hello, World!’”。通常，**赋值语句由等号左边的变量名和等号右边的值构成**。

Python 是一种***动态类型（dynamically typed）***的语言，这意味着我们不需要在建立脚本时提前创建好一个名为`message`的变量，或者确定什么类型的值应该存储在`message`中，而只需要在这个脚本中说明我们的文本串被赋给`message`，接着在后面的`print`语句中引用这个变量`message`就行了。

> 动态类型语言是指在运行时决定变量类型的编程语言，开发者不需要显式声明变量的类型，变量类型可以根据赋值动态变化。这种语言以其灵活性和开发效率著称，非常适合快速开发、原型设计和学习编程。
>
> 尽管动态类型语言在性能和类型安全性方面可能不如静态类型语言，但其简洁、易用的特点让它在现代开发中非常流行。像 Python和 JavaScript是动态类型语言的典型代表，也是目前主流编程开发中不可或缺的工具！

变量的命名很大程度上取决于 Python 用户。Python 的简单规则是：变量名必须以字母开头，可以包含任意数量的字母、数字和下划线字符（_）。Python 的变量名是***区分大小写的（case-sensitive）***即有大小写问题。名为`size`的变量，与名为`Size`或`SIZE`的变量是截然不同的。有一部分**关键字（keyword）**，即为 Python 中的一些特殊含义保留的名字，不能用作变量名。在 Python 内置帮助系统里能查看这个关键字清单。

``````python
help('keywords')
``````



变量可用于存储 Python 能表示的所有类型的值。

```python
my_integer = 5
my_floating_point = 26.2
my_Boolean = True
my_string = ‘characters’
```

Python 使用 0x 前缀来表示十六进制值，如

```python
my_integer = 0xFF
print(my_integer)
```

Unicode字符包含着无所不在的 ASCII 子集中说没有的字符，但文本编辑器支持 Unicode 字符时，可以直接在字符串里包含 Unicode 字符：

```python
print('\u00A31000')  # Prints ￡1000, one thousand British
                     # Pounds sterling
```

***#号表示注释（comment）的开始，这是人类可读的Python代码符号，在计算机执行时会被忽略***。脚本顶端的高级描述向人类阅读者介绍脚本的总体目标和脚本中使用的方法。#号右边一直到行尾的所有字符都会被Python忽略。程序的解释能帮助人们理解程序的功能和用途。

### 1.8.4  运算符和表达式

Python的内置运算符允许人们以各种熟悉的方式对数值进行操作和组合。

```python
print(3 + 4) 		# Prints "7", which is 3 plus 4.
print(5 - 6) 		# Prints "-1", which is 5 minus 6.
print(7 * 8) 		# Prints "56", which is 7 times 8.
print(45 / 4)		# Prints "11.25", which is 45 divided by 4.
print(2 ** 10)	# Prints "1024", which is 2 to the 10 power
```

当一个运算（如45除以4）产生的是非整数结果（如11.25）时，Python会将表示类型隐式转换为浮点表示。如果希望结果是整数，就要使用另外一组运算符：

```python
print(45 // 4)		# Prints "11", which is 45 integer divided by 4.
print(45 % 4)			# Prints "1", because 4 * 11 + 1 = 45 
```

双斜线（ // ）表示*整除（integer floor division）运算符*，百分号（ % ）表示*取模（modulus）或取余运算符*。

还可以用一些直观的方式对字符串值进行组合和操作。

```python
s = 'hello' + 'world'
t = s * 4
print(t)			# Prints "helloworldhelloworldhelloworldhelloworld"
```

其中，加运算符（ + ）用于拼接（concatenate）字符串值，乘运算符（ * ）用于复制（replicate）字符串值。

### 1.8.5  货币转换

给定一定数量的美元，脚本会对其进行货币转换，转换成四种其他货币。

```python
# A converter for intenational currency exchange.
USD_to_GBP = 0.76			# Today's rate, US dollars to British Pounds
USD_to_EUR = 0.86			# Today's rate, US dollars to Euros
USD_to_JPY = 114.08		# Today's rate, US dollars to Japanese Yen
USD_to_INR = 63.64		# Today's rate, US dollars to Indian Rupees

GBP_sign = '\u00A3'	# Unicode values for non-ASCII currency symbols.
EUR_sign = '\u20AC'
JPY_sign = '\u00A5'
INR_sign = '\u20B9'

dollars = 1000		# The number of dollars to convert
pounds = dollars * USD_to_GBP		# Conversion calculations
euros = dollars * USD_to_EUR
yen = dollars * USD_to_JPY
rupees = dollars * USD_to_INR

print('Today, $'+ str(dollars))		# Printing the results
print('converts to ' + GBP_sign + str(pounds))
print('converts to ' + EUR_sign + str(euros))
print('converts to ' + JPY_sign + str(yen))
print('converts to ' + INR_sign + str(rupees))
```

执行该脚本时，输出如下：

```python
Today, $1000
converts to £760.0
converts to €860.0
converts to ¥114080.0
converts to ₹63640.0
```

### 1.8.6  调试

初学者在编写软件时，需要花费大量的时间努力查找代码中**bug**或者错误。定位这些bug并修正它们因此称为**调试（debugging）**。软件中的bug可分为3大类：

* **语法错误（syntax error）**：键入时产生的符号错误。
* **语义错误（semantic error）**：程序含义的错误。
* **运行时错误（runtime error）**：程序运行时发生的错误。

## 1.9  数据压缩

> **数据压缩（data compression）**：是一种通过减少数据存储或传输时的大小来节省存储空间或提高传输效率的技术。它是计算机科学中的一个重要领域，广泛应用于文件存储、数据传输、多媒体处理等场景。
>
> 压缩的核心目的是以更小的尺寸表示数据，同时尽量保留原始数据的完整性或可接受的质量。

### 1.9.1  通用的数据压缩技术

数据压缩方案有两类：

1. 一类是**无损的（lossless）**：无损方案在压缩过程中是不丢失信息的。
   * 对于被压缩数据由一长串相同的值组成的情况，流行使用称为**行程长度编码（run-length encoding）**的压缩技术。它的过程是，将一组相同的数据成分替换成一个编码，指出重复的成分以及在序列中出现的次数。
   * **频率相关编码（frequency-dependent encoding）**，在这个系统里，用于表示数据项的位模式的长度与这个项的使用频率是反相关的。
2. 一类是**有损的（lossy）**：有损方案在压缩过程中会丢失信息。

在某些情况下，要压缩的数据流有数据单元组成，每个数据单元与其前面一个的差别很小，使用**相对编码（relative encoding）**——也称为**差分编码（differential encoding）**技术是很有用的。这些技术记录的是两个连续数据单元之间的区别，而不是整个单元。也就是说，每个单元是根据其与前一个单元的关系被编码的。<br>相对编码（差分编码）可以用无损形式实现，也可以用有损形式实现，具体取决于两个连续数据单元之间的差别是精确编码还是近似编码的。

**字典编码（dictionary encoding）**用于通过用短编码代替数据中重复出现的模式或片段，来减少存储或传输数据的大小。在字典编码中，"*字典*" 是一个数据结构，它存储了数据中重复出现的模式或片段（例如字符串），并为这些模式分配唯一的标识符（通常是数字或较短的编码）。在字典编码中，"字典" 的作用是作为一个参考或查找表，让数据的实际内容可以用更短的形式（编码）来表示。

**字处理程序**可以使用字典编码来压缩文本文档，因为这些字处理程序中已经包含了用于拼写检查的字典，而这些字典都是出色的压缩字典。

> **字处理程序**（Word Processor）是一种用于创建、编辑、格式化和打印文本文档的软件工具。它是现代计算机常用的应用程序之一，广泛用于写作、办公和文档处理任务。相比传统的文字编辑工具，字处理程序提供了更丰富的功能，包括字体样式设置、段落排版、插入图片和表格、拼写检查等。

虽然无损数据压缩会减少存储或传输的位数，但允许完整重构原始数据。有损数据压缩可以大大减少存储和传输的位数，但代价是智能重构原始数据的近似值。

### 1.9.2  图像压缩

**GIF，图像交换格式**（是 graphic interchange format /ˈɡræf.ɪ.kəl ˌɪn.təˈtʃeɪndʒ ˈfɔː.mæt/ 的缩写）是一个字典编码系统。它处理压缩问题的方法是，将赋予一个像素的颜色数量减少到只有 256 个。每种颜色的“红--绿--蓝”组合都用 3 个字节编码，这 256 个编码被存储在一个称为调色板的表格（字典）里。图像中的每个像素都可以用一个字节表示，该字节的值指明了这个像素的颜色是由 256 个调色板（字典）条目中的哪一个表示的。

通过使用 LZW 技术将这个简单的字典系统扩展为自适应字典系统，GIF 可以进一步压缩。尤其是，编码过程中遇到的像素模式可以被加到字典中，使这些模式在将来出现时可以被更加高效地编码。因此，最终的字典是由调色板和一组像素模式构成的。

GIF 调色板（字典）中的每一个颜色通常会被赋予值“透明”，这意味着背景色可以透过每个被赋予该“颜色”的区域展现出来。这个选择与 GIF 系统的相对简便性相结合，使得 GIF 称为简单动画应用的合理选择。另外，它只能够对 256 种颜色编码，这使得它不适合精度要求较高的应用，如摄影领域。

另外一种流行的图像雅俗系统是**JPEG**（读作“JAY-peg”）。它是 ISO 中的**联合图像专家组**（Joint Photographic Experts Group，标准因此得名） 研制开发标准。JPEG 已经被证明是压缩彩色照片的一种有效标准，并被广泛用于摄影业，这一点可由大多数数码相机都将 JPEG 作为默认的压缩技术这一事实来印证。

JPEG 标准实际上包含多种图像压缩方法，每种都有它自己的目标。在需要尽可能精确的情况下，JPEG 可提供无损模式。不过，相对于 JPEG 的其他模式，JPEG 的无损模式不会产生高级别的压缩。此外，JPEG 的其它模式已被证明很成功，这意味着人们很少使用 JPEG 的无损模式。<br>相反称为 JPEG 基线标准（也称为 JPEG 的有损顺序模式）的 JPEG 模式已经称为许多应用的选择标准。

使用 JPEG 基线标准的图像压缩需要几个步骤，其中有一些是利用人眼的局限性设计的。尤其是，相对于颜色的变化，人眼对**亮度**的变化更为敏感。

> 在计算机科学和数字图像处理领域，**亮度**（Luminance / ˈluːmɪnəns/）是一个用于描述图像或视频中某个像素的明亮程度的概念。亮度是颜色的一个组成部分，反映了人眼对光的明亮程度的感知。

因此，我们首先看一幅根据亮度成分和**色度**成分进行编码的图像。第一步是在一个 2x2 的像素方块中求色度的平均值。这能将色度信息的大小减少为 1/4，同时保留所有的原始亮度信息。结果是，在没有明显损失图像质量的情况下获得了很高的压缩率。

> **色度**：描述了图像中颜色的质量，即色调和饱和度。

下一步是将图像划分成多个 8x8 的像素块，并将每个块中的信息压缩为一个单元。这可以通过一种称为**离散余弦转换**的数学技术来实现，这里我们不需要关心这个转换的细节。重要的一点是，这种转换能将原始的 8x8 块变成另一种块，而不是实际的像素值。在这个新块里，那些低于预定**阈值**的值将被替换成 0，反映的事实是这些值表示的变化非常小，人眼无法觉察。

> **离散余弦变换**（Discrete Cosine Transform, DCT）是一种将信号或图像数据从空间域转换到频率域的变换技术。DCT 的作用是将原始信号表示为多个余弦函数的加权和。这种转换在压缩技术中非常有用，因为信号的重要信息常集中在低频区域，而高频区域则可被压缩。
>
> DCT 在图像和视频压缩（如 JPEG 和 MPEG 格式）中广泛使用，因为它能有效地将图像数据的能量集中在少数几个频率分量上，从而便于压缩。

> 在计算机科学、数字图像处理、信号处理以及许多其他领域中，**阈值**（Threshold /ˈθrɛʃ(h)əʊld/）是一个用于区分或分类数据的界限值。通过将数据与阈值进行比较，可以决定数据的某种状态、类别或行为。这一概念在各个领域有着广泛的应用，尤其是在图像处理和二值化操作中。

这时候，可以应用更传统的行程长度编码、相对编码以及变长编码技术进行进一步的压缩。总之，JPEG 基线标准一边能在没有明显质量损失的情况下，将彩色图像压缩至少 10 倍，有时甚至能压缩 30 倍。

另一个与图像相关的数据压缩系统是 **TIFF**（tagged image file format 的缩写）即**标记图像文件格式**。不过，TIFF 最普遍的应用不是数据压缩，而是存储照片及其相关的信息（如日期、时间以及相机设置）的一个标准格式。

### 1.9.3  音频和视频压缩

最常用的音频及视频的编码和压缩标准是由 ISO 领导的**运动图像专家组**（Motion Picture Experts Group，MPEG）开发的，因此这些标准称为 MPEG。

MPEG 包含许多不同应用的各种标准。一般来说，视频压缩技术是以图片序列构建成的视频为基础的，与将运动图像录制到胶片上的方式基本相同。为了压缩这样的序列，只有一些称为 I 帧（I-frame）的图片是被整个编码的。两个 I 帧之间的图片是采用相对编码技术进行编码的。也就是说，不对整个图片进行编码，只记录与前一个图片的差别。I 帧本身经常使用类似于 JPEG 的技术进行压缩。

最著名的音频压缩系统是 **MP3**，它是在 MPEG 标准中开发出来的。事实上，MP3 是 MPEG layer 3 的缩写。与其他压缩技术相比，MP3 利用人耳的特性，删除了人儿觉察不到的细节：

* **时域掩蔽（temporal masking）**，指的是在一次巨大声响后，短时间内人耳觉察不到本可以听见的轻柔声音。
* **频域掩蔽（frequency masking）**，指的是某一频率的声音可能掩盖相近频率的轻柔声音。

利用这些特性，可以通过 MP3 获得显著的音频压缩，而且保持接近 CD 的音质。

音频和视频压缩的目的不一定是节省存储空间，真正重要的是获得编码，使信息能够通过现在的通信系统得到及时的传输。

## 1.10  通信差错

> **通信差错（Communication Error）**是指在数据或信息的发送端和接收端之间传输过程中，由于各种原因导致的数据不完整、扭曲、丢失或被误解的现象。

### 1.10.1  奇偶校验位

**奇偶校验位**（Parity Bit）是一种简单的差错检测机制，用于检测数据传输过程中是否发生了单比特错误。奇偶校验通过在数据末尾添加一个额外的二进制位来实现，该位用于确保数据中 1 的个数满足特定的奇偶性要求。奇偶校验通常用于串行通信和存储系统中，以提高数据传输的可靠性。

奇偶位校验有两种两种类型：**奇校验**和**偶校验**。

1. **奇校验（Odd Parity)**
   * 定义：奇校验位使得整个数据（包括校验位）中的 1 的个数位奇数。
   * 示例：假设要传输的数据是`1010`，1 的个数是 2（偶数）。为了使 1 的个数为奇数，添加一个奇数校验位`1`，得到`10101`。
2. **偶校验（Even Parity）**
   * 定义：偶校验位使得整个数据（包括校验位）中1 的个数为偶数。
   * 示例：假设要传输的数据是`1010`，1 的个数是 2（偶数）。为了保持 1 的个数为偶数，添加一个偶校验位`0`，得到`10100`。

奇偶校验位的计算方法非常简单，主要步骤如下：

1. 计算原始数据中 1 的个数。
2. 根据奇偶校验规则确定校验位：
   * 对于奇校验，如果 1 的个数为偶数，则校验位为 1；如果 1 的个数为奇数，则校验位为 0。
   * 对于偶校验，如果 1 的个数为偶数，则校验位为 0；如果 1 的个数为奇数，则校验位为 1。

示例如下：

假设我们有一串 4 位的数据`1101`，我们将为其添加奇校验位和偶校验位。

奇校验位

* 数据：`1101`
* 1的个数：3（奇数）
* 奇校验位：0（因为 1 的个数本来就是奇数）
* 结果：`11010`

偶校验位

* 数据：`1101`
* 1 的个数：3（奇数）
* 偶校验位：1（为了使 1 的个数变为偶数）
* 结果：`11011`

奇偶校验检测过程

* 发送方
  1. 计算数据中 1 的个数
  2. 根据奇偶校验规则添加适当的校验位。
  3. 发送包含校验位的数据。
* 接收方
  1. 接收包含校验位的数据。
  2. 计算数据中 1 的个数，包括校验位。
  3. 检查 1 的个数是否符合奇偶校验规则。
     * 如果符合，说明数据没有检测到单比特错误。
     * 如果不符合，说明数据在传输过程中发生了错误。

下面是一个使用 Python 实现奇偶校验的简单示例：

```python
def calculate_parity_bit(data, parity_type='even'):
		"""
		计算奇偶校验
		:param data: 输入数据（字符串形式，如'1101'）
		:param parity_type: 校验类型（'even' 或'odd'）
		:return: 包含校验位的数据
		"""
		num_of_ones = data.count('1')
		
		if parity_type == 'even':
				parity_bit = '0' if num_of_ones % 2 == 0 else '1'
		elif parity_type == 'odd':
				parity_bit = '1' if num_of_ones % 2 == 0 else '0'
		else:
				raise valueError("INvalid parity type. Use 'even' or 'odd'.")
				
		return data + parity_bit
# 示例
date = '1101'
even_parity_data = calculate_parity_bit(data, 'even')
odd_parity_data = calculate_parity_bit(data, 'odd')

print(f"原始数据：{data}") 
print(f"偶校验数据：{even_parity_data}") # 输出：11011
print(f"奇校验数据：{odd_parity_data}") # 输出：11010
```

总结

奇偶校验位是用于检测数据传输过程中单比特错误的简单而有效的机制。通过在数据末尾添加二进制位，奇偶校验可以确保数据中1的个数满足特定的奇偶性要求。虽然奇偶校验智能检测单比特错误，但其实现简单、开销低，广泛应用于串行通信、内存和存储系统以及网络通信中。

### 1.10.2  纠错码

**纠错码**（**Error-Correcting Code**，简称ECC），是一种编码技术，用于在数据传输或存储过程中纠正错误。与简单的如奇偶校验码那样的简单的差错校验码不同，纠错码不仅能检测数据中的错误，还能通过编码附加的冗余信息来自动修正某些类型的错误，从而提高数据的可靠性和完整性。

纠错码广泛应用于通信系统（如无线通信、卫星通信）、存储设备（如硬盘、SSD、内存）以及其他对数据完整性要求较高的领域。

#### 为什么需要纠错码？

在数据通信或存储中，数据可能会受到各种干扰或故障的影响，导致比特值（0或1）发生错误。例如：

* 信道噪音：在无线通信中，噪音可能使某些比特变得不可读。
* 硬件故障：存储设备可能因硬件老化导致数据损坏。
* 环境影响：如天气、物理震动、放射性干扰等。

为了确保数据的可靠性，纠错码通过添加冗余位来检测并纠正这些错误。

#### 纠错码的两种主要功能

1. 错误检测
   * 发现传输或存储过程中是否发生了错误。
   * 示例：奇偶校验码。
2. 错误纠正：
   * 不仅发现错误，还能通过附加的冗余信息定位错误并修正。
   * 示例：汉明码。

#### 纠错码的基本原理

纠错码通过在原始数据中添加**冗余信息**（**Redundant Information**）来提高数据的可靠性。这些冗余信息由特定的数学规则生成，接收方可以利用这些规则检查数据的完整性，并在必要时修正错误。

> **冗余信息**（Redundant Information）是指在原始数据之外，额外添加的数据，用于辅助检测和纠正传输或存储过程中可能发生的错误。冗余信息不直接包含原始数据的主要内容，但通过特定的编码规则与原始数据相关联，可以帮助检测和修复错误。*奇偶校验码*和*汉明码*的校验码都属于冗余信息。
>
> 冗余的作用：
>
> * 冗余信息就像数据的“备份提示”，如果某个比特出错，可以利用冗余信息来判断原始数据的正确值。
> * 冗余的数量决定了纠错能力的强弱：越多的冗余位可以纠正更多的错误，但会增加数据传输的开销。

#### 汉明码纠错码

**汉明码**（Hamming Code）是一种经典的**差错检测与纠错码**，由美国数学家理查德·汉明（Richard Hamming）于 1950 年提出。汉明码是一种线性块码，通过在原始数据中添加冗余位（校验位）来检测和纠正单比特错误。

##### 汉明码的主要特点

1. 纠错能力：汉明码可以检测最多两位错误，并可以纠正一位错误。
2. 冗余位的数量：冗余位的数量取决于数据位的长度。
3. 线性块码：汉明码的编码过程基于矩阵运算，可用代数方法表示。
4. 效率较高：汉明码在提供单比特纠错能力的同时，仅需少量冗余位，是早期计算机存储和通信中常用的纠错方法。

##### 汉明码的工作原理

1. 校验位的插入

   在汉明码中，校验位插入到特定的位置，这些位置是（2<sup>n</sup>，即1、2、4、8 等）。

   其余位置用来存放数据位。

   例子：如果 4 位数据（D_1, D_2, D_3, D_4），我们需要添加 3 个校验位（P_1、P_2、P_4），总共 7位。数据和校验位的排列：（P_1, P_2, D_1, D_2, P_4, D_2, D_3, D_4）。

2. 校验位的计算

   每个校验位负责检测特定位置上的比特是否满足奇偶性规则。校验位的计算以奇偶性校验为基础，具体规则如下：

   * （P_1）：校验位负责检查所在位置的编号中第 1 位为 1 的数据位，例如位置 1、3、5、7。
   * （P_2）：校验位负责检查所有位置的编号中第 2 位为 1 的数据位，例如位置 2、3、6、7。
   * （P_4）：校验位负责检查所有位置的编号中第 3 位为 1 的数据位，例如位置 4、5、6、7。

3. 接收端的工作

   接收方也根据奇偶校验规则重新计算校验位，并与接收到的数据进行比较。

   如果某些校验位不一致，接收方可以确定发生错误的位置，并纠正错误。

##### 示例：汉明码（7,4）的编码和纠错

编码过程：假设我们要传输 4 位数据（1011）：

1. 插入校验位：

   带校验位的初始数据：（P_1, P_2, 1, p_4, 0, 1, 1）。

2. 计算校验位：

   * （P_1）：检查第 1 位、3 位、5 位、7 位：（1, 1, 0, 1），奇数个 1，因此（P_1 =1）。
   * （P_2）: 检查第 2 位，3 位，6 位 、7 位：( 1, 1, 1, 1 )，奇数个 1，因此 ( P_2 = 1 )。
   * ( P_4 )：检查第 4 位、5 位、6 位、7 位：( 0, 0, 1, 1 )，偶数个 1，因此 ( P_4 = 0 )。

3. 最终编码数据：

   编码后的数据是：（1010011）。

纠错过程：

假设接收到的数据为（1110011），与原始编码数据（1010011）不同，说明发生了错误。

1. 重新计算校验位：

   - ( P_1 )：检查第 1 位、3 位、5 位、7 位：( 1, 1, 0, 1 )，奇数个 1，因此 ( P_1 = 1 )。
   - ( P_2 )：检查第 2 位、3 位、6 位、7 位：( 1, 1, 1, 1 )，奇数个 1，因此 ( P_2 = 1 )。
   - ( P_4 )：检查第 4 位、5 位、6 位、7 位：( 0, 0, 1, 1 )，偶数个 1，因此 ( P_4 = 0 )。

2. 分析错误位置：

   根据校验位的结果，发现( P_4 P_2 P_1 = 011 )，即错误位是第 3 位。

3. 纠正错误：

   将第 3 位从(1)改为(0)，得到正确数据(1010011)。

4. 提取原始数据：

   去掉校验位 ( P_1, P_2, P_4 )，得到原始数据 ( 1011 )。

汉明码是一种经典的纠错码，通过添加少量的冗余位，可以高效地检测和纠正单比特错误。它的计算简单，开销低，是早期通信和存储系统中的重要技术。尽管其纠错能力有限，但在许多场景中仍然具有重要的实用价值，尤其是在对资源要求较低的系统中。