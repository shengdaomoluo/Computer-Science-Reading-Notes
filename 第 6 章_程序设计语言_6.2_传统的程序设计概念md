## 6.2  传统的程序设计概念

高级程序设计语言包括从简单（常量、文字和变量）到中等复杂（语句、表达式和控制结构）到深奥（过程单元、模块和库）的多种抽象。

```mermaid
graph BT
C[简单<br>常量、文字、变量] --> B[**中等复杂**<br>语句、表达式、控制结构] --> A[**<big>深奥</big>**<br>过程单元、模块、库]
```

### 脚本语言

**脚本语言**（scripting language）是命令式程序设计语言的一个子集，它通常用来执行管理任务，而不是开发复杂的程序。对这种任务的表述称为**脚本**（script），该语言因此得名“脚本语言”。

-----

通常，程序由一组语句组成，这些语句一般可以分成 3 类：声明语句、命令语句和注释。
**声明语句**（declarative statement/dɪˌklær.ə.tɪv ˈsteɪt.mənt/）定义程序中后面要用的自定义术语，如用来引用数据项的名字。
**命令语句**（imperative statement）描述底层算法中的步骤。
**注释**（comment）通过比较人性化的形式来解释程序中的一些难懂的特性，从而提高程序的可读性。

### 6.2.1  变量和数据类型

**变量**（variable/ˈveə.ri.ə.bəl/）：是指改变这个位置里的值，与该名字相关联的值就会随着程序的执行而改变。
**数据类型**（data type）：是指数据项的编码方式以及可以在该数据上执行的操作。<br>主要包括以下几种类型：

* **整型**（integer）是指由整数组成的数字数据，可能以二进制补码记数法存储。可以进行的运算包括传统的算术运算和相对大小的比较。
* **浮点型**（float）又称为**实型**（real）是指可能包含整数以外的值的数字数据，可能以浮点记数法存储。可以进行的操作与整型数据上进行的运算一致。
  在 C、C++、Java 和 C#语言中，变量`WeightLimit`来指向一个主存储器区域，这个区域包含一个用二进制补码记数法编码的数值，可以通过在程序中插入下面的语句来声明这个意图：

```java
int WeightLimit;
```

这条语句的意思是：“名字`WeightLimit`将要在后面的程序中用到，它指向一个存储器区域，该存储器区域中包含一个用二进制补码记数法存储的值。”同一类型的多个变量通常的可以在同一个声明语句中声明。例如，语句

```java
int Height, Width;
```

声明两个整型变量`Height`和`Width`。还可以为变量赋一个初始值。因此，语句

```java
int WeightLimit = 100;
```

不仅声明了一个整型变量`WeightLimit`，还会为这个变量赋一个初始值`100`。

* **布尔型**（Boolean）是指值仅为真或假的数据项。在布尔型数据上执行的操作包括判断当前值是真还是假。<br>例如，如果将变量`LimitExceeded`声明为布尔型，那么下面这种形式的语句就是合理的：

```c++
if (LimitExceeded) then (...) else (...)
```

**基本数据类型**（primitive data type）是指作为原语包括在程序设计语言中的数据类型（如用于整数的`int`和用于字符的`char`）。

### 6.2.2  数据结构

**数据结构**（data structure）是数据在概念上的形态或排列。例如：
**数组**（array /əˈreɪ/）：即由相同类型的元素组成的块。<br>数组一旦声明，就能够通过它的名字在程序中的其他地方引用它，或者通过一个称作**索引**（index/ˈɪndeks/）的整数值来标识这些数组的组成元素，索引指定所需的行和列等信息。

### 6.2.3  常量和字面量

**表达式**（expression）：是指通过算术运算符和逻辑运算符组合的变量、文字和常量值。

在大多数程序设计语言中，由文本组成的字面量都是用单引号或双引号来划界的，以便与其他程序部分相区分。<br>例如，语句`LastName = "Smith"`可以用来把文本“Smith”赋给变量'Lastname'，而语句`Lastname = Smith`则用来把变量`Smith`的值赋值给变量`Lastname`。

**常量**（constant /ˈkɒn.stənt/） 给特定的不会改变的值分配一个描述性的名字，这个名字称为常量。

### 6.2.4  赋值语句

**赋值语句**（assignment statement）是指请求将一个表达式的值赋给一个变量的命令语言。<br>或者更准确地说，将一个表达式的值存储在该变量标识的存储区域中。<br>这种语句采用的语法形式通常为：一个变量，后跟一个代表赋值运算的符号，最后跟一个赋值表达式。<br>这种语句的语义就是对表达式进行求值，把得到的结果存储为变量的值。例如：

```C
z = x + y;
```

是将 x 和 y 相加的和赋给变量 z。行尾的分号在许多命令式语言中用来分隔语句。
**重载**（overloading）是指同一个运算符的多种用法。

### 6.2.5  控制语句

**控制语句**（control statement）是指可以改变程序的执行顺序的命令语句。

### 6.2.6  注释

**注释**（comment/ˈkɒm.ent/）在程序中起到解释性作用的语句。