## *10.5  处理全局光照

为了实现全局光照模型有两套解决方案——光线跟踪和辐射度，他们都克服了传统渲染流水线固有的局部光照模型的局限性。

### 10.5.1  光线跟踪

**光线跟踪**（ray tracing）：通过跟踪单条光线，采用点对点的方法，本质上是沿着光线进行反方向的跟踪以找到它的光源的过程。这个过程的一般步骤是：

1. 选择要渲染的像素，确定这个像素和经过投影中心的直线。
2. 跟踪沿着这条直线射入图像窗口的光线。这个跟踪过程沿着此直线进入场景，直至它接触到一个物体。
   * 如果这个物体是光源，则终止光线跟踪过程，将像素渲染为光源上的一个点；
   * 否则，计算物体表面的特征，以此确定入射光的方向，该光线被反射产生了该过程正在追溯的光线。
     * 继续跟踪入射光线反向找到它的光源；
     * 在这个点上可能还会有另一条需要识别和跟踪的光线。

光线跟踪的一个缺点是它仅跟踪镜面光。因此，通过这种方法渲染的所有物体往往都具有有光泽的的外观。为了抵消这个影响，可以应用光线跟踪的一个变种——**分布式光线跟踪**（distributed ray tracing）。<br>分布式光线跟踪与光线跟踪的区别在于，分布式光线跟踪不是从反射点向后跟踪单条光线，而是同时跟踪从这个点出发的多条光线，每条光线的延伸方向略有不同。

当涉及透明物体时，每当向后跟踪光线至表面时，必须考虑两种效果，一种是反射，一种是折射。<br>在反射和折射两种效果的情况下，跟踪原始光的任务分成了两个：向后跟踪反射光和向后跟踪折射光。

光线跟踪通常是递归实现的，每次递归都被要求跟踪光线和它的源头。

1. 第一次递归可能跟踪它的光线至一个有光泽的不透明表面。<br>在这一点上，此光线可能被识别为一条入射光线的反射，计算这个入射光的方向，并调用另一次递归跟踪这个入射光线。
2. 第二次递归将执行一个类似的任务，去查找它的光线的源头。<br>这个过程可能还会递归调用。

可以用各种条件来终止递归光线跟踪：

* 它们包括：
  * 被跟踪的光线与光源相交；
  * 被跟踪的光线可能存在于不与物体相交的场景中；
  * 递归次数达到了预定的限制。
* 还有一种终止条件是基于所遇到的表面的吸收特性的：
  * 如果表面是高吸收性的（如灰暗不光滑的表面），那么任何入射光线几乎都不会对表面外观产生影响，光线跟踪停止，累积的吸收会产生相似的效果。<br>也就是说，访问多个适度可吸收性的表面后，光线跟踪可以终止。

光线跟踪有一个很大的缺点，那就是费时。因此，光线跟踪没有在“消费者级”实时系统中实现，但在实时性要求不高的“专业水平”的应用中能找到它。

### 10.5.2  辐射度

**辐射度**（radiosity）：通过考虑两个平面片对之间的辐射光能的总和，采取更加区域化的方法。这个辐射的光能本质上就是漫射光。

一个物体辐射的光能，可能是这个物体产生的（如光源这种情况）；也可能是这个物体反射的。因此，每个物体的外观都要通过考虑从其他物体接收的光能来确定。

**形状因子**（form factor）是指一个物体辐射的光对另一个物体外观的影响程度。

**渗色**（color bleeding）是指一个物体的颜色影响其周围其他物体的色调。

辐射度存在冥想的优缺点：

* 优点是：
  * 使用辐射度确定物体的外观是不受辐射度影响的。
  * 辐射度捕获了光的许多细微特征。
* 缺点是：
  * 辐射度的计算量非常大，无法满足当前消费市场上图形系统的实时要求。
  * 无法捕获镜面光的细节，这意味着用辐射度渲染的所有表面倾向于灰暗的外观。

