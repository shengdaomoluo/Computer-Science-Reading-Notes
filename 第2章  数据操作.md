# 第2章  数据操作

本章学习计算机如何操控数据以及如何与外围设备（如打印机和键盘）通信。这里所说的操控包括将数据从一个位置移到另一个位置，以及执行各种操作，如算术计算、文本编辑和图像处理。

## 持久理解以下问题

**建立在二进制序列基础上的各种抽象可用于表示所有数字数据**。

**多层抽象用于编写程序或创建其他计算工作**。

**人们编写程序来执行算法**。

**适当的抽象式编程更容易**。

## 2.1  计算机体系结构（不含数据存储系统）

计算机中控制数据操控的电路成为**中央处理器**（central processing unit），即**CPU**，通常简称为*处理器*。

现在，现在台式计算机和笔记本计算机中的 CPU 都被封装成了小巧的正方形薄片，可以放在人的手掌中，它们的连接引脚插在机器主电路板[称为**主板**（motherboard）]的插座上。在智能手机、平板电脑和其他移动计算设备中，CPU 大约是邮票的一半大小。因为这些处理器尺寸小，所以被称为**微处理器**（microprocessor）。

### 2.1.1  CPU基础知识

CPU 有三部分构成：

1. **算术/逻辑单元**（arithmetic/logic unit），它包括在数据上执行运算（如加法和减法）的电路；
2. **控制单元**（control unit），它包含协调机器活动的电路。为了操作存储在主存储器中的数据：
   1. 控制单元要把存储器里的数据传入通用寄存器；
   2. 控制单元要通知算术/逻辑单元哪些寄存器保存了这一数据；
   3. 控制单元要激活算术/逻辑单元中的的有关电路；
   4. 控制单元并要告知算术/逻辑单元哪个寄存器应该接收结果。
3. **寄存器单元**（register unit），它包含称为**寄存器**（register）的数据存储单元（与主存储器存储单元相似），用于 CPU 内部信息的临时存储。在寄存器单元中：
   * 一些寄存器被看作**通用寄存器**（general-purpose register），它用于临时存储 CPU 正在操控的数据；这些寄存器存储算术/逻辑单元电路的输入值以及该单元产生的结果。
   * 其他寄存器责备看作**专用寄存器**（special-purpose register）。

为了传输位模式，机器 CPU 和主存储器通过一组称为**总线**（bus）的线路进行连接。利用总线，CPU 通过提供相关存储单元的地址以及电信号（告知存储器电路，它应该获取指定单元中的数据），从主存储器提取（读取）数据。同理，CPU 可以向主存储器中放入（写入）数据，方法是提供目的单元地址和一起写入的数据以及适当的电信号（告知主存储器，将要存储发送给它的数据）。

基于这个设计，将存储在主存储器中的两个值相加的任务不仅仅涉及执行加法运算。数据必须先从主存储器传输到 CPU 内的寄存器中，值相加后，先把先把结果存放在寄存器中，然后再把结果存储到主存储器的存储单元中。整个过程被总结成 5 个步骤：

1. 步骤一：从存储器中取出一个要加的值放入一个寄存器中。
2. 步骤二：从存储器中取出另一个要加的值放入另一个寄存器中。
3. 步骤三：激活加法电路，以步骤一和步骤二所用的寄存器作为输入，用另一个寄存器存放相加的结果。
4. 步骤四：将结果存入存储器。
5. 步骤五：停止。

### 2.1.2  存储程序概念

将计算机程序存储在主存储器中的思想称为**存储程序概念**（stored-program concept），它已经成为今天使用的标准方法——实际上它的标准性很显然。

#### 高速缓冲存储器

**高速缓冲存储器**（cache /kæʃ/ memory）是位于 CPU 内部的高速存储器的一部分（也许有几百 KB），在这个特殊的存储区域中，机器试图保存主存储器中当前最重要的那部分内容的副本。它使得在寄存器与主存储器之间进行的数据传输变成了*寄存器与高速缓冲存储器*之间的数据传输。因此，对高速缓冲存储器的任何改变都会在恰当的时间被一起传输给主存储器。

高速缓冲存储器与其他存储设备一起构成了一个丰富的存储器层次：

* 寄存器用于存储可立即进行运算的数据。
* 主存储器用于存储即将使用的数据。
* 海量存储器用于存储最近也许不会使用的数据。

## 2.2  机器语言

**机器语言**（machine language）是指可以被 CPU 识别的位模式编码指令以及编码系统。使用机器语言表达的指令称为机器级指令指令，更习惯的称呼为**机器指令**（machine instruction）。

### 2.2.1  指令系统

根据不同的计算机设计思想，产生了两种不同的计算机：

1. **精简指令集计算机**（reduced instruction computer，RISC）这种计算机效率高、速度快、制造成本低，*CPU 只需要执行最小的机器指令集*。
2. **复杂指令集计算机**（complexed instruction computer CISC）这种计算机的设计者认为 *CPU 应该能够执行大量复杂的指令*。

不管是选择 RISC 还是选择 CISC，机器指令都可以分为 3 类：（1）数据传输类；（2）算术/逻辑类；（3）控制类。

### 2.2.2  数据传输类

**数据传输类指令**是将数据从一个位置移动到另一个位置的请求指令组成。执行传输类指令的过程更像是复制数据而不是移动数据，因此，使用诸如*复制*（copy）或*克隆*（clone）之类的术语能更好地描述这类指令的活动。

在 CPU 与主存储器之间的数据的传输时，有专门的术语：

* 用存储单元的内容填充通用寄存器的请求称为加载（LOAD）指令。
* 将寄存器中的内容传输给存储单元的请求称为存储（STORE）指令。

在数据传输类中，有一组与 CPU-主存储器环境之外的设备（打印机、键盘、显示屏、磁盘驱动器等）进行通信的重要指令。它们被称为**I/O 指令**（I/O instruction），负责处理机器的输入/输出（I/O）活动。

### 2.2.3  算术/逻辑类

**算术/逻辑类指令**由告诉控制单元请求在算术/逻辑单元内实现一个活动的指令组成。算术/逻辑类指令不仅可以完成*算术运算*（如：加法和减法等），还能完成*布尔运算*（AND、OR 和 XOR）还能完成另外一种运算——能让寄存器中的内容在寄存器中左右移动，即*移位运算*（SHIFT）或*循环运算*（ROTATE），前者丢弃一端“移出的位”，而后者将它们（“移出的位”）放到另一段留出的空位上。

### 2.2.4  控制类

**控制类指令**由*指导程序执行*而非数据操作的指令组成。 

```markdown
步骤1：把存储器中的一个值加载到一个寄存器中。		# 注意：术语“加载”（LOAD）
步骤2：把存储器中的另一个值加载到另一个寄存器中。		# 注意：术语“加载”（LOAD）
步骤3：如果第二个值为0，那么转移到步骤6。			# 注意：条件跳转。
步骤4：用第一个寄存器中的值除以第二个寄存器中的值，结果留在第三个寄存器中。
步骤5：把第三个寄存器中的值存储到存储器中。			# 注意：术语“存储”（STORE）。
步骤6：停止。
```

上图中的“步骤 5”术语控制类指令，但它是一个很初级的例子。这一类指令包括机器指令系统中许多比较有趣的指令，如*跳转*（JUMP）或*分支*（BRANCH）系列指令，用于指示 CPU 执行列表中下一条指令以外的其他指令。这些指令分为两种：**无条件跳转**（unconditional jump）和**条件跳转**（conditional jump）。两者的区别是，*只有满足某个条件时，条件跳转才会引起“地点改变”*。无条件跳转的例子是指令“跳转到步骤 5”；条件跳转的一个例子是指令“如果所得值为 0，就跳转到步骤 5”。

### 2.2.5  Vole：一种演示用的机器语言

机器指令的编码形式包括两部分:

1. **操作码（operation code）**字段。
   * 操作码字段中的位模式指明该指令要求的是什么基本运算。
   * 如`STORE`、`SHIFT`、`XOR` 和 `JUMP`。

2. **操作数（operand）**字段。
   * 操作数字段中的位模式提供操作码指定运算的更详细信息。
   * 以`STORE`操作为例，其操作数字段中的信息指示哪个寄存器包含将被存储的数据，哪个存储单元用于接收该数据。

## 2.3  程序执行

计算机总是按照需要把存储器里的指令复制到 CPU 中来执行存储器中的程序。每个指令一旦到达 CPU，就会被译码并执行。从存储器中取指令的顺序与这些指令存储在存储器中的顺序是相对应的，除非`JUMP`指令更改了这个顺序。

CPU 内部有两个专用寄存器：

1. **指令寄存器（instruction register）**。
   * 指令寄存器用于存储正在执行的指令。
2. **程序计数器（program counter）**。
   * 程序计数器中包含的是要执行的下一条指令的地址，因此它用于以机器方式跟踪程序执行到了什么地方。

CPU 通过不断重复执行一个算法来完成它的工作，该算法引导它完成一个称为**机器周期（machine cycle）**的 3 步过程。该机器的 3 步是取指、译码和执行。

在取指步骤，CPU 请求主存储器给它提供程序计数器指定的地址中存储的指令。CPU 将从存储器中读取的指令存放在它的指令寄存器中，然后将程序计数器的值加 2，使得程序计数器包含存储器中存储的下一条指令的地址。这时，程序计数器为下一次取指做好了准备。

由于指令现在已经存入了指令寄存器，CPU 对该指令译码，其中包括根据该指令的操作码将操作数字段分解为适当的部分。

然后，CPU 激活相应电路以执行指令，完成所要求的任务。

一旦指令寄存器中的指令执行完毕，CPU 又将从取指步骤开始进入下一个机器周期。

```
1. 取指：根据程序计数器规定的地址从存储器中取下一条指令，然后增加程序计数器的值。
2. 译码：对指令寄存器中位模式进行译码。
3. 执行：实现指令寄存器里指令所规定的动作。

1. 取指
2. 译码
3. 执行
……
```

### 比较计算机能力

购买个人计算机时，人们通常用时钟速度来比较计算机。计算机的**时钟（clock）**是一个称为振荡器的电路，能生成用于协调机器活动的脉冲——这个振荡器电路生成脉冲的速度越快，机器执行其机器周期的速度就越快。

遗憾的是，不同的 CPU 设计在一个时钟周期里完成的工作量是不同的。因此，在比较具有不同 CPU 的机器时，单单比较时钟速度没有太大的意义。如果你在比较两台机器，一台基于英特尔处理器，一台基于 ARM 处理器，那么通过**基准测试（benchmark）**来比较它们的性能才更有意义。

基准测试是指，让不同的机器执行同一程序（称为基准）来比较其性能的过程。通过选择代表不同类型应用的基准，就能够得到对各个细分市场有意义的比较结果。

### 2.3.1  程序执行的一个例子

如果我们需要遵照详细的指令列表执行程序，那么存储在存储器里的程序的执行过程就如同你我都可以做的那样。我们可以通过标记指令来确定执行到的位置，而 CPU 则利用程序计数器来确定执行到的位置。在确定下一步要执行哪条指令后，我们需要读该指令并分析它的含义，然后实现所请求的任务并返回到指令的列表，为下一条指令做准备。这与 CPU 执行其指令寄存器中的指令然后从另一个取指步骤开始继续进行的方式是一样的。

###  2.3.2  程序与数据

许多程序可以同时存储在一个计算机的主存储器里，只要它们的地址不同。机器开启时运行哪个程序可以通过适当地设置程序计数器来确定。

然而，我们必须记住：数据也存储在主存储器中，也是用 0 和 1 编码的，所以机器自己无法知道什么是数据，什么是程序。如果程序计数器被赋予了数据的地址而非所希望的程序的地址，那么在没有更好选择的情况下，该 CPU 会想取指指令一样提取此数据的位模式并执行它们。最终的结果取决于所涉及的数据。

然而，我们不应该就此得出结论，以为将程序和数据以相同的形式存入机器的存储器是不好的。事实上，这已经被证明是一个有用的特性，因为它使得每个程序可以操控其他程序（甚至是自己），就像它可以操控数据一样。例如，设想有这样一个程序，它可以根据其与环境的交互自我修正，从而表现出学习能力；或者设想有这样一个程序，它可以编写和执行其他程序，以解决所遇到的问题。

## 2.4  算术/逻辑指令

算术/逻辑指令由请求算术、逻辑和移位的指令组成。

### 2.4.1  逻辑运算

逻辑运算`AND`（与）、`OR`（或）和`XOR`（异或，一般读作“ex-or”），它们都是组合两个输入的二进制位，得到一个输出的二进制位。这些运算可以扩展成为这样的**按位运算（bitwise operation）**：组合两个二进制位串，在各个列上应用基本运算，产生一个输出串。

> 逻辑运算的规则：
>
> 1. 与运算（`AND`）：两个输入都为 1 时，结果为 1，否则为 0。
> 2. 或运算（`OR`）：任意一个输入为 1，结果为 1。只有两个输入为 0 时，结果为 0。
> 3. 异或运算（`XOR`）：两个输入值不同，结果为 1；相同，结果为 0。

`AND`运算的一个主要用途是将位模式的一部分设为 0，而不干扰另一部分。还可以用于复制一个位串的一部分，方法是将不复制的部分置 0。

`OR`运算也可用于复制一个串的一部分，但方法是将不复制的部分置 1。

`XOR`运算的一个主要用途是形成一个位串的补码。

### 2.4.2  循环移位运算及移位运算

循环移位运算及移位运算提供了移动寄存器内二进制位的方法，常用于解决对齐问题。这些运算根据移动方向（向右或向左）和其过程是否循环来分类。

如果我们将其内容向右移一位，我们想象最右端的位被移出边界，最左端出现一个空位。对于这个额外的位及留出的空位如何操作是区别各种移动运算的特征：

* 一种技术是将右端移出的位置在左端的空位上，这类运算称为**循环移位（circular shift 或 rotation）**
* 另一种技术是丢弃移出边界的位，并用 0 填充空位，这类运算称为**逻辑移位（logical shift）**。

无论哪种移位，在使用某些记数法系统时都必须注意保留符号位。因此，我们经常发现，右移时留出的空位（在符号位的位置出现）总是用它原来的值来填。

* 保留符号位不变的移位称为**算术移位（arithmetic shift）**。

### 2.4.3  算术运算

减法运算可以通过加法及取负来模拟；乘法只不过就是反复进行加法运算；除法就是反复进行减法运算。因此，一些小型 CPU 的设计中只有加法指令，或者只有加法和减法指令。

每种算术运算都有许多变体。

## 2.5  与其他设备通信

主存储器和 CPU 构成了计算机的核心。

### 2.5.1  控制器的作用

计算机与其他设备的通信通常是通过称为**控制器（controller）**的中间设备来处理的。对于台式计算机，控制器可能由计算机主板上固定安装的电路组成，有时为了灵活，控制器还会以扩展板的形式插在主板的插槽中。无论哪种形式，控制器都是通过电缆与计算机机箱里的外围设备，或者是计算机背面称为**端口（port）**的连接器相连接的，端口上可以插其他设备。这些控制器有些本身就是小型计算机，每个都有其自己的存储电路和简单的 CPU，可以执行引导控制器活动的程序。

控制器将*信息*和*数据*在两种形式之间来回翻译：

* 一种是与计算机内部特征相适应的形式。
* 另一种是与所连接外围设备相适应的形式。

**通用串行总线（universal serial bus，USB）**、**高清晰度多媒体接口（high definition multimedia interface，HDMI）**和**DisplayPort**的后续更快版本都是被广泛采用的将外围设备连接到计算机的标准。

> **DisplayPort** 是一种数字显示接口标准，用于连接计算机与显示器、以及其他视频显示设备。

每个控制器与其所在计算机的通信，都是通过连接到连接计算机的 CPU 和主存储器的总线来实现的。从这个位置，每个控制器都能够监视到 CPU 与主存储器之间正在发送的信号，还能够将自己的信号传入总线。<br>通过这种安排，CPU 能够以与主存储器通信的方式与连接到总线上的控制器通信。为了给控制器发送一个位模式，首先要在 CPU 的其中一个通用寄存器中构建该位模式，然后由 CPU 执行一个类似`STORE`指令的指令，将该位模式“存储”到控制器中。同样，从一个控制器接收一个位模式时，要使用一条类似`LOAD`指令的指令。

### 2.5.2  直接存储器存取

因为控制器是链接到计算机的总线上的，所以控制器能在 CPU 不使用总线的几纳秒时间里实现与主存储器的通信。控制器这种存取主存储器的能力称为**直接存储器存取（direct memory access DMA）**，它是计算机性能的重大资本。

### 2.5.3  握手

**握手（handshaking）**双向对话，计算机和外围设备通过这种双向对话交换设备的状态信息，协调它们的活动。握手通常涉及一个**状态字（status word）**，它是由外围设备生成并发送给控制器的一个位模式。该状态字是一个位图，其中的位反映该设备的各种情况。

### 2.5.4  流行的通信媒介

两个**计算设备**之间的通信可通过两种途径来处理：并行和串行。这两个术语指的是信号传输的方式。

> 计算设备是指任何能够执行计算任务、处理数据或执行逻辑操作的设备。<br>计算设备在日常生活中无处不在，从个人计算设备如手机和电脑，到家用设备如智能电视，再到专业设备如服务器和工作站。

**并行通信（parallel communication）**是指若干信号同时传输，每个信号都在各自的“线路”上。

* 这种技术传输数据的速度快，但是需要相对复杂的通信路径。

**串行通信（serial communication）**是指信号在一条线路上一个接一个地传输。

* 这种技术只需一条相对简单的数据路径。

### 2.5.5  通信速率

位从一个计算部件传到另一个计算部件的速率是以**位每秒**（又称比特每秒）（bits per second，bit/s）计量的。通用的单位有：

* **Kbit/s**：是 kilo-bps 的简写，即千位每秒，等于 10<sup>3</sup>bit/s。
* **Mbit/s**：是 mega-bps 的简写，即兆位每秒，等于 10<sup>6</sup>bit/s。
* **Gbit/s**：是 giga-bps 的简写，即吉位每秒，等于 10<sup>9</sup>bit/s。

注意：位与字节之间的区别，8Kbit/s 相当于 1KB/s。使用简写形式时，小写的英文字母 b 通常表示*位（bit）*，而大写的英文字母 B 通常表示*字节（byte）*。

一个特定设置可获得的最大速率，取决于通信路径的种类以及其实现过程中使用的技术。这个最大速率通常大致等同于通信路径的**带宽（bandwidth）**，但带宽这个术语除了传输速率这个含义还有容量的含义。<br>也就是说，说一条通信路径具有高带宽（或提供**宽带**服务）意味着这条通信路径能以高速率传输位，同时意味着该通信路径能够同时携带大量的信息。

## 2.6  数据操控编程

> 数据操控编程，通常是指一种编程方法或实践，它主要专注于如何获取、处理、转换和分析数据。这个过程涉及从多个来源获取数据，并对数据进行清洗、转换和分析，以提取有价值的信息或生成新的数据结构。数据操控编程在数据科学、数据分析、数据库管理等领域非常重要。

* 一种程序设计语言的代码经常会被翻译成要在机器上执行的另一种（较低级的）语言的代码。
* 程序指令是顺序执行的。

### 2.6.1  逻辑运算和移位运算

逻辑运算和移位运算可以在任何类型的数值数据上执行。

```python
x = 0b00110011
mask = 0b00001111
```

### 2.6.2  控制结构

`if`语句，它允许代码段在脚本中的布尔值不为真的情况下被条件性跳过。

```python
if (water_temp > 140):
  print('Bath water too hot!')
```

循环结构`while`，它允许一个代码段被执行多次，一般受某个条件控制。

```python
while (n < 10):
  print(n)
  n = n + 1
```

### 2.6.3  函数

* 函数式程序设计指令的一个命名分组。

* 函数是可（重）复（使）用的程序设计抽象。

* 函数简化了编写和维护程序的复杂性。

函数的语法是，在函数名称后面紧接着放一个开括号，然后给出函数**参数值**，最后跟一个闭括号。

* **参数值（argument value）**，用于在对函数定义进行求值时插入参数。
  * 在程序中调用过程时，参数提供不同的值作为过程的输入。
  * 参数通过允许使用函数而不是复制代码来归纳解决方案。

函数有许多种类。有些函数有多个参数。如`max()`函数：

```python
x = 1034
y = 1056
z = 2078
biggest = max(x, y, z)
print(biggest) # prints "2078"
```

有些函数**返回（return）**值，也就是说，这种函数调用本身可以出现一个在更复杂的表达式中，或者出现在赋值语句的右边。这些函数有时称为**有返回值函数（fruitful function）**。不返回值的函数有时称为**无返回值函数（void function）**或**过程（procedure）**。过程有名称，可能还有参数和返回值。

Python 库模块包含许多可能一般用不到但但需要的时候就可以调用的有用函数。

```python
# Calculates the hypotenuse(/haɪˈpɒt.ən.juːz/,直角三角形的斜边)of a right triangle(/ˈtraɪ.æŋ.ɡəl/,三角形)
import math

sideA = 3.0
sideB = 4.0
# Calculate third side via Pythagorean Theorem(/paɪˌθæɡ.ər.əs ˈθɪə.rəm/，毕达哥拉斯定理)
hypotenuse = math.sqrt(sideA**2 + sideB**2)

print(hypotenuse)
```

在这个例子中，`import语句`预先警告了 Python 解释器：这个脚本引用的是名为“math”的库，它恰巧是 Python 配备的一组标准库模块中的一个。`math 库模块`定义的`sqr()`函数提供了参数的平方根，在这个例子里，参数是表达式“sideA 的平方加上 sideB 的平方”。注意，库函数调用包括模块名（math）和函数名（sqrt），中间由一个点连接。

### 2.6.4  输入和输出

许多程序设计语言都提供了类似的实现输入和输出的机制，为程序员提供了一个方便的抽象，用于将数据移入或移出计算机处理器。事实上，这些 I/O 内置函数都是与硬件控制器及 2.5 节《与其他设备通信》讨论的外围设备进行通信的。

简单的用户输入可以用 Python 内置的`input()`函数来完成。

```python
echo = input('Please enter a string to echo:')
print(echo * 3)
```

`input()`函数把提示字符串当作可选参数，在等待用户输入时呈现给用户。当这个脚本运行时：

* 在显示完“`Please enter a string to echo: `”这个提示后，脚本会暂停，等待用户键入内容。
* 当用户按下回车键时，脚本将键入的字符组成的字符串（不包括回车键）赋给变量`echo`。
* 接着，脚本的第二行"`print(echo * 3)`"将这个字符重复输出 3 次。

现在有了获取输入的能力，让我们重写前面的斜边脚本来提示用户输入边长，而不是将值“硬”编码到赋值语句中。

```python
# Calculates the hypotenuse of a right triangle
import math


# Inputting the side lengths, first try
sideA = input('Length of side A? ')
sideB = input('Length of side B? ')
# Calculate third side via Pythagorean Theorem
hypotenuse = math.sqrt(sideA**2 + sideB**2)

print(hypotenuse)
```

运行时，这个脚本提示用户：“Length of side A？”，并等待输入。假设用户输入“3”并回车。该脚本继续提示用户：“Length of side B？”，并等待输入。假设用户键入“4”并回车。此时，Python 解释器中止脚本，打印输出：

hypotenuse = math.sqrt(sideA ** 2 + sideB ** 2)
TypeError: unsupported operand type(s) for ** or pow(): 'str' and 'int'

像 Python 这样的动态类型语言很容易产生这种类型的错误。我们的斜边计算在这个脚本的早期版本中能正常工作，现在把值改为由用户输入后就会导致错误。这个问题实际是一个“`TpyeError`”错误，这个错误源于 Python 不再知道如何获取变量`sideA`的平方这一事实，因为现在这个脚本中的`sideA`是一个字符串，而不是之前那样的整数。这个问题不是在这个脚本中计算斜边的那一行产生的，而是在前面`input()`返回给`sideA`和`sideB`的值时产生的。这个问题也是使用程序设计语言出错时常见的问题。Python 解释器试图提供这个程序出错的脚本行，但真正导致出错的行实际上在这行脚本的前面。

在上面的字符串回显代码段中，很明显，赋给`echo`的值应该是用户键入的字符串。虽然程序员的目的现在是输入整数值，但`input()`函数的行为方式和斜边程序中的一样。字符串“4”的 ASCII 或 UTF-8 编码表示与整数 4 的二进制补码表示不同，在使用整数继续进行计算前，该 Python 脚本必须显示地将一种表示转换为另一种表示。

幸运的是，另一个内置函数提供了这种功能。`int()`函数会尝试将其参数转换为整数表示。如果无法转换，会产生相应的错误信息。它是通过仔细检查其字符串参数并将其解释为数来执行转换的。如果输入的字符串是一个数，但不是整数，如“3.14”，那么`int()`函数会丢弃小数部分，只返回整数值。这是一个截断操作，不会像人类期望的那样“向上舍入”。

在这个脚本中，至少有三个地方可以使用`int()`函数来修正 bug。我们甚至可以在将`input()`函数的结果赋给变量之前就调用它（`int()`）。我们可以只为调用函数添加新脚本行，还可以只在对 `math.sqrt()`函数调用里的变量进行平方运算之前进行转换。

* 第一种方法是：

  ```python
  # Calculates the hypotenuse of a right triangle
  import math
  
  
  # Inputting the side lengths, first try
  sideA = int(input('Length of side A? '))
  sideB = int(input('Length of side B? '))
  # Calculate third side via Pythagorean Theorem
  hypotenuse = math.sqrt(sideA**2 + sideB**2)
  
  print(hypotenuse)
  ```

* 第二种方法是：

  ```python
  # Calculates the hypotenuse of a right triangle
  import math
  
  
  # Inputting the side lengths, first try
  sideA = input('Length of side A? ')
  sideB = input('Length of side B? ')
  # Calculate third side via Pythagorean Theorem
  hypotenuse = math.sqrt(int(sideA)**2 + int(sideB)**2)
  
  print(hypotenuse)
  ```

Python 中所有的其他标准值类型都有类似的转换函数。

### 2.6.5  马拉松训练助手

下面这个脚本能根据跑步者想跑的距离和*步速*，帮助跑步者计算他需要多久的训练性锻炼。给定步速和总里程，该脚本计算跑步锻炼的预计跑行时间，以及一个以英里每小时为单位的用户友好的速度。

> 步速（pace）：跑 1 英里的分钟数和秒数。

```python
# Marathon training assistant.
import math

# This function converts a number of minutes and seconds into just seconds.
def total_seconds(min, sec):
  return min * 60 + sec

# This function calculates a speed in miles per hour given
# a time (in seconds) to run a single mile.
def speed(time):
  return 3600 / time

# Prompt user for pace and mileage.
pace_minutes = int(input('Minutes per mile? '))
pace_seconds = int(input('Seconds per mile? '))
miles = int(input('Total miles? '))

# Calculate and print speed.
mph = speed(total_seconds(pace_minutes, pace_seconds))
print('Your speed is ')
print(mph)

# Calculate elapsed time for planned workout.
total = miles * total_seconds(pace_minutes, pace_seconds)
elapsed_minutes = total // 60
elapsed_seconds = total % 60

print('You total elapsed time is ')
print(elapsed_minutes)
print(elapsed_seconds)
```

上面的脚本既使用了内置函数`input()`、`int()`和`print()`，又使用了用户定义的函数`speed()`和`total_seconds()`。

关于用户自定义的函数，如`speed()`和`total_seconds()`：

* 关键字`def`的后面是用户函数定义；

  再后面是：

  * 函数调用时要提供的函数名称
  * 参数列表；

* 接下来的缩进行称为函数**体（body）**，表示的是定义该函数的步骤。

另外还有注意这个脚本中函数调用堆叠的方式：

* `input()`函数调用的结果被立即当作参数传递给`int()`函数；
  * 然后`int()`函数的结果被赋值给一个变量。

同样道理：

* `total_second()`函数的结果也被立即当作参数传递给`speed()`函数
  * `speed()`函数的结果随后又被赋值给变量`mph`。

在这两种情况下，都允许一次调用一个函数，将结果赋值给新变量，然后调用下一个依赖头一个结果的函数。

## <sup>*</sup>2.7  其他体系结构

为了拓宽视角，我们来考虑一些已经讨论过的传统计算机体系结构的替代方案。

### 2.7.1  流水线

**吞吐量（throughput）**：机器在给定时间内可以完成的工作总量。

**流水线（pipelining）**技术，该技术允许一个机器周期内的各个步骤重叠进行。特别是，当执行一条指令时，可以取下一条指令，这意味着在任何一个时刻可以有不止一条指令在“流水线”上，每条指令处在不同的处理阶段。

### 2.7.2  多处理器机器

可以把流水线技术看作迈向**并行处理（parallel processing）**的第一步，并行处理是若干活动在同一时间里实现的性能。然而，真正的并行处理需要多个处理单元，于是产生了称为多处理器或**