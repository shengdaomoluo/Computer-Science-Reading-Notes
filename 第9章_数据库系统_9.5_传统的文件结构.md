## *9.5  传统的文件结构

文件结构代表着数据存储和检索的历史开端，现在的数据库技术就是由此发展而来的。为这些文件结构开发的许多技术是构建今天大规模复杂的数据库的重要工具。

### 文件相关知识概述

**文件**（file /faɪl/）：在计算机科学中，是指用于存储数据的一种逻辑单位。也是操作系统管理存储数据的基本单位，同时是用户和程序交互的重要部分。

#### 一、文件的基本概念

##### 1. 文件名

文件通常有一个唯一的名称，用来标识它在文件系统中的位置。文件名可以包含扩展名，用于提示文件的类型或格式（例如，`.txt`表示文本文件；`.jpg`表示图片文件）。

##### 2. 文件路径

文件路径指的是在文件系统中的具体位置。路径可以是绝对路径（从根目录开始）或相对路径（从当前目录开始）。

##### 3. 文件类型

文件类型通常由文件扩展名标识，决定文件的内容格式和操作方式。例如，`.docx`文件通常由文字处理软件打开。

##### 4. 文件系统

文件系统是操作系统管理和组织文件的结构和方法。它提供了创建、删除、读取和写入文件的功能，以及访问控制和权限管理。

文件是存储和组织数据的基本单位，操作系统通过文件系统提供对文件的管理和操作。理解文件的概念和操作不仅是计算机科学初学者的基本技能，也是日常计算机使用的重要组成部分。通过文件，用户和程序可以有效地存储、访问和管理大量信息。

### 9.5.1  顺序文件

**顺序文件**（sequential[/sɪˈkwen.ʃəl/ 按特定顺序的、连续的] file）：是一种从头到尾以顺序方式访问的文件，好像文件中的信息都排成一行。顺序文件中的数据必须以能保持文件顺序的方式记录在海量存储器里。<br>顺序文件是存储那种处理的次序就是其文件存储次序的数据的理想选择。

通常，把顺序文件的末尾称为**文件结束**（end-of-file, EOF）。有许多标识 EOF 的方法：

* 在文件的末尾放置一个特殊的记录——**哨兵**（Sentinel）。
* 利用操作系统的目录系统中的信息来标识一个文件的 EOF。<br>由于操作系统知道哪些扇区包含有此文件，因此它也就知道这个文件在什么地方终止。

### 9.5.1  索引文件

当文件中的记录必须以一种不可预测的次序进行检索时，一种流行的解决方案是对文件使用检索，这与书本里的索引用来定位主题在书中位置的方式是非常一致的。这种文件系统称为**索引文件**（indexed file）。

文件的索引包含存储在该文件中的键的列表和指示包含每个键的记录存储位置的项。因此，要找到某个记录，需要先在索引中找到标识键，再检索存储在与那个键相关的位置处的信息块。

文件的索引通常作为一个单独的文件与被索引的文件存储在同一个海量存储设备里。在索引文件处理开始前，通常要先将索引传送到主存储器中，以便在需要访问文件中的记录时，能很容易地访问它。

索引文件的一个突出的例子就是大多数操作系统为组织文件存储所采用的分层目录系统。<br>在这种情况下，目录（或文件夹）起的是索引的作用，而每个索引又包含了指向其子索引的链接。<br>从这个角度来看，整个文件系统知识一个大型的索引文件。

### 9.5.3  散列文件

**散**（sàn）：分散，强调数据的分散和非结构化特性。

列：体现了将数据组织成一种结构形式。

**散列**（hashing，也称哈希）：是一种技术，提供类似于检索机制的访问效果。散列系统概括为：<br>数据存储空间被分成若干称为**桶**（bucket）的区，每个桶都能放入记录；它允许通过键值来定位记录；记录根据散列函数的算法分散存储在这些桶中。<br>要想得到一个有效的散列系统，应该将要存储的记录均匀地分布在这些桶中。

**散列函数**（hash function）：是指将键的值转换为桶号的算法。

要检索一条已经置于这种存储结构中的记录，首先要对该记录的标识键应用散列函数，以确定相应的桶，然后检索桶中的内容，最后从检索到的数据中搜索所需的记录。

```mermaid
graph LR
A((用户检<br>索记录)) -->|<samll>应用散列函数<br>查找标识键</small>| B[<big>**确定相应的桶**</big>] -->C[**<big>检索桶中的内容</big>**] -->|<small>从检索到的数据中检索</small>| D[**<big>所需的记录</big>**]
```

**散列文件**（hash file）：是指将散列用于海量存储器中的存储结构得到的结果。

**散列表**（hash table）：是指将散列用于主存储器中得到的结果。

散列不仅是构建高效数据存储系统的一种手段，还是认证因特网传送消息的一种手段。

##