## 8.3  数据结构的实现

本节的目标是理解如何将处理这些数据结构的程序翻译成用来处理存放在主存储器中的数据的机器语言程序。

### 8.3.1  存储数组

>  数组（array）：是一组“矩形的”数据块，它的每一项数据都具有相同的数据类型。

### 8.3.2 存储聚合

> 聚合：是一个由数据项组成的块，其中的各个数据项可能具有不同的类型和大小。块里各个数据项通常称为字段（field）。

### 8.3.3  存储列表

> 列表（list）：它是一个集合，其表项按照顺序排列。

**邻接表**（contiguous /kənˈtɪɡ.ju.əs/ list）：整个列表都存储在一大块存储单元中，其连续的项依次存放在相邻的存储单元中。

### 8.3.4  存储栈和队列

> 栈（stack）：是一种列表，其项只能在表头进行添加和删除。
>
> 队列（queue）：是一种列表，其项只能从表头移除，其新项只能从表尾添加。

就栈而言，所预留的存储块的大小要足够容纳其大小达到最大时的栈。将这个存储块指定为栈底，入栈的第一个项就存储在这里。

队列的传统实现方法类似于栈的实现方法，也是在主存储器中预留一块连续的存储单元，其大小要足以容纳其大小达到最大时的队列。然而，就队列而言，需要在队列的两端进行操作，所以不能像栈那样只用一个指针，这里需预留两个存储单元用作指针。

* 一个指针称为**头指针**（head pointer），用来记录队列的队头；
  * 要从队列中移除一个项，需要先读取头指针指向的那个项，然后调整头指针，令其指向队列中的下一项。
* 另一个指针称为**尾指针**（tail pointer），用来记录队列的队尾。
  * 每次向队列中插入项时，就将该项放在由尾指针指向的位置，然后调整尾指针，令其指向下一个未用位置。
  * 通过这种方式，尾指针始终指向队尾的第一个空闲处。

至此，所描述的这种存储系统存在一个问题，即随着项的插入和移除，队列会在存储器中慢慢移动。这样一来，就需要一种机制，使队列保持在预留的存储块中。这个问题的解决方案很简单，就是让队列自始至终都在所分配的存储块中移动。

* 于是，当队尾到达队列的末端时，插入新项的操作就要回到存储块的起始端进行，这时的起始端是空闲的。
* 同样，当存储块中最后一项成为队头并被移除时，就将头指针调回到存储块的开头，此时其他项正在那里等待。

用这种方式，队列在存储块内按环状依次排列，就像存储块的尾部被连接起来形成了一个环。这种技术所实现的队列称为**环状队列**（circular queue）。

### 8.3.5  存储二叉树

> 树（tree）：是一种集合，其项具有层次化的组织形式。

二叉树的每个节点至多有两个孩子。这种树通常采用类似于链表所用的链式结构存储在存储器中。然而，二叉树的每个项（或称节点）不是由两个元素组成的，而是由 3 个元素组成的：

1. 数据；
2. 指向该节点的第一个孩子的指针；
3. 指向该节点的第二个孩子的指针。

尽管在机器中不存在左右之分，但这里为了方便，称第一个指针为**左孩子指针**（left child pointer），而称另一个指针为**右孩子指针**（right child pointer），这样我们就可以方便地在纸上画出树的图形了。因此，二叉树的每个节点可由一个短的、连续的存储单元来表示。

要在存储器中存储树：

* 首先要找到可用的存储单元块来存放节点，
* 然后依据想要的树结构将这些节点链接起来。
  * 每个指针必须设置成指向相关节点的左孩子或者右孩子，如果树的某个方向不再有节点，则将相应的指针赋值为`null`——这意味着，终端节点的特征是其两个指针的值都为 null。
* 最后，留出一个专门的存储器位置，称为**根指针**（root pointer），用来存储根节点的地址。

对树的访问就是从根指针开始的。

对于二叉树的存储，除了用链式结构，还可以用单个的连续存储单元来存储整棵树。

1. 把树的根节点存储在这个存储块的第 1 个单元中。
2. 把根的左孩子存储在第 2 个单元中。
3. 把根的右孩子存储在第 3 个单元中。
4. 一般而言，继续把在单元 n 中找到的节点的左孩子和右孩子分别存储在单元 2n 和 2n+1 中。
5. 在存储块中，没有被树用到的单元就用一个特别的位模式来标识，表示这个位置没有数据。

这个存储系统本质上是从上到下将树中各层的节点作为存储段来存储，一层接一层。也就是说，存储块中的第1 项是根节点，接下来是根的孩子，然后是根的孙子，依次类推。

### 8.3.6  操控数据结构

为了允许用户将数据结构当作一种抽象工具来访问，必须对用户屏蔽实际存储系统的复杂性。这意味着，用户给出的以及按照抽象工具的方式规定的指令必须转换成适合实际存储系统的步骤。

```python
def PrintList(List):
    CurrentPointer =List.Head
    while (CurrentPointer != None):
        print(Currentpointer.Value)
        CurrentPointer = CurrentPointer.Next
```

上面展示了一个名为`printlist()`的用来打印值的链表的函数。这个函数假设称为`List`的聚合中的`Head`字段指向了链表中的第一项，并且链表中的每个项都由两部分组成：值（`Value`）和指向下一项的指针（`Next`）。Python 的特殊值`None`被用作空指针。<br>一旦这个函数开发完成，就可以作为一个抽象工具用来打印链表，而务须关心打印链表所需的实际步骤。

例如，要获得 Economics 301班的打印班级列表，用户只需执行下面的函数调用就能得到想要的结果：`printList(Economics301ClassList)`此外，如果我们以后想改变列表的实际存储方式，那么只需改变`printList()`函数的内部动作，用户仍然可以继续使用以前的那个函数调用来完成列表打印。

