# 第12章  计算理论

计算理论是计算机科学的理论基础，正是计算理论为计算机科学奠定了其真正的科学地位。

本章讨论的是有关计算机能做什么、不能做什么的问题，即**研究计算机的能力**。

## 12.1  函数及其计算

从数学意义上讲，**函数**（function）是一组可能的输入值和一组可能的输出值之间的对应关系，它使每个可能的输入被赋予单个的输出。

**函数的计算**是指对于一个给定的输入，确定其具体输出值的过程。<br>对函数进行计算的能力非常重要，因为我们能解决问题的手段就是计算函数。因此，计算机科学的一个基本任务就是找出要解决的问题背后的函数的技术。

* 计算函数的一个比较简便的方法是简化为**搜索表**的过程：
  * 将函数的输入和输出预先记录在一个表中；每当需要函数的输出时，我们只需在表中查找给定的输入就能找到所需的输出。
  * 虽然这种系统很方便，但功能有限，因为许多函数无法完全表示成表格的形式。
* 计算函数的一个比较有效的方法是采用**代数式**的方法：
  * 即遵循代数式所提供的方向，将可能的输入/输出组合表示计算出来。
  * 有些函数的函数的输入/输出关系太过复杂，一直不能用代数运算来描述。

<big>*不管函数的复杂性如何，我们是否总能找到一个系统来计算它们？*<br>*答案是*:**否定的**。</big>

**不可计算函数**：是指函数的计算超出了任何算法系统的能力范围而无法计算的函数。

**可计算函数**：是指可以依据输入值通过算法来确定其输出值的函数。

机器只能执行由算法描述的任务，所以对可计算函数的研究即是对机器能力的研究。

**基本知识点**

* 可决定问题是可以构造算法来对所有输入回答“是”或“否”的问题。
  * 例如：这个数字是偶数吗？
* 不可决定问题是构造不粗总能产生正确的是或否答案的算法的问题。

## 12.2  图灵机

图灵机是被用作研究算法能力的一种工具。

### 12.2.1  图灵机的原理

**图灵机**（Turing machine）是由一个控制单元组成的，它能够通过一个读/写磁头对磁带上的符号进行读和写。<br>磁带两端可以无限延伸，并分成一个个单元，而每个单元可以包含符号的任意一个有限集合，这个集合称为机器的字母表。

图灵机的计算过程：

* 图灵机的计算开始于一个特定的状态，称为初始状态；停止于另一个特定的状态，称为停止状态。
* 图灵机的计算由机器的控制单元执行的一系列步骤组成：
  * 每一步都观察当前磁带单元的符号；
  * 将观察到的符号写进这个单元；
  * 可能要将读/写磁头左移或右移一个单元；
  * 接下来改变状态；
* 要执行的确切操作是由程序决定的，程序通过机器的状态和磁带当前单元的内容来告诉控制单元做什么。

## 12.2  丘奇—图灵论题

**图灵可计算函数**：是指以图灵机的方式计算的函数。

图灵机的计算能力囊括了任何算法系统的能力，也就是说，所有可计算函数的解都能够被图灵可计算函数表示。**可计算函数等同于图灵可计算函数**。

**图灵机被确立为一个标准**，如果一个计算系统能够计算所有的图灵可计算函数，那么这个计算系统就和其他任何计算系统一样强大。